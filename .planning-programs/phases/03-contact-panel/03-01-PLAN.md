---
phase: 03-contact-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/components/ui/sheet.tsx
  - dashboard/src/app/dashboard/programs/[id]/program-detail-content.tsx
  - dashboard/src/app/dashboard/programs/components/contact-panel/contact-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking a registrant row opens a slide-out panel from the right"
    - "Panel is 600px wide on desktop, full width on mobile"
    - "Panel can be closed via X button or clicking overlay"
    - "Panel displays selected registrant's name in header"
  artifacts:
    - path: "dashboard/src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component with side variants"
      exports: ["Sheet", "SheetContent", "SheetHeader", "SheetTitle", "SheetDescription", "SheetClose"]
    - path: "dashboard/src/app/dashboard/programs/components/contact-panel/contact-panel.tsx"
      provides: "Main contact panel wrapper component"
      min_lines: 40
  key_links:
    - from: "program-detail-content.tsx"
      to: "contact-panel.tsx"
      via: "Sheet controlled open state"
      pattern: "selectedRegistration.*Sheet"
---

<objective>
Create Sheet component foundation and wire up row click to open Contact Panel slide-out.

Purpose: Establishes the slide-out panel infrastructure that will house all contact details. This is the entry point for Phase 3 - users will click a row and see the panel open.

Output: Working slide-out panel that opens when clicking any registrant row, showing the registrant's name as confirmation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-programs/PROJECT.md
@.planning-programs/ROADMAP.md
@.planning-programs/STATE.md
@.planning-programs/phases/03-contact-panel/03-RESEARCH.md

# Existing files to modify
@dashboard/src/app/dashboard/programs/[id]/program-detail-content.tsx
@dashboard/src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shadcn Sheet component</name>
  <files>dashboard/src/components/ui/sheet.tsx</files>
  <action>
Create Sheet component based on shadcn/ui pattern. Uses existing @radix-ui/react-dialog primitives.

Sheet component structure:
- Sheet (Root) - wraps content
- SheetTrigger - opens sheet (optional, we use controlled mode)
- SheetClose - close button
- SheetPortal - portal for rendering
- SheetOverlay - backdrop with fade animation
- SheetContent - main content area with slide animation

Key configuration:
- Support `side` prop: "top" | "bottom" | "left" | "right" (default: "right")
- Right side animation: slide-in-from-right, slide-out-to-right
- Width for right side: `w-full sm:w-[600px]` (responsive per RESEARCH.md pitfall #1)
- Add `overflow-y-auto` to SheetContent for scrolling
- Include SheetHeader, SheetTitle, SheetDescription, SheetFooter exports

Follow existing dialog.tsx patterns for cn() usage and animation classes.
Use Tailwind animate-in/animate-out classes from dialog.tsx as reference.
  </action>
  <verify>
File exists at dashboard/src/components/ui/sheet.tsx.
Exports: Sheet, SheetTrigger, SheetClose, SheetPortal, SheetOverlay, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription.
TypeScript compiles: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>Sheet component created with all exports, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create ContactPanel wrapper component</name>
  <files>dashboard/src/app/dashboard/programs/components/contact-panel/contact-panel.tsx</files>
  <action>
Create the ContactPanel component that will be rendered inside the Sheet.

Props interface:
```typescript
interface ContactPanelProps {
  registration: RegistrationRosterItem;
  onClose: () => void;
}
```

Initial implementation (placeholder sections):
- Import from @/components/ui/sheet
- SheetHeader with SheetTitle showing registrant name
- SheetDescription showing company name
- Placeholder div for "Person Hero section coming in Plan 02"
- Placeholder div for "Registration section coming in Plan 02"
- Placeholder div for "Payment section coming in Plan 02"
- Placeholder div for "Company section coming in Plan 02"
- Placeholder div for "Engagement section coming in Plan 03"

Use 'use client' directive since it will have interactive elements.
Add basic section styling: space-y-6 for vertical spacing between sections.
  </action>
  <verify>
File exists at dashboard/src/app/dashboard/programs/components/contact-panel/contact-panel.tsx.
Component exports ContactPanel and accepts registration prop.
TypeScript compiles: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>ContactPanel component created with placeholder sections, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 3: Wire up row click to open Sheet</name>
  <files>dashboard/src/app/dashboard/programs/[id]/program-detail-content.tsx</files>
  <action>
Update ProgramDetailContent to render the Sheet with ContactPanel.

Changes:
1. Import Sheet, SheetContent from '@/components/ui/sheet'
2. Import ContactPanel from '../components/contact-panel/contact-panel'
3. Keep existing selectedRegistration state (already there)
4. Remove the toast.info call in handleRowClick - replace with just setSelectedRegistration(registration)
5. Keep the triggerEnrichment function (will be used by ContactPanel later)
6. Add Sheet component after the Tabs, controlled by selectedRegistration:

```tsx
<Sheet
  open={selectedRegistration !== null}
  onOpenChange={(open) => !open && setSelectedRegistration(null)}
>
  <SheetContent side="right" className="w-full sm:w-[600px] overflow-y-auto">
    {selectedRegistration && (
      <ContactPanel
        registration={selectedRegistration}
        onClose={() => setSelectedRegistration(null)}
      />
    )}
  </SheetContent>
</Sheet>
```

This replaces the temporary toast with the actual panel.
  </action>
  <verify>
Run dev server and test: Click any registrant row in a program's Registrations tab.
Expected: Sheet slides in from right, shows registrant name in header.
Close: Click X or click overlay, sheet closes.

Manual verification steps:
1. Navigate to /dashboard/programs
2. Click any program to open detail
3. Click any registrant row
4. Verify panel slides in from right (600px wide on desktop)
5. Verify registrant name appears in panel header
6. Click X to close, verify panel slides out
  </verify>
  <done>
Row click opens slide-out panel showing registrant name.
Panel closes via X button or overlay click.
No toast notification on row click (replaced with panel).
  </done>
</task>

</tasks>

<verification>
- [ ] Sheet component exists with all exports (Sheet, SheetContent, SheetHeader, SheetTitle, etc.)
- [ ] ContactPanel component renders inside Sheet
- [ ] Row click triggers panel open (no more toast)
- [ ] Panel width is 600px on desktop, full width on mobile
- [ ] Panel closes via X button
- [ ] Panel closes via overlay click
- [ ] TypeScript compiles without errors
- [ ] No console errors when opening/closing panel
</verification>

<success_criteria>
PROG-17 partially complete: Clicking registrant row opens Contact Panel slide-out.
Panel infrastructure ready for content sections in Plan 02.
</success_criteria>

<output>
After completion, create `.planning-programs/phases/03-contact-panel/03-01-SUMMARY.md`
</output>

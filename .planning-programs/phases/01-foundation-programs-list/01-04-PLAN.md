---
phase: 01-foundation-programs-list
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - dashboard/src/app/dashboard/programs/components/program-filters.tsx
  - dashboard/src/app/dashboard/programs/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can filter programs by start date range"
    - "Date filter updates URL with dateFrom/dateTo params"
    - "Selected date range persists on page reload"
  artifacts:
    - path: "dashboard/src/app/dashboard/programs/components/program-filters.tsx"
      provides: "Date range picker inputs in filter panel"
      contains: "dateFrom"
    - path: "dashboard/src/app/dashboard/programs/page.tsx"
      provides: "URL param parsing for dateFrom/dateTo"
      contains: "dateFrom"
  key_links:
    - from: "dashboard/src/app/dashboard/programs/components/program-filters.tsx"
      to: "URL search params"
      via: "router.push with dateFrom/dateTo"
      pattern: "params\\.set\\('dateFrom'"
    - from: "dashboard/src/app/dashboard/programs/page.tsx"
      to: "dashboard/src/lib/api/programs-queries.ts"
      via: "getProgramsList with dateFrom/dateTo params"
      pattern: "dateFrom.*dateTo"
---

<objective>
Add date range filter to the ProgramFilters component to close the gap identified in verification.

Purpose: The API already supports dateFrom/dateTo parameters, but the UI has no date picker. This plan adds date inputs to the filter panel so users can filter programs by start date range.
Output: Working date range filter integrated into the programs list filtering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-programs/PROJECT.md
@.planning-programs/AUTONOMOUS-BUILD-GUIDE.md
@.planning-programs/phases/01-foundation-programs-list/01-03-SUMMARY.md

Gap being closed:
- VERIFICATION.md identified that API supports dateFrom/dateTo but UI has no date picker
- ProgramFilters component currently only has city, format, status dropdowns

Existing implementation:
@dashboard/src/app/dashboard/programs/components/program-filters.tsx
@dashboard/src/app/dashboard/programs/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date range inputs to ProgramFilters component</name>
  <files>dashboard/src/app/dashboard/programs/components/program-filters.tsx</files>
  <action>
Update the ProgramFilters component to include date range inputs (From and To).

1. Add `dateFrom` and `dateTo` to the `currentFilters` interface
2. Add two date input fields in the filter panel grid (make it 4 columns to fit)
3. Update the `updateFilter` function to handle date params
4. Update `hasActiveFilters` to include date filters
5. Update `clearFilters` to clear date params

Use native HTML date inputs (`<input type="date">`) for simplicity - they work well and don't require additional dependencies. Style them to match the existing Select components.

Key changes:
- Interface: Add `dateFrom: string | null` and `dateTo: string | null` to currentFilters
- Grid: Add two date input columns after Status filter
- Handler: `updateFilter('dateFrom', value)` and `updateFilter('dateTo', value)`
- Display: Labels "From" and "To" above inputs
- Styling: Match h-9 height, text-sm, border styling of Select components

Example date input styling:
```tsx
<input
  type="date"
  value={currentFilters.dateFrom || ''}
  onChange={(e) => updateFilter('dateFrom', e.target.value)}
  className="h-9 w-full rounded-md border border-input bg-background px-3 py-1 text-sm shadow-sm focus:outline-none focus:ring-1 focus:ring-ring"
/>
```
  </action>
  <verify>
TypeScript compiles: `cd dashboard && npx tsc --noEmit`
Filter panel shows date inputs when opened
  </verify>
  <done>
- ProgramFilters has From and To date inputs
- Date inputs update URL params on change
- hasActiveFilters includes date range
- Clear button resets date params
  </done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx to parse and pass date params</name>
  <files>dashboard/src/app/dashboard/programs/page.tsx</files>
  <action>
Update the page component to:

1. Add `dateFrom` and `dateTo` to the searchParams interface
2. Parse these values in the ProgramsDataLoader
3. Pass them to getProgramsList (already supported by the function)
4. Pass them to ProgramsContent's currentFilters prop

Changes needed:

In PageProps interface:
```typescript
dateFrom?: string;
dateTo?: string;
```

In filters object:
```typescript
dateFrom: params.dateFrom,
dateTo: params.dateTo,
```

In ProgramsContent currentFilters prop:
```typescript
dateFrom: filters.dateFrom || null,
dateTo: filters.dateTo || null,
```
  </action>
  <verify>
TypeScript compiles: `cd dashboard && npx tsc --noEmit`
Navigate to /dashboard/programs?dateFrom=2026-01-01&dateTo=2026-03-01 - list should filter
  </verify>
  <done>
- page.tsx parses dateFrom/dateTo from URL
- Values passed to getProgramsList
- Values passed to ProgramsContent for display in filter panel
  </done>
</task>

</tasks>

<verification>
1. Navigate to http://localhost:3000/dashboard/programs
2. Click "Filters" button - panel should show date inputs (From, To)
3. Enter a date in "From" field - URL updates with ?dateFrom=YYYY-MM-DD
4. Enter a date in "To" field - URL updates with ?dateTo=YYYY-MM-DD
5. Programs list filters to show only programs starting in that range
6. Refresh page - date values persist in inputs
7. Click "Clear" - date params removed from URL
</verification>

<success_criteria>
- Date range filter appears in filter panel
- Selecting dates updates URL with dateFrom/dateTo params
- Programs list filters correctly based on date range
- Date selection persists on page reload
- Clear button resets date filters
- Gap closed: "Filter by date range works" (PROG-07)
</success_criteria>

<output>
After completion, create `.planning-programs/phases/01-foundation-programs-list/01-04-SUMMARY.md`
</output>

---
phase: 12-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/planning/migrate/page.tsx
  - dashboard/src/app/dashboard/planning/migrate/migrate-content.tsx
  - dashboard/src/app/dashboard/planning/migrate/actions.ts
  - dashboard/src/app/dashboard/planning/migrate/components/project-selector.tsx
  - dashboard/src/app/dashboard/planning/migrate/components/migration-preview.tsx
  - dashboard/src/app/dashboard/planning/migrate/components/migration-status.tsx
autonomous: true

must_haves:
  truths:
    - "User can see list of old dev_projects available for migration"
    - "User can select which projects to migrate"
    - "User can preview how project data will map to Planning Studio"
    - "User can execute migration and see progress/results"
    - "Migrated projects appear in Planning Studio pipeline"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/migrate/page.tsx"
      provides: "Migration page entry point"
      exports: ["default"]
    - path: "dashboard/src/app/dashboard/planning/migrate/actions.ts"
      provides: "Server actions for fetching old projects and migrating"
      exports: ["getOldProjects", "migrateProject", "migrateMultipleProjects"]
    - path: "dashboard/src/app/dashboard/planning/migrate/migrate-content.tsx"
      provides: "Main migration UI with selection and execution"
      min_lines: 100
  key_links:
    - from: "migrate-content.tsx"
      to: "actions.ts"
      via: "server action calls"
      pattern: "migrateProject|getOldProjects"
    - from: "actions.ts"
      to: "supabase"
      via: "schema queries"
      pattern: "dev_projects.*planning_studio"
---

<objective>
Build an interactive migration UI that allows the user to selectively migrate projects from the old Development Dashboard (dev_projects table) into the Planning Studio schema.

Purpose: The user wants to preserve valuable project data while transitioning to the new Planning Studio. Migration is selective - not all projects need to be migrated.

Output: A one-time-use migration page at /dashboard/planning/migrate with project selection, preview, and batch migration capability.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/phases/12-migration-cleanup/12-CONTEXT.md
@.planning-planning-studio/phases/12-migration-cleanup/12-RESEARCH.md

# Reference existing patterns
@dashboard/src/app/dashboard/planning/page.tsx
@dashboard/src/app/dashboard/planning/planning-content.tsx
@dashboard/src/lib/api/planning-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions for Migration</name>
  <files>
    dashboard/src/app/dashboard/planning/migrate/actions.ts
  </files>
  <action>
Create server actions file with:

1. `getOldProjects()` - Fetch all projects from dev_projects table:
   - Query: id, project_key, project_name, description, status, created_at, completed_at
   - Order by created_at DESC
   - Return typed array

2. `migrateProject(oldProjectId: string)` - Migrate single project:
   - Fetch old project from dev_projects
   - Map status using STATUS_MAP from RESEARCH.md:
     - 'idle' -> { status: 'idea', phase: 'capture' }
     - 'executing' -> { status: 'planning', phase: 'discover' }
     - 'needs_input' -> { status: 'planning', phase: 'discover' }
     - 'blocked' -> { status: 'planning', phase: 'discover' }
     - 'complete' -> { status: 'shipped', phase: 'package' }
   - Insert into planning_studio.projects with mapped fields
   - Create initial phase record for current_phase
   - Call revalidatePath('/dashboard/planning')
   - Return { success: boolean, projectId?: string, error?: string }

3. `migrateMultipleProjects(projectIds: string[])` - Batch migration:
   - Loop through projectIds calling migrateProject
   - Collect results for each
   - Return array of results

Type definitions:
```typescript
interface OldProject {
  id: string;
  project_key: string;
  project_name: string;
  description: string | null;
  status: string;
  created_at: string;
  completed_at: string | null;
}

interface MigrationResult {
  oldProjectId: string;
  success: boolean;
  newProjectId?: string;
  error?: string;
}
```

Use `createServerClient()` from `@/lib/supabase/server` for Supabase access.
  </action>
  <verify>TypeScript compiles with `cd dashboard && npx tsc --noEmit`</verify>
  <done>Server actions can fetch old projects and migrate them to planning_studio schema</done>
</task>

<task type="auto">
  <name>Task 2: Migration UI Components</name>
  <files>
    dashboard/src/app/dashboard/planning/migrate/components/project-selector.tsx
    dashboard/src/app/dashboard/planning/migrate/components/migration-preview.tsx
    dashboard/src/app/dashboard/planning/migrate/components/migration-status.tsx
  </files>
  <action>
Create three UI components:

**project-selector.tsx**
- Props: `{ projects: OldProject[], selected: string[], onSelectionChange: (ids: string[]) => void }`
- Render checkbox list of old projects
- Show: project_name, description (truncated), status badge, created_at
- "Select All" / "Clear Selection" buttons
- Visual differentiation for already-completed projects (may want to skip)

**migration-preview.tsx**
- Props: `{ projects: OldProject[] }` (selected ones)
- Show table/list of what will be created:
  - Old Name -> New Title
  - Old Status -> New Status + Phase
  - Created date preserved
- Summary: "X projects will be migrated"

**migration-status.tsx**
- Props: `{ results: MigrationResult[], isRunning: boolean }`
- While running: spinner, "Migrating X of Y..."
- After complete: list of results with success/failure indicators
- Show error messages for failed migrations
- "View in Planning Studio" link when complete

Use existing UI primitives:
- Card, CardHeader, CardContent from `@/dashboard-kit/components/ui/card`
- Checkbox from `@/dashboard-kit/components/ui/checkbox`
- Button from `@/dashboard-kit/components/ui/button`
- Badge styling similar to pipeline-board status badges
  </action>
  <verify>Components render without errors in browser</verify>
  <done>Three migration UI components exist and can display project selection, preview, and status</done>
</task>

<task type="auto">
  <name>Task 3: Migration Page Assembly</name>
  <files>
    dashboard/src/app/dashboard/planning/migrate/page.tsx
    dashboard/src/app/dashboard/planning/migrate/migrate-content.tsx
  </files>
  <action>
**page.tsx** - Server component:
- Fetch old projects via getOldProjects()
- Pass to MigrateContent client component
- Add Suspense with simple loading skeleton

**migrate-content.tsx** - Client component ('use client'):
- State: selectedProjects: string[], results: MigrationResult[], isRunning: boolean, step: 'select' | 'preview' | 'running' | 'complete'
- Three-step flow:

Step 1 - Select:
- Render ProjectSelector
- "Next: Preview" button (disabled if none selected)
- "Back to Planning" link

Step 2 - Preview:
- Render MigrationPreview with selected projects
- "Start Migration" button
- "Back" button to return to selection

Step 3 - Running:
- Render MigrationStatus with isRunning=true
- Call migrateMultipleProjects action
- Update results as they come in (or all at once for simplicity)

Step 4 - Complete:
- Render MigrationStatus with final results
- "View Pipeline" button -> /dashboard/planning
- "Migrate More" button -> reset to step 1

Page styling:
- Same layout pattern as other planning pages (centered content, max-w-4xl)
- Header: "Migrate from Development Dashboard"
- Subheader: "Select projects to bring into Planning Studio"
  </action>
  <verify>
Navigate to /dashboard/planning/migrate, see old projects listed, can select and migrate at least one test project
  </verify>
  <done>Migration page shows old projects, allows selection, executes migration, and shows results</done>
</task>

</tasks>

<verification>
1. Navigate to /dashboard/planning/migrate
2. Old dev_projects appear in the selector
3. Select one or more projects
4. Click through to preview - shows correct status mapping
5. Execute migration - progress displayed
6. Migrated projects appear in /dashboard/planning pipeline
7. No console errors during flow
</verification>

<success_criteria>
- Migration page accessible at /dashboard/planning/migrate
- Old projects displayed with accurate data
- Status mapping matches RESEARCH.md specification
- Migrated projects appear in Planning Studio immediately
- User can migrate multiple projects in one session
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/12-migration-cleanup/12-01-SUMMARY.md`
</output>

---
phase: 12-migration-cleanup
plan: 05
type: execute
wave: 2
depends_on: ["12-02", "12-03"]
files_modified:
  - CLAUDE.md
  - dashboard/docs/PLANNING-STUDIO-API.md
  - dashboard/e2e/tests/performance/page-load.spec.ts
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md includes Planning Studio section with usage instructions"
    - "API documentation covers all Planning Studio endpoints"
    - "Performance benchmarks verify page loads under 1 second"
    - "Documentation is accurate and actionable"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project instructions with Planning Studio section"
      contains: "Planning Studio"
    - path: "dashboard/docs/PLANNING-STUDIO-API.md"
      provides: "API documentation for Planning Studio endpoints"
      min_lines: 100
    - path: "dashboard/e2e/tests/performance/page-load.spec.ts"
      provides: "Performance benchmark tests"
      min_lines: 40
  key_links:
    - from: "CLAUDE.md"
      to: "Planning Studio"
      via: "documentation section"
      pattern: "Planning Studio"
    - from: "page-load.spec.ts"
      to: "Navigation Timing API"
      via: "performance measurement"
      pattern: "performance.*timing|domContentLoaded"
---

<objective>
Create comprehensive documentation and performance benchmarks for Planning Studio.

Purpose: Documentation ensures maintainability and onboarding. Performance benchmarks enforce the <1 second page load target from CONTEXT.md.

Output: Updated CLAUDE.md, API documentation, performance test suite with enforced thresholds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/12-migration-cleanup/12-CONTEXT.md
@.planning-planning-studio/phases/12-migration-cleanup/12-RESEARCH.md
@.planning-planning-studio/phases/12-migration-cleanup/12-03-SUMMARY.md

# Reference existing documentation
@CLAUDE.md

# Reference API routes for documentation
@dashboard/src/app/api/planning/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with Planning Studio Section</name>
  <files>
    CLAUDE.md
  </files>
  <action>
Add a new section to CLAUDE.md documenting Planning Studio. Insert after the "n8n-brain" section and before "Understanding Confirmation".

```markdown
## Planning Studio

Planning Studio is an AI-guided idea-to-production pipeline. Ideas go through 6 phases with enforced incubation periods and semantic memory.

### Phases

| Phase | Purpose | Incubation After |
|-------|---------|------------------|
| CAPTURE | Quick capture before idea escapes | 24 hours |
| DISCOVER | Deep research - problem, user, market | 24-48 hours |
| DEFINE | Narrow the problem, create Lean Canvas | None |
| DEVELOP | Design solution - features, UX, technical | 24 hours |
| VALIDATE | Final gut check and readiness assessment | None |
| PACKAGE | Generate GSD documents for Claude Code | None |

### Key Features

- **Pipeline View:** Kanban board at `/dashboard/planning`
- **AI Conversations:** Claude-guided planning per phase
- **Memory System:** Semantic search across all project history (Cmd+K)
- **Document Generation:** ICP, Lean Canvas, Feature Specs, GSD packages
- **Deep Research:** Perplexity integration for market research
- **Priority Queue:** AI-ranked ready-to-build projects at `/dashboard/planning/queue`
- **Build Tracker:** Track active development progress

### Database Schema

Schema: `planning_studio` in Supabase

| Table | Purpose |
|-------|---------|
| projects | Main project records |
| phases | Phase tracking per project |
| conversations | Chat sessions |
| messages | Individual chat messages |
| memories | Extracted insights with embeddings |
| documents | Generated planning documents |
| research | Perplexity research results |
| user_goals | Goals for AI prioritization |

### API Routes

All routes under `/api/planning/`:

| Route | Method | Purpose |
|-------|--------|---------|
| /projects | GET | List all projects |
| /projects | POST | Create new project |
| /projects/[id] | GET | Get project details |
| /chat | POST | Send message (SSE streaming) |
| /documents | POST | Generate document |
| /research | POST | Trigger Perplexity research |
| /search | POST | Semantic memory search |
| /ask | POST | RAG-based question answering |
| /prioritize | POST | Calculate priority scores |
| /export | POST | Generate GSD package ZIP |

### Workflow

1. **Capture:** Quick capture via modal or API
2. **Plan:** Navigate through phases with AI guidance
3. **Package:** Generate GSD documents when ready
4. **Build:** Export to Claude Code and track progress
5. **Ship:** Mark complete when deployed

### Migration

To migrate from the old Development Dashboard, visit `/dashboard/planning/migrate` and select projects to bring over.
```

Ensure the section follows the existing documentation style and format.
  </action>
  <verify>CLAUDE.md contains Planning Studio section, no syntax errors</verify>
  <done>CLAUDE.md updated with comprehensive Planning Studio documentation</done>
</task>

<task type="auto">
  <name>Task 2: Create API Documentation</name>
  <files>
    dashboard/docs/PLANNING-STUDIO-API.md
  </files>
  <action>
Create detailed API documentation for all Planning Studio endpoints.

```markdown
# Planning Studio API Documentation

> **CEO Summary:** API reference for Planning Studio endpoints. Use these to integrate with external tools or automate workflows.

## Base URL

All endpoints are relative to `/api/planning/`.

## Authentication

All endpoints require Supabase authentication. Include the session cookie or Authorization header from the Supabase client.

---

## Projects

### List Projects

```
GET /api/planning/projects
```

**Query Parameters:**
| Param | Type | Description |
|-------|------|-------------|
| status | string | Filter by status (idea, planning, ready_to_build, building, shipped) |
| limit | number | Max results (default: 50) |

**Response:**
```json
{
  "projects": [
    {
      "id": "uuid",
      "title": "Project Name",
      "one_liner": "Brief description",
      "status": "planning",
      "current_phase": "discover",
      "created_at": "2026-01-28T12:00:00Z",
      "updated_at": "2026-01-28T12:00:00Z"
    }
  ]
}
```

### Create Project

```
POST /api/planning/projects
```

**Request Body:**
```json
{
  "title": "New Project Name",
  "one_liner": "Optional brief description"
}
```

**Response:** Created project object (201)

### Get Project

```
GET /api/planning/projects/[id]
```

**Response:** Full project object with phases and recent conversations

---

## Chat

### Send Message

```
POST /api/planning/chat
```

**Request Body:**
```json
{
  "projectId": "uuid",
  "conversationId": "uuid",
  "message": "User's message"
}
```

**Response:** Server-Sent Events (SSE) stream

**SSE Events:**
| Event | Data | Description |
|-------|------|-------------|
| chunk | `{"content": "..."}` | Partial response text |
| done | `{"messageId": "uuid"}` | Stream complete |
| error | `{"error": "..."}` | Error occurred |
| phase_complete | `{"phase": "discover"}` | Phase completion marker |
| doc_generate | `{"docType": "icp", "title": "..."}` | Document generation suggestion |
| research_suggest | `{"query": "...", "type": "..."}` | Research suggestion |

---

## Documents

### Generate Document

```
POST /api/planning/documents
```

**Request Body:**
```json
{
  "projectId": "uuid",
  "docType": "icp",
  "conversationId": "uuid"
}
```

**Document Types:**
- `icp` - Ideal Customer Profile
- `competitive_intel` - Competitive Analysis
- `lean_canvas` - Lean Canvas
- `problem_statement` - Problem Statement
- `feature_spec` - Feature Specification
- `technical_scope` - Technical Scope
- `gsd_project` - GSD PROJECT.md
- `gsd_requirements` - GSD REQUIREMENTS.md
- `gsd_roadmap` - GSD ROADMAP.md

**Response:**
```json
{
  "id": "uuid",
  "doc_type": "icp",
  "content": "# ICP Document...",
  "version": 1
}
```

### Get Documents

```
GET /api/planning/documents?projectId=[uuid]
```

**Response:** Array of document objects

---

## Research

### Trigger Research

```
POST /api/planning/research
```

**Request Body:**
```json
{
  "projectId": "uuid",
  "conversationId": "uuid",
  "query": "What are the main competitors for...",
  "researchType": "competitive_analysis"
}
```

**Research Types:**
- `icp_deep_dive`
- `competitive_analysis`
- `market_research`
- `user_workflows`
- `technical_feasibility`
- `custom`

**Response:**
```json
{
  "id": "uuid",
  "status": "pending"
}
```

### Get Research Results

```
GET /api/planning/research/[id]
```

**Response:** Research object with results when complete

---

## Search & Ask

### Semantic Search

```
POST /api/planning/search
```

**Request Body:**
```json
{
  "query": "What did we decide about pricing?",
  "projectId": "uuid",
  "limit": 10
}
```

**Response:**
```json
{
  "results": [
    {
      "id": "uuid",
      "content": "We decided to use tiered pricing...",
      "memory_type": "decision",
      "similarity": 0.85,
      "project_title": "SaaS Product"
    }
  ]
}
```

### Ask AI (RAG)

```
POST /api/planning/ask
```

**Request Body:**
```json
{
  "question": "What are the key features we planned?",
  "projectId": "uuid"
}
```

**Response:**
```json
{
  "answer": "Based on your planning sessions...",
  "sources": [
    {
      "memory_id": "uuid",
      "memory_type": "decision",
      "excerpt": "..."
    }
  ]
}
```

---

## Prioritization

### Calculate Priorities

```
POST /api/planning/prioritize
```

**Request Body:**
```json
{
  "projectIds": ["uuid1", "uuid2"]
}
```

**Response:**
```json
{
  "results": [
    {
      "projectId": "uuid1",
      "score": 85.5,
      "reasoning": "High alignment with revenue goals..."
    }
  ]
}
```

---

## Export

### Generate GSD Package

```
POST /api/planning/export
```

**Request Body:**
```json
{
  "projectId": "uuid"
}
```

**Response:** ZIP file download

---

## Database Types

### Project Status Values

| Status | Description |
|--------|-------------|
| idea | Initial capture, not yet planned |
| planning | Active planning in progress |
| ready_to_build | Packaged and prioritized |
| building | Active development |
| shipped | Completed and deployed |
| archived | No longer active |

### Phase Types

| Phase | Description |
|-------|-------------|
| capture | Initial idea capture |
| discover | Research and discovery |
| define | Problem definition |
| develop | Solution design |
| validate | Readiness validation |
| package | GSD export |

### Memory Types

| Type | Description |
|------|-------------|
| decision | Recorded decision |
| inspiration | Idea or inspiration |
| insight | Learning or insight |
| pivot | Direction change |
| research_finding | From Perplexity |
| constraint | Limitation identified |
| user_preference | User preference noted |
| rejection_reason | Why something was rejected |

---

## Error Responses

All endpoints return standard error format:

```json
{
  "error": "Error message",
  "code": "ERROR_CODE"
}
```

**Common Error Codes:**
- `UNAUTHORIZED` - Not authenticated (401)
- `NOT_FOUND` - Resource not found (404)
- `VALIDATION_ERROR` - Invalid request body (400)
- `RATE_LIMITED` - Too many requests (429)
- `INTERNAL_ERROR` - Server error (500)
```

Create the `dashboard/docs/` directory if it doesn't exist.
  </action>
  <verify>File exists at dashboard/docs/PLANNING-STUDIO-API.md, markdown renders correctly</verify>
  <done>API documentation created with all endpoints, request/response formats, and types</done>
</task>

<task type="auto">
  <name>Task 3: Create Performance Benchmark Tests</name>
  <files>
    dashboard/e2e/tests/performance/page-load.spec.ts
  </files>
  <action>
Create performance tests that enforce the <1 second page load target.

```typescript
import { test, expect } from '@playwright/test';

// Target from CONTEXT.md: page loads < 1 second
const LOAD_TIME_THRESHOLD_MS = 1000;

// Pages to test
const PLANNING_PAGES = [
  { name: 'Pipeline', path: '/dashboard/planning' },
  { name: 'Queue', path: '/dashboard/planning/queue' },
  { name: 'Goals', path: '/dashboard/planning/goals' },
  { name: 'Analytics', path: '/dashboard/planning/analytics' },
  { name: 'Migration', path: '/dashboard/planning/migrate' },
];

test.describe('Planning Studio Performance', () => {
  // Run performance tests serially to avoid resource contention
  test.describe.configure({ mode: 'serial' });

  for (const { name, path } of PLANNING_PAGES) {
    test(`${name} page loads under ${LOAD_TIME_THRESHOLD_MS}ms`, async ({ page }) => {
      const startTime = Date.now();

      await page.goto(path);
      await page.waitForLoadState('domcontentloaded');

      const loadTime = Date.now() - startTime;

      console.log(`  ${name} page: ${loadTime}ms`);

      expect(loadTime, `${name} page took ${loadTime}ms (threshold: ${LOAD_TIME_THRESHOLD_MS}ms)`).toBeLessThan(LOAD_TIME_THRESHOLD_MS);
    });
  }

  test('captures detailed Navigation Timing metrics', async ({ page }) => {
    await page.goto('/dashboard/planning');
    await page.waitForLoadState('load');

    const metrics = await page.evaluate(() => {
      const entry = performance.getEntriesByType('navigation')[0] as PerformanceNavigationTiming;
      return {
        // DNS lookup
        dns: Math.round(entry.domainLookupEnd - entry.domainLookupStart),
        // TCP connection
        tcp: Math.round(entry.connectEnd - entry.connectStart),
        // Time to first byte
        ttfb: Math.round(entry.responseStart - entry.requestStart),
        // DOM content loaded
        domContentLoaded: Math.round(entry.domContentLoadedEventEnd - entry.navigationStart),
        // Full page load
        load: Math.round(entry.loadEventEnd - entry.navigationStart),
        // DOM interactive
        domInteractive: Math.round(entry.domInteractive - entry.navigationStart),
      };
    });

    console.log('\n  Performance Metrics:');
    console.log(`    DNS lookup:        ${metrics.dns}ms`);
    console.log(`    TCP connection:    ${metrics.tcp}ms`);
    console.log(`    Time to first byte: ${metrics.ttfb}ms`);
    console.log(`    DOM interactive:   ${metrics.domInteractive}ms`);
    console.log(`    DOM content loaded: ${metrics.domContentLoaded}ms`);
    console.log(`    Full page load:    ${metrics.load}ms`);

    // Key metric: DOM content loaded should be under threshold
    expect(metrics.domContentLoaded, `DOM content loaded took ${metrics.domContentLoaded}ms`).toBeLessThan(LOAD_TIME_THRESHOLD_MS);
  });

  test('project detail page loads under threshold', async ({ page }) => {
    // First, get a project ID from the pipeline
    await page.goto('/dashboard/planning');
    await page.waitForLoadState('domcontentloaded');

    // Get first project link
    const projectLink = page.locator('[data-testid="project-card"] a').first();
    const href = await projectLink.getAttribute('href');

    if (href) {
      const startTime = Date.now();
      await page.goto(href);
      await page.waitForLoadState('domcontentloaded');
      const loadTime = Date.now() - startTime;

      console.log(`  Project Detail page: ${loadTime}ms`);
      expect(loadTime).toBeLessThan(LOAD_TIME_THRESHOLD_MS);
    } else {
      console.log('  No projects available for detail page test');
    }
  });

  test('page load stability (multiple runs)', async ({ page }) => {
    const runs = 3;
    const loadTimes: number[] = [];

    for (let i = 0; i < runs; i++) {
      // Clear cache
      await page.context().clearCookies();

      const startTime = Date.now();
      await page.goto('/dashboard/planning');
      await page.waitForLoadState('domcontentloaded');
      loadTimes.push(Date.now() - startTime);
    }

    const avg = Math.round(loadTimes.reduce((a, b) => a + b, 0) / runs);
    const max = Math.max(...loadTimes);
    const min = Math.min(...loadTimes);

    console.log(`\n  Load time stability (${runs} runs):`);
    console.log(`    Min: ${min}ms`);
    console.log(`    Max: ${max}ms`);
    console.log(`    Avg: ${avg}ms`);
    console.log(`    Variance: ${max - min}ms`);

    // Average should be under threshold
    expect(avg, `Average load time ${avg}ms exceeds threshold`).toBeLessThan(LOAD_TIME_THRESHOLD_MS);

    // Max should not be too far above (allow 50% buffer for variance)
    expect(max, `Max load time ${max}ms exceeds buffer`).toBeLessThan(LOAD_TIME_THRESHOLD_MS * 1.5);
  });
});
```

**Important:** These tests must run against a production build (`npm run build`), not the development server, for accurate performance measurement.
  </action>
  <verify>
`cd dashboard && npm run build && npm run test:e2e -- tests/performance/page-load.spec.ts` passes with all pages under 1 second
  </verify>
  <done>Performance benchmark tests created with 1-second threshold enforcement</done>
</task>

</tasks>

<verification>
1. CLAUDE.md has Planning Studio section with phases, features, API routes
2. dashboard/docs/PLANNING-STUDIO-API.md exists with all endpoints documented
3. Performance tests exist and enforce <1s threshold
4. `npm run test:e2e -- tests/performance/` passes
5. Documentation is accurate (spot-check 2-3 endpoints)
</verification>

<success_criteria>
- CLAUDE.md updated with comprehensive Planning Studio section
- API documentation covers all endpoints with request/response formats
- Performance tests verify all pages load under 1 second
- Documentation follows existing project style
- Tests run successfully against production build
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/12-migration-cleanup/12-05-SUMMARY.md`
</output>

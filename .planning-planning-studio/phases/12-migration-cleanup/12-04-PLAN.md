---
phase: 12-migration-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-03"]
files_modified:
  - dashboard/e2e/tests/planning/capture.spec.ts
  - dashboard/e2e/tests/planning/phases.spec.ts
  - dashboard/e2e/tests/planning/documents.spec.ts
  - dashboard/e2e/tests/planning/queue.spec.ts
  - dashboard/e2e/tests/migration/verify-migration.spec.ts
  - dashboard/src/app/dashboard/planning/planning-content.tsx (data-testid)
  - dashboard/src/app/dashboard/planning/components/project-card.tsx (data-testid)
  - dashboard/src/app/dashboard/planning/[projectId]/project-detail-content.tsx (data-testid)
  - dashboard/src/app/dashboard/planning/queue/queue-content.tsx (data-testid)
autonomous: true

must_haves:
  truths:
    - "E2E tests cover the full planning journey"
    - "E2E tests cover core CRUD operations"
    - "Migration verification test confirms migrated data is accessible"
    - "All tests pass when run against production build"
  artifacts:
    - path: "dashboard/e2e/tests/planning/capture.spec.ts"
      provides: "Capture idea flow tests"
      min_lines: 30
    - path: "dashboard/e2e/tests/planning/phases.spec.ts"
      provides: "Phase navigation tests"
      min_lines: 30
    - path: "dashboard/e2e/tests/migration/verify-migration.spec.ts"
      provides: "Migration verification tests"
      min_lines: 20
  key_links:
    - from: "spec files"
      to: "page objects"
      via: "fixture imports"
      pattern: "import.*auth.*fixtures"
    - from: "components"
      to: "tests"
      via: "data-testid attributes"
      pattern: "data-testid"
---

<objective>
Write comprehensive E2E tests covering the critical Planning Studio flows: capture, phases, documents, queue, and migration verification.

Purpose: Ensure the application works correctly end-to-end. Tests serve as regression protection and documentation of expected behavior.

Output: Test specs covering all critical flows, data-testid attributes added to components for reliable selection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/12-migration-cleanup/12-CONTEXT.md
@.planning-planning-studio/phases/12-migration-cleanup/12-RESEARCH.md
@.planning-planning-studio/phases/12-migration-cleanup/12-03-SUMMARY.md

# Reference page objects
@dashboard/e2e/page-objects/planning-page.ts
@dashboard/e2e/fixtures/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid Attributes to Components</name>
  <files>
    dashboard/src/app/dashboard/planning/planning-content.tsx
    dashboard/src/app/dashboard/planning/components/project-card.tsx
    dashboard/src/app/dashboard/planning/components/pipeline-board.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-detail-content.tsx
    dashboard/src/app/dashboard/planning/queue/queue-content.tsx
  </files>
  <action>
Add data-testid attributes to key elements that page objects need to select. This enables stable test selectors that won't break on CSS changes.

**planning-content.tsx / pipeline-board.tsx:**
- Pipeline board container: `data-testid="pipeline-board"`
- Each column: `data-testid="column-{status}"` (e.g., column-idea, column-planning)
- Capture button: `data-testid="capture-button"`
- Search input: already has placeholder, verify accessible

**project-card.tsx:**
- Card container: `data-testid="project-card"`
- Title link: `data-testid="project-title"` (already has Link)

**project-detail-content.tsx:**
- Header section: `data-testid="project-header"`
- Title: `data-testid="project-title"`
- Current phase indicator: `data-testid="current-phase"`
- Phase progress bar buttons: `data-testid="phase-{phase}"` (e.g., phase-capture, phase-discover)
- Chat input: `data-testid="chat-input"`
- Send button: `data-testid="send-button"`
- Message containers: `data-testid="message-{role}"` (message-user, message-assistant)

**queue-content.tsx:**
- Queue list container: `data-testid="queue-list"`
- Queue items: `data-testid="queue-item"`
- Priority score: `data-testid="priority-score"`
- Start build button: already has text "Start Build"
- Export button: already has text "Export"

These are minimal additions - only add where page objects need them. Prefer role-based selectors (getByRole, getByLabel) where possible.
  </action>
  <verify>Components render without errors, data-testid attributes visible in browser DevTools</verify>
  <done>Components have data-testid attributes for reliable E2E test selection</done>
</task>

<task type="auto">
  <name>Task 2: Write Core Planning Flow Tests</name>
  <files>
    dashboard/e2e/tests/planning/capture.spec.ts
    dashboard/e2e/tests/planning/phases.spec.ts
    dashboard/e2e/tests/planning/documents.spec.ts
  </files>
  <action>
**capture.spec.ts** - Capture idea flow:
```typescript
import { test, expect } from '../../fixtures/auth';
import { uniqueTestName, cleanupTestData } from '../../fixtures/test-data';

test.describe('Capture Idea Flow', () => {
  test.afterAll(async () => {
    await cleanupTestData();
  });

  test('can create a new project via capture modal', async ({ planningPage, page }) => {
    const projectName = uniqueTestName('Capture Test');

    await planningPage.navigate();
    await planningPage.waitForLoad();

    await planningPage.captureIdea(projectName, 'A test project for E2E');

    // Verify project appears in Ideas column
    await expect(page.locator('[data-testid="column-idea"]')).toContainText(projectName);
  });

  test('displays validation error for empty title', async ({ planningPage, page }) => {
    await planningPage.navigate();
    await planningPage.waitForLoad();

    // Open modal and try to submit empty
    await page.getByRole('button', { name: /capture/i }).click();
    await page.getByRole('button', { name: /create/i }).click();

    // Expect validation error
    await expect(page.getByText(/title.*required/i)).toBeVisible();
  });

  test('can search for projects', async ({ planningPage, page }) => {
    const projectName = uniqueTestName('Search Test');

    await planningPage.navigate();
    await planningPage.captureIdea(projectName);

    await planningPage.searchProjects('Search Test');

    // Project should be visible
    const cards = await planningPage.getProjectCards();
    expect(cards.length).toBeGreaterThan(0);
  });
});
```

**phases.spec.ts** - Phase navigation:
```typescript
import { test, expect } from '../../fixtures/auth';
import { createTestProject, cleanupTestData } from '../../fixtures/test-data';

test.describe('Phase Navigation', () => {
  let testProjectId: string;

  test.beforeAll(async () => {
    const project = await createTestProject('Phase Nav Test', 'planning', 'discover');
    testProjectId = project.id;
  });

  test.afterAll(async () => {
    await cleanupTestData();
  });

  test('displays current phase correctly', async ({ projectDetailPage, page }) => {
    await projectDetailPage.navigate(testProjectId);
    await projectDetailPage.waitForLoad();

    const phase = await projectDetailPage.getCurrentPhase();
    expect(phase?.toLowerCase()).toContain('discover');
  });

  test('can view previous phase content', async ({ projectDetailPage, page }) => {
    await projectDetailPage.navigate(testProjectId);
    await projectDetailPage.waitForLoad();

    // Click on capture phase (should be completed)
    await page.getByTestId('phase-capture').click();

    // Should see capture phase content (read-only)
    await expect(page.locator('[data-testid="phase-content"]')).toBeVisible();
  });

  test('shows phase progress bar', async ({ projectDetailPage, page }) => {
    await projectDetailPage.navigate(testProjectId);

    // All 6 phases should be visible in progress bar
    await expect(page.getByTestId('phase-capture')).toBeVisible();
    await expect(page.getByTestId('phase-discover')).toBeVisible();
    await expect(page.getByTestId('phase-define')).toBeVisible();
    await expect(page.getByTestId('phase-develop')).toBeVisible();
    await expect(page.getByTestId('phase-validate')).toBeVisible();
    await expect(page.getByTestId('phase-package')).toBeVisible();
  });
});
```

**documents.spec.ts** - Document generation:
```typescript
import { test, expect } from '../../fixtures/auth';
import { createTestProject, cleanupTestData } from '../../fixtures/test-data';

test.describe('Document Management', () => {
  test.afterAll(async () => {
    await cleanupTestData();
  });

  test('documents panel shows existing documents', async ({ projectDetailPage, page }) => {
    // Create project with status that would have documents
    const project = await createTestProject('Doc Test', 'ready_to_build', 'package');

    await projectDetailPage.navigate(project.id);
    await projectDetailPage.waitForLoad();

    // Documents panel should be visible
    await expect(page.locator('[data-testid="documents-panel"]')).toBeVisible();
  });

  test('can open document preview modal', async ({ projectDetailPage, page }) => {
    const project = await createTestProject('Doc Preview Test', 'ready_to_build', 'package');

    await projectDetailPage.navigate(project.id);

    // Click on a document (if any exist)
    const docButton = page.locator('[data-testid="document-item"]').first();
    if (await docButton.isVisible({ timeout: 2000 }).catch(() => false)) {
      await docButton.click();
      await expect(page.getByRole('dialog')).toBeVisible();
    }
  });
});
```
  </action>
  <verify>`cd dashboard && npm run test:e2e -- tests/planning/` passes</verify>
  <done>Core planning flow tests written and passing</done>
</task>

<task type="auto">
  <name>Task 3: Write Queue and Migration Tests</name>
  <files>
    dashboard/e2e/tests/planning/queue.spec.ts
    dashboard/e2e/tests/migration/verify-migration.spec.ts
  </files>
  <action>
**queue.spec.ts** - Build queue tests:
```typescript
import { test, expect } from '../../fixtures/auth';
import { createTestProject, cleanupTestData, TEST_PREFIX } from '../../fixtures/test-data';

test.describe('Build Queue', () => {
  test.afterAll(async () => {
    await cleanupTestData();
  });

  test('queue page loads and displays projects', async ({ queuePage, page }) => {
    // Create a ready-to-build project
    await createTestProject('Queue Test', 'ready_to_build', 'package');

    await queuePage.navigate();
    await queuePage.waitForLoad();

    // Queue should show the project
    await expect(page.locator('[data-testid="queue-list"]')).toBeVisible();
  });

  test('queue items show priority scores', async ({ queuePage, page }) => {
    await createTestProject('Priority Test', 'ready_to_build', 'package');

    await queuePage.navigate();
    await queuePage.waitForLoad();

    // At least one item should have a priority score
    const scoreElement = page.locator('[data-testid="priority-score"]').first();
    if (await scoreElement.isVisible({ timeout: 2000 }).catch(() => false)) {
      const score = await scoreElement.textContent();
      expect(score).toBeDefined();
    }
  });

  test('can navigate from queue to project detail', async ({ queuePage, page }) => {
    const project = await createTestProject('Nav Test', 'ready_to_build', 'package');

    await queuePage.navigate();
    await queuePage.waitForLoad();

    // Click on project name/link
    await page.getByRole('link', { name: new RegExp(TEST_PREFIX) }).first().click();

    // Should be on project detail page
    await expect(page).toHaveURL(new RegExp(`/dashboard/planning/${project.id}`));
  });
});
```

**verify-migration.spec.ts** - Migration verification:
```typescript
import { test, expect } from '../../fixtures/auth';
import { createClient } from '@supabase/supabase-js';

test.describe('Migration Verification', () => {
  test('migration page loads', async ({ page }) => {
    await page.goto('/dashboard/planning/migrate');

    // Page should load without error
    await expect(page.getByRole('heading', { name: /migrate/i })).toBeVisible();
  });

  test('migration page shows old projects', async ({ page }) => {
    await page.goto('/dashboard/planning/migrate');

    // Should either show projects or empty state
    const hasProjects = await page.locator('[data-testid="old-project-item"]').count() > 0;
    const hasEmptyState = await page.getByText(/no projects/i).isVisible().catch(() => false);

    expect(hasProjects || hasEmptyState).toBeTruthy();
  });

  test('migrated projects are accessible in Planning Studio', async ({ planningPage, page }) => {
    // This test verifies that any previously migrated projects appear correctly
    await planningPage.navigate();
    await planningPage.waitForLoad();

    // Pipeline should render without errors
    await expect(page.locator('[data-testid="pipeline-board"]')).toBeVisible();

    // If there are migrated projects, they should be clickable
    const cards = await planningPage.getProjectCards();
    if (cards.length > 0) {
      // First card should be clickable
      await cards[0].click();
      // Should navigate to detail page
      await expect(page).toHaveURL(/\/dashboard\/planning\/[a-f0-9-]+/);
    }
  });

  test('old development route redirects to planning', async ({ page }) => {
    await page.goto('/dashboard/development');

    // Should redirect to planning
    await expect(page).toHaveURL('/dashboard/planning');
  });
});
```
  </action>
  <verify>
`cd dashboard && npm run test:e2e -- tests/planning/queue.spec.ts tests/migration/` passes
  </verify>
  <done>Queue and migration verification tests written and passing</done>
</task>

</tasks>

<verification>
1. All test files exist in e2e/tests/
2. `cd dashboard && npm run test:e2e` runs all tests
3. Tests pass against production build (npm run build first)
4. No flaky tests (run 3 times to verify stability)
5. Components have data-testid attributes for reliable selection
</verification>

<success_criteria>
- E2E tests cover: capture flow, phase navigation, document viewing, queue functionality
- Migration verification test confirms redirect works and migrated data accessible
- All tests pass consistently
- Tests use page objects and fixtures for maintainability
- data-testid attributes added to components for stable selectors
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/12-migration-cleanup/12-04-SUMMARY.md`
</output>

---
phase: 09-ready-to-build-queue-prioritization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/2026020300_add_pinned_to_projects.sql
  - dashboard/src/dashboard-kit/types/departments/planning.ts
  - dashboard/src/app/dashboard/planning/actions.ts
  - dashboard/src/app/dashboard/planning/goals/goals-content.tsx
  - dashboard/src/app/dashboard/planning/goals/components/goal-form.tsx
  - dashboard/src/lib/api/planning-queries.ts
autonomous: true

must_haves:
  truths:
    - "User can view existing goals on the goals page"
    - "User can add a new goal with type and tier"
    - "User can edit and delete existing goals"
    - "Goal count is limited to 5 active goals in UI"
    - "Only business goal types are available (revenue, strategic, quick_win)"
  artifacts:
    - path: "supabase/migrations/2026020300_add_pinned_to_projects.sql"
      provides: "pinned boolean column on projects table"
      contains: "ALTER TABLE planning_studio.projects ADD COLUMN pinned"
    - path: "dashboard/src/app/dashboard/planning/goals/goals-content.tsx"
      provides: "Goals CRUD UI"
      min_lines: 80
    - path: "dashboard/src/app/dashboard/planning/goals/components/goal-form.tsx"
      provides: "Add/edit goal dialog"
      min_lines: 60
  key_links:
    - from: "goals-content.tsx"
      to: "actions.ts"
      via: "server actions for CRUD"
      pattern: "(createGoalAction|updateGoalAction|deleteGoalAction)"
    - from: "goals-content.tsx"
      to: "planning-queries.ts"
      via: "data fetching"
      pattern: "getGoals"
---

<objective>
Goals management page and database migration for pinned projects.

Purpose: Goals drive priority scoring. Users need to manage business goals before the queue can prioritize projects. The pinned column enables pin-to-top override in the queue.
Output: Working goals CRUD page, pinned column migration, goal-related server actions and queries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-CONTEXT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-RESEARCH.md
@dashboard/src/dashboard-kit/types/departments/planning.ts
@dashboard/src/app/dashboard/planning/actions.ts
@dashboard/src/lib/api/planning-queries.ts
@dashboard/src/app/dashboard/planning/goals/goals-content.tsx
@dashboard/src/app/dashboard/planning/goals/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Types + Server Actions + Queries</name>
  <files>
    supabase/migrations/2026020300_add_pinned_to_projects.sql
    dashboard/src/dashboard-kit/types/departments/planning.ts
    dashboard/src/app/dashboard/planning/actions.ts
    dashboard/src/lib/api/planning-queries.ts
  </files>
  <action>
    1. Create migration `2026020300_add_pinned_to_projects.sql`:
       - `ALTER TABLE planning_studio.projects ADD COLUMN pinned BOOLEAN DEFAULT FALSE;`

    2. Run migration: `cd "/Users/mike/IAML Business OS" && supabase db push`

    3. Update `planning.ts` types:
       - Add `pinned: boolean;` to `PlanningProject` interface (after `incubation_skipped`)
       - Add GOAL_TIERS constant:
         ```typescript
         export const GOAL_TIERS = {
           'must-have': { value: 3, label: 'Must-have', color: 'red' },
           'should-have': { value: 2, label: 'Should-have', color: 'amber' },
           'nice-to-have': { value: 1, label: 'Nice-to-have', color: 'gray' },
         } as const;
         export type GoalTier = keyof typeof GOAL_TIERS;
         export const BUSINESS_GOAL_TYPES: GoalType[] = ['revenue', 'strategic', 'quick_win'];
         export function getTierFromPriority(priority: number): GoalTier {
           if (priority >= 3) return 'must-have';
           if (priority >= 2) return 'should-have';
           return 'nice-to-have';
         }
         ```

    4. Add to `actions.ts`:
       - `createGoalAction(goalType: GoalType, description: string, priority: number): Promise<ActionResult>` — insert into user_goals, revalidatePath('/dashboard/planning/goals')
       - `updateGoalAction(goalId: string, updates: { description?: string; priority?: number; active?: boolean }): Promise<ActionResult>` — update user_goals
       - `deleteGoalAction(goalId: string): Promise<ActionResult>` — delete from user_goals
       - `togglePinAction(projectId: string, pinned: boolean): Promise<ActionResult>` — update projects.pinned
       - `startBuildAction(projectId: string): Promise<ActionResult>` — update status to 'building', set build_started_at, revalidate /dashboard/planning and /dashboard/planning/queue
       - All follow existing ActionResult pattern with try/catch and revalidatePath

    5. Add to `planning-queries.ts`:
       - `getGoals(): Promise<UserGoal[]>` — fetch from user_goals, order by priority DESC, created_at ASC
       - `getActiveGoals(): Promise<UserGoal[]>` — same but filtered to active: true
  </action>
  <verify>
    - TypeScript compiles: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
    - Migration applied successfully (check supabase db push output)
  </verify>
  <done>
    - pinned column exists on projects table
    - GOAL_TIERS, GoalTier, BUSINESS_GOAL_TYPES, getTierFromPriority exported from planning.ts
    - PlanningProject includes pinned field
    - All 5 server actions exist and compile
    - getGoals and getActiveGoals query functions exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Goals Page UI</name>
  <files>
    dashboard/src/app/dashboard/planning/goals/goals-content.tsx
    dashboard/src/app/dashboard/planning/goals/components/goal-form.tsx
  </files>
  <action>
    1. Create `goal-form.tsx`:
       - Dialog component (shadcn Dialog) for add/edit goal
       - Props: `open`, `onOpenChange`, `goal?: UserGoal` (for edit mode)
       - Form fields:
         - Goal type: native select, options from BUSINESS_GOAL_TYPES only (revenue, strategic, quick_win) with labels from getGoalTypeLabel
         - Description: textarea, required, max 200 chars
         - Tier: three radio buttons styled as cards — Must-have (red), Should-have (amber), Nice-to-have (gray). Maps to priority 3/2/1
       - Submit calls createGoalAction or updateGoalAction
       - Show toast on success/error (use sonner if available, else console)

    2. Replace `goals-content.tsx` placeholder with full CRUD:
       - Fetch goals via props (passed from page.tsx server component) or use client fetch
       - Display as vertical list of goal cards, each showing:
         - Goal type badge (colored by type)
         - Description text
         - Tier badge (Must-have red, Should-have amber, Nice-to-have gray)
         - Edit button (opens goal-form in edit mode)
         - Delete button with confirmation (AlertDialog)
       - "Add Goal" button at top, disabled when 5+ active goals exist (show tooltip: "Maximum 5 active goals")
       - Empty state: "No goals yet. Add business goals to prioritize your build queue."
       - Pass goals data from page.tsx using getGoals()

    3. Update `goals/page.tsx` if needed to fetch goals and pass to goals-content.
  </action>
  <verify>
    - `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
    - Dev server renders /dashboard/planning/goals without errors
  </verify>
  <done>
    - Goals page shows list of goals with type, description, tier
    - Can add goal via dialog (only revenue/strategic/quick_win types)
    - Can edit and delete goals
    - Add button disabled at 5 active goals
    - Empty state displays when no goals
  </done>
</task>

</tasks>

<verification>
- Goals page renders at /dashboard/planning/goals
- Can CRUD goals with business-only types
- pinned column exists in database
- All new server actions and queries compile
</verification>

<success_criteria>
- Working goals management page with add/edit/delete
- Goal tiers (Must-have/Should-have/Nice-to-have) map to priority 3/2/1
- Only business goal types offered (revenue, strategic, quick_win)
- Max 5 active goals enforced in UI
- pinned boolean column on projects table
- All server actions for phase 9 created (goals CRUD, pin toggle, start build)
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-01-SUMMARY.md`
</output>

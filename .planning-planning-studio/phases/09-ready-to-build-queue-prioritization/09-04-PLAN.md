---
phase: 09-ready-to-build-queue-prioritization
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx
  - dashboard/src/app/dashboard/planning/queue/components/queue-actions.tsx
autonomous: true

must_haves:
  truths:
    - "User can start a build from the queue"
    - "User can export GSD package from the queue"
    - "User can view project details from the queue"
    - "Start Build transitions project to building status"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/queue/components/queue-actions.tsx"
      provides: "Action buttons for queue items"
      min_lines: 40
  key_links:
    - from: "queue-actions.tsx"
      to: "actions.ts"
      via: "startBuildAction server action"
      pattern: "startBuildAction"
    - from: "queue-actions.tsx"
      to: "/api/planning/documents/export"
      via: "existing export route"
      pattern: "api/planning/documents/export"
---

<objective>
Wire queue item actions: View, Start Build, and Export.

Purpose: The queue needs actionable items. Users should be able to start building directly from the queue, export GSD packages, and navigate to project details.
Output: Fully functional queue item actions with Start Build flow and export reuse.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-CONTEXT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-RESEARCH.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-01-SUMMARY.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-02-SUMMARY.md
@dashboard/src/app/dashboard/planning/actions.ts
@dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx
@dashboard/src/app/dashboard/planning/[projectId]/components/export-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue actions component and Start Build flow</name>
  <files>
    dashboard/src/app/dashboard/planning/queue/components/queue-actions.tsx
    dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx
  </files>
  <action>
    1. Create `queue-actions.tsx` — client component with three action buttons:
       - **View**: Link to `/dashboard/planning/{projectId}` — simple Next.js Link, eye icon
       - **Start Build**: Button that calls startBuildAction(projectId).
         - Shows confirmation AlertDialog: "Start building {title}? This will move the project to active builds."
         - On confirm: call startBuildAction, show loading, on success router.push to /dashboard/planning (pipeline shows building column) or /dashboard/planning/{projectId}
         - On error: show error message
         - Icon: play/hammer icon
       - **Export**: Button that triggers GSD package download.
         - Reuse export logic from ExportPanel: fetch `/api/planning/documents/export?projectId={id}` to get ZIP blob, trigger download via file-saver or createObjectURL pattern.
         - Also include "Copy Claude Code command" — fetch project.claude_code_command and copy to clipboard. If null, generate a default: `claude "Start building {title} — see .planning/ for project docs"`
         - Icon: download icon

    2. Update `queue-item.tsx` to use QueueActions component:
       - Replace placeholder action buttons with QueueActions
       - Pass projectId and project title as props
  </action>
  <verify>
    - `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
    - Dev server: Start Build button shows confirmation and transitions project
    - Export button downloads ZIP
  </verify>
  <done>
    - View button navigates to project detail page
    - Start Build shows confirmation, transitions to building status, navigates away
    - Export downloads GSD ZIP package
    - Copy command copies Claude Code command to clipboard
    - Queue item uses QueueActions component
  </done>
</task>

</tasks>

<verification>
- All three queue actions functional
- Start Build transitions project status correctly
- Export downloads valid ZIP
- Project removed from queue after starting build
</verification>

<success_criteria>
- Start Build transitions project to 'building' status with build_started_at timestamp
- Export reuses existing GSD export infrastructure
- View navigates to project detail
- Confirmation dialog prevents accidental builds
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-04-SUMMARY.md`
</output>

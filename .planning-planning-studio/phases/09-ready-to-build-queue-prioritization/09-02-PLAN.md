---
phase: 09-ready-to-build-queue-prioritization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/planning/queue/page.tsx
  - dashboard/src/app/dashboard/planning/queue/queue-content.tsx
  - dashboard/src/app/dashboard/planning/queue/queue-skeleton.tsx
  - dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx
  - dashboard/src/app/dashboard/planning/queue/components/empty-queue.tsx
autonomous: true

must_haves:
  truths:
    - "User can see ready-to-build projects ranked by priority"
    - "Pinned projects appear first regardless of score"
    - "Each item shows name, score, AI reasoning, goal tag, doc count"
    - "Empty state shows phase counts with guidance"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/queue/queue-content.tsx"
      provides: "Queue list with ranked projects"
      min_lines: 60
    - path: "dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx"
      provides: "Individual queue row"
      min_lines: 40
    - path: "dashboard/src/app/dashboard/planning/queue/components/empty-queue.tsx"
      provides: "Empty state with phase counts"
      min_lines: 30
  key_links:
    - from: "queue/page.tsx"
      to: "planning-queries.ts"
      via: "server component data fetch"
      pattern: "getReadyToBuildQueue"
---

<objective>
Queue page UI displaying ready-to-build projects ranked by priority score.

Purpose: The queue is the primary view for deciding what to build next. Projects that completed planning (PACKAGE phase) appear here sorted by AI priority score with pinned items first.
Output: Queue page with ranked list, project cards, and empty state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-CONTEXT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-RESEARCH.md
@dashboard/src/dashboard-kit/types/departments/planning.ts
@dashboard/src/lib/api/planning-queries.ts
@dashboard/src/app/dashboard/planning/goals/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue page structure and skeleton</name>
  <files>
    dashboard/src/app/dashboard/planning/queue/page.tsx
    dashboard/src/app/dashboard/planning/queue/queue-skeleton.tsx
    dashboard/src/lib/api/planning-queries.ts
  </files>
  <action>
    1. Create `queue/page.tsx` — server component with Suspense wrapping queue-content. Follow exact pattern from goals/page.tsx. Fetch ready-to-build projects and pass as props.

    2. Create `queue-skeleton.tsx` — loading skeleton matching queue layout (5-6 horizontal bars representing queue items).

    3. Add to `planning-queries.ts`:
       - `getReadyToBuildQueue(): Promise<QueueProject[]>` — fetch projects with status 'ready_to_build', ordered by `pinned DESC NULLS LAST, priority_score DESC NULLS LAST`. Also fetch document counts per project. Return enriched type.
       - Add `QueueProject` type to planning.ts or inline:
         ```typescript
         export interface QueueProject extends PlanningProject {
           document_count: number;
         }
         ```
       - `getProjectCountsByStatus(): Promise<Record<ProjectStatus, number>>` — for empty state display
  </action>
  <verify>`cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`</verify>
  <done>Queue page route exists, data fetching functions created, skeleton renders</done>
</task>

<task type="auto">
  <name>Task 2: Queue content, items, and empty state</name>
  <files>
    dashboard/src/app/dashboard/planning/queue/queue-content.tsx
    dashboard/src/app/dashboard/planning/queue/components/queue-item.tsx
    dashboard/src/app/dashboard/planning/queue/components/empty-queue.tsx
  </files>
  <action>
    1. Create `queue-content.tsx` (client component):
       - Receives `projects: QueueProject[]` and `statusCounts: Record<ProjectStatus, number>` as props
       - If no projects, render EmptyQueue with statusCounts
       - Otherwise render header with title "Build Queue" and project count
       - Render vertical list of QueueItem components
       - Include "Refresh Priorities" button (placeholder — wired in plan 03)

    2. Create `queue-item.tsx`:
       - Horizontal card for each project showing:
         - Pin icon (filled if pinned, outline if not) — clickable toggle via togglePinAction
         - Rank number (#1, #2, etc.)
         - Project title (link to /dashboard/planning/[id])
         - Priority score badge (large number, colored: green >70, amber 40-70, red <40). Show "Not scored" in gray if null.
         - One-line AI reasoning text (priority_reasoning field, or "Not yet prioritized" if null)
         - Goal alignment tag if available (from priority reasoning)
         - Document count badge (e.g., "4 docs")
         - Actions: "View" link, "Start Build" button, "Export" button (all placeholder — wired in plan 04)

    3. Create `empty-queue.tsx`:
       - Shows message: "No projects ready to build yet"
       - Shows project counts by status in a simple grid (e.g., "3 Ideas, 2 Planning, 0 Ready")
       - Guidance text: "Complete planning phases to move projects here"
       - Link to pipeline: "View Pipeline" button linking to /dashboard/planning
  </action>
  <verify>
    - `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
    - Dev server renders /dashboard/planning/queue without errors
  </verify>
  <done>
    - Queue page displays ranked list of ready-to-build projects
    - Each item shows rank, title, score, reasoning, doc count
    - Pin toggle works via server action
    - Empty state shows phase counts and guidance
    - Pinned items sort to top
  </done>
</task>

</tasks>

<verification>
- Queue page accessible at /dashboard/planning/queue
- Projects displayed in priority order with pinned first
- Empty state renders when no ready-to-build projects
- Pin toggle functional
</verification>

<success_criteria>
- Queue page renders ranked list sorted by pinned DESC, priority_score DESC
- Each queue item shows: rank, title, score, reasoning, doc count
- Empty state shows project counts by status with link to pipeline
- Pin toggle updates database and re-sorts
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-02-SUMMARY.md`
</output>

---
phase: 09-ready-to-build-queue-prioritization
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - dashboard/src/app/api/planning/prioritize/route.ts
  - dashboard/src/app/dashboard/planning/actions.ts
  - dashboard/src/app/dashboard/planning/queue/queue-content.tsx
autonomous: true

must_haves:
  truths:
    - "Claude calculates priority scores for all ready-to-build projects"
    - "Scores include numeric value (0-100) and one-line reasoning"
    - "Refresh button triggers batch recalculation"
    - "Scores update on the queue page after recalculation"
    - "Auto-recalculates when goals change"
  artifacts:
    - path: "dashboard/src/app/api/planning/prioritize/route.ts"
      provides: "Claude-powered batch priority calculation"
      min_lines: 60
      exports: ["POST"]
  key_links:
    - from: "queue-content.tsx"
      to: "/api/planning/prioritize"
      via: "fetch on Refresh button click"
      pattern: "fetch.*api/planning/prioritize"
    - from: "route.ts"
      to: "planning_studio.projects"
      via: "update priority_score and priority_reasoning"
      pattern: "priority_score|priority_reasoning"
---

<objective>
AI priority calculation via Claude API with batch scoring and refresh button.

Purpose: Priority scores determine queue order. Claude evaluates each project against active goals, considering effort, recency, and doc completeness. Batch calculation is efficient and enables relative comparison.
Output: Working priority calculation API, refresh button wired in queue, auto-recalc on goal change.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-CONTEXT.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-RESEARCH.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-01-SUMMARY.md
@.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-02-SUMMARY.md
@dashboard/src/app/api/planning/ask/route.ts
@dashboard/src/dashboard-kit/types/departments/planning.ts
@dashboard/src/app/dashboard/planning/actions.ts
@dashboard/src/app/dashboard/planning/queue/queue-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Priority calculation API route</name>
  <files>
    dashboard/src/app/api/planning/prioritize/route.ts
  </files>
  <action>
    Create POST `/api/planning/prioritize/route.ts`:

    1. Fetch all projects with status 'ready_to_build' from planning_studio.projects
    2. Fetch all active goals from planning_studio.user_goals (active = true)
    3. Fetch document counts per project from planning_studio.documents
    4. If no projects, return { success: true, updated: 0 }
    5. If no goals, still calculate but weight goal alignment at 0

    6. Build Claude prompt (non-streaming, use claude-sonnet-4-20250514):
       - System: "You are a project prioritization assistant. Score each project 0-100."
       - Include all active goals with tier labels (Must-have/Should-have/Nice-to-have)
       - Include all projects with: title, one_liner, document count, doc types, created_at, days in pipeline
       - Scoring factors: Goal Alignment (40%), Doc Completeness (25%), Effort Estimate (20%), Recency (15%)
       - Request JSON array response: `[{ "project_id": "...", "score": N, "reasoning": "one line", "goal_alignment": "primary goal tag" }]`
       - Use tool_choice or structured output to ensure valid JSON

    7. Parse Claude response, update each project:
       - priority_score = score
       - priority_reasoning = reasoning (include goal_alignment tag)
       - priority_updated_at = now

    8. Return { success: true, updated: N }

    Pattern: Follow `/api/planning/ask/route.ts` for Claude API setup (Anthropic SDK import, API key from env). But do NOT stream — use regular `messages.create()` without stream.
  </action>
  <verify>
    - `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`
    - `curl -X POST http://localhost:3000/api/planning/prioritize` returns valid JSON
  </verify>
  <done>
    - POST /api/planning/prioritize calculates scores for all ready-to-build projects
    - Scores and reasoning saved to projects table
    - Returns count of updated projects
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire refresh button and auto-recalc on goal change</name>
  <files>
    dashboard/src/app/dashboard/planning/queue/queue-content.tsx
    dashboard/src/app/dashboard/planning/actions.ts
    dashboard/src/app/dashboard/planning/goals/goals-content.tsx
  </files>
  <action>
    1. In `queue-content.tsx`, wire the "Refresh Priorities" button:
       - onClick: fetch POST /api/planning/prioritize
       - Show loading spinner during calculation
       - On success: call router.refresh() to reload server data
       - On error: show error toast/message

    2. In `actions.ts`, update createGoalAction, updateGoalAction, deleteGoalAction:
       - After successful mutation, also revalidatePath('/dashboard/planning/queue') so queue reflects goal changes
       - Note: Auto-recalc on goal change means the queue page shows stale scores but prompts user to refresh. True auto-recalc would require calling the prioritize API from within server actions, which is complex. Instead: add a visual indicator "Priorities may be outdated" when goals were updated more recently than priority_updated_at on any project. This is simpler and gives user control.

    3. In `queue-content.tsx`, add "Priorities may be outdated" banner:
       - Show if any project's priority_updated_at is null OR if goals were modified after the oldest priority_updated_at
       - Pass `goalsLastUpdated` timestamp from page.tsx
       - Banner text: "Goals changed since last calculation. Refresh priorities to update scores."
       - Dismiss on successful refresh
  </action>
  <verify>
    - TypeScript compiles
    - Refresh button triggers API call and updates scores
    - Stale banner appears when goals are newer than scores
  </verify>
  <done>
    - Refresh Priorities button calls API and refreshes queue
    - Loading state shown during calculation
    - Stale scores banner appears when goals changed
    - Goal mutations revalidate queue path
  </done>
</task>

</tasks>

<verification>
- Priority scores calculated by Claude API for all ready-to-build projects
- Refresh button works with loading state
- Stale indicator shows when goals changed
- Queue re-sorts after refresh
</verification>

<success_criteria>
- Batch priority calculation via Claude returns scores 0-100 with reasoning
- Scores persist to projects table (priority_score, priority_reasoning, priority_updated_at)
- Refresh button triggers recalculation and updates UI
- Stale banner guides user to refresh after goal changes
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/09-ready-to-build-queue-prioritization/09-03-SUMMARY.md`
</output>

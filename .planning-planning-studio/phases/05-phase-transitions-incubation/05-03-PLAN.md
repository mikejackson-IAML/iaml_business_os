---
phase: 05-phase-transitions-incubation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/project-detail-client.tsx
autonomous: true

must_haves:
  truths:
    - "Incubation overlay shows live countdown timer that updates every minute"
    - "User can jot down ideas/notes during incubation"
    - "Skip incubation button actually clears lock and refreshes page"
    - "Page auto-refreshes when incubation expires"
    - "Notes captured during incubation are persisted as memories"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx"
      provides: "Full incubation UI with countdown, note capture, skip flow"
      min_lines: 80
  key_links:
    - from: "incubation-overlay.tsx"
      to: "actions.ts"
      via: "skipIncubationAction and saveIncubationNoteAction calls"
      pattern: "skipIncubationAction|saveIncubationNoteAction"
    - from: "incubation-overlay.tsx"
      to: "project-detail-client.tsx"
      via: "rendered when isIncubating(project) is true"
      pattern: "isIncubating.*IncubationOverlay"
---

<objective>
Wire the incubation overlay with real functionality: live countdown, idea capture, working skip button, and auto-unlock.

Purpose: The incubation overlay exists from Phase 3 but has placeholder skip logic and no note capture. This plan makes it fully functional.

Output: Working incubation experience with countdown, notes, and skip.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/05-phase-transitions-incubation/05-01-SUMMARY.md
@dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx
@dashboard/src/app/dashboard/planning/[projectId]/project-detail-client.tsx
@dashboard/src/app/dashboard/planning/actions.ts
@dashboard/src/dashboard-kit/types/departments/planning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fully wire incubation overlay with countdown, notes, and skip</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-detail-client.tsx
  </files>
  <action>
**Rewrite `incubation-overlay.tsx`:**

Keep the existing warm/amber visual design but add real functionality:

1. **Live countdown timer:**
   - Use `useEffect` + `setInterval` at 60-second intervals
   - Compute time remaining from `project.phase_locked_until`
   - Display as "Xh Ym remaining" (use existing `getIncubationTimeRemaining` helper for format)
   - When timer reaches 0: call `router.refresh()` to reload server component (which will re-evaluate `isIncubating()` and show conversation instead)
   - Clean up interval on unmount

2. **Idea capture textarea:**
   - Replace the disabled "View Documents" / "Review Conversations" buttons with a note-capture area
   - Simple textarea with placeholder "Jot down any ideas that come to mind..."
   - "Save Note" button below
   - On save: call `saveIncubationNoteAction(project.id, noteText)` imported from actions
   - Show saved notes below the textarea in a simple list (fetch from a new state array, or just show optimistically)
   - Keep it lightweight -- just a textarea, a button, and a list of saved notes
   - Use `useTransition` for the save action pending state

3. **Working skip button:**
   - Keep existing AlertDialog pattern
   - On confirm: call `skipIncubationAction(project.id)` imported from actions
   - On success: call `router.refresh()` to reload page
   - Use `useTransition` for pending state, show "Skipping..." during transition

4. **Props update:**
   Keep same interface `{ project: PlanningProject }`. Use `useRouter` from `next/navigation`.

**Update `project-detail-client.tsx`:**

No structural changes needed -- it already conditionally renders `IncubationOverlay` when `isIncubating(project)` is true. But verify the import path is correct after the rewrite.
  </action>
  <verify>
    Run `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit`
  </verify>
  <done>
    - Countdown timer updates every 60 seconds and auto-refreshes on expiry
    - Notes can be typed and saved (persisted as memories via server action)
    - Skip button calls real server action and refreshes page
    - Warm/amber visual design preserved
    - TypeScript compiles
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Incubation overlay has countdown, note capture, and working skip
- Skip calls skipIncubationAction and refreshes
- Notes call saveIncubationNoteAction
- Auto-refresh on timer expiry
</verification>

<success_criteria>
- Incubation countdown is accurate and updates live
- Ideas captured during incubation are persisted
- Skip works end-to-end (clears lock, refreshes, shows conversation)
- Timer expiry auto-unlocks without manual refresh
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/05-phase-transitions-incubation/05-03-SUMMARY.md`
</output>

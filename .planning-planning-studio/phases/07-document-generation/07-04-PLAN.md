---
phase: 07-document-generation
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/doc-preview-modal.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/doc-editor.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/doc-version-selector.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
autonomous: true

must_haves:
  truths:
    - "User can view any document in a preview modal with rendered markdown"
    - "User can edit document content and save creates a new version"
    - "User can switch between document versions via dropdown"
    - "Documents panel shows all documents for the project"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-preview-modal.tsx"
      provides: "Full-screen modal for viewing documents with markdown rendering"
      min_lines: 50
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-editor.tsx"
      provides: "Textarea editor for document editing"
      min_lines: 30
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-version-selector.tsx"
      provides: "Version history dropdown"
      min_lines: 20
  key_links:
    - from: "dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx"
      to: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-preview-modal.tsx"
      via: "click handler opens modal"
      pattern: "setSelectedDoc|openPreview"
    - from: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-preview-modal.tsx"
      to: "/api/planning/documents/[docId]"
      via: "fetch for version history and content"
      pattern: "fetch.*documents"
---

<objective>
Build document viewing, editing, and version history UI components. Update the existing DocumentsPanel to show real documents and open the preview modal.

Purpose: Users need to view generated documents, browse version history, and make manual edits that create new versions.
Output: Preview modal, editor, version selector, and updated documents panel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/07-document-generation/07-RESEARCH.md
@.planning-planning-studio/phases/07-document-generation/07-02-SUMMARY.md
@dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview modal with version selector</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/doc-preview-modal.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/doc-version-selector.tsx
  </files>
  <action>
**doc-preview-modal.tsx:**
Client component using shadcn Dialog (already in project). Props:
- `document: { id: string; doc_type: string; content: string; version: number; project_id: string } | null`
- `open: boolean`
- `onOpenChange: (open: boolean) => void`
- `onDocumentUpdated: () => void` — callback to refresh parent

Layout:
- DialogContent with max-w-4xl, max-h-[85vh] with overflow-y-auto
- Header: DOC_TYPE_LABELS[doc_type] as title, DocVersionSelector on the right, current version badge "v{N}"
- Body: rendered markdown via ReactMarkdown with remark-gfm (already installed). Use prose styling classes (dark:prose-invert).
- Footer: "Edit" button (pencil icon) to switch to editor mode, "Close" button
- When Edit clicked: replace markdown view with DocEditor component (same modal, toggled state)
- State: `isEditing: boolean`, `editContent: string`, `selectedVersion: number`

**doc-version-selector.tsx:**
Client component. Props:
- `projectId: string`
- `docType: string`
- `currentVersion: number`
- `onVersionSelect: (version: { id: string; version: number }) => void`

On mount, fetch version list: GET `/api/planning/documents/list?projectId=${projectId}&docType=${docType}&version=all`
(Note: adjust the API call to match the [docId] route — may need to use a query-based approach or add a list route. Use whatever the [docId] GET route supports for version listing.)

Render as a native `<select>` dropdown (matching project convention from 02-02 — native select over shadcn Select). Options: "Version N — {formatted date}" for each version. On change, call onVersionSelect with selected version info, then parent fetches that version's content.
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes. Modal renders markdown content with version dropdown.</verify>
  <done>Preview modal shows rendered markdown with version switching capability</done>
</task>

<task type="auto">
  <name>Task 2: Editor component and documents panel update</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/doc-editor.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
  </files>
  <action>
**doc-editor.tsx:**
Client component. Props:
- `initialContent: string`
- `docId: string`
- `onSaved: (newDoc: { id: string; version: number; content: string }) => void`
- `onCancel: () => void`

UI:
- `<textarea>` with monospace font (font-mono), full width/height, dark bg matching theme
- "Save" button (primary) and "Cancel" button
- On Save: PUT to `/api/planning/documents/${docId}` with `{ content: editedContent }`
- On success: call onSaved with response data (parent updates displayed content and version)
- Loading state on Save button while request is in flight
- No rich text — plain textarea per research recommendation

**documents-panel.tsx updates:**
The existing DocumentsPanel shell needs to show real documents. Update it to:
1. Accept `projectId` prop (or get from context/params)
2. Fetch documents for this project: query planning_studio.documents grouped by doc_type, only latest version per type (use a server action or client-side fetch)
3. Render a list of document cards, each showing:
   - Icon (FileText) + DOC_TYPE_LABELS[doc_type]
   - "v{version}" badge
   - "Updated {relative time}" text
   - Click handler: opens DocPreviewModal with this document
4. Empty state: "No documents generated yet. Documents will appear here as you discuss your project with Claude."
5. State: `selectedDoc` for modal, `isModalOpen`
6. Include DocPreviewModal at bottom of component, controlled by selectedDoc state
7. On document updated (from modal save or generation): re-fetch document list
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes. DocumentsPanel shows document list. Clicking opens preview modal. Edit saves new version.</verify>
  <done>Full document UI flow: panel lists documents, click opens preview with markdown, edit saves new version, version history navigable</done>
</task>

</tasks>

<verification>
- Documents panel lists all generated documents for the project
- Clicking a document opens the preview modal with rendered markdown
- Version selector shows all versions and switching loads correct content
- Edit mode shows textarea, save creates new version, modal updates
- Empty state shown when no documents exist
</verification>

<success_criteria>
- View documents with rendered markdown in modal
- Edit documents and save as new version
- Browse version history via dropdown
- Documents panel reflects real data from database
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/07-document-generation/07-04-SUMMARY.md`
</output>

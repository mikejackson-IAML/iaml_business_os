---
phase: 07-document-generation
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - dashboard/src/app/api/planning/chat/route.ts
  - dashboard/src/lib/planning/system-prompts.ts
  - dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/doc-suggestion-card.tsx
autonomous: true

must_haves:
  truths:
    - "Claude suggests document generation via HTML comment markers during chat"
    - "Chat route detects markers and emits doc_suggestion SSE events"
    - "User sees inline approval card and can approve or dismiss"
    - "Approving triggers generation and refreshes document list"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/doc-suggestion-card.tsx"
      provides: "Inline approval UI for document generation suggestions"
      min_lines: 30
    - path: "dashboard/src/app/api/planning/chat/route.ts"
      provides: "Updated chat route with doc marker detection"
      contains: "GENERATE_DOC"
    - path: "dashboard/src/lib/planning/system-prompts.ts"
      provides: "Updated system prompts with doc generation instructions"
      contains: "GENERATE_DOC"
  key_links:
    - from: "dashboard/src/app/api/planning/chat/route.ts"
      to: "dashboard/src/lib/planning/doc-generation.ts"
      via: "imports detectDocGenerateMarker, stripDocMarkers"
      pattern: "import.*detectDocGenerateMarker.*doc-generation"
    - from: "dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx"
      to: "/api/planning/documents/generate"
      via: "fetch POST on approve"
      pattern: "fetch.*documents/generate"
---

<objective>
Wire document generation into the chat flow: update system prompts to instruct Claude about doc markers, detect markers in chat route, emit SSE events, and show inline approval cards.

Purpose: This is the core integration — connecting the conversation engine to document generation via the established marker-detection + SSE event pattern.
Output: Updated chat route, system prompts, conversation shell, and new suggestion card component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/07-document-generation/07-RESEARCH.md
@.planning-planning-studio/phases/07-document-generation/07-01-SUMMARY.md
@dashboard/src/app/api/planning/chat/route.ts
@dashboard/src/lib/planning/system-prompts.ts
@dashboard/src/lib/planning/phase-transitions.ts
@dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: System prompts + chat route marker detection</name>
  <files>
    dashboard/src/lib/planning/system-prompts.ts
    dashboard/src/app/api/planning/chat/route.ts
  </files>
  <action>
**system-prompts.ts changes:**
Add document generation instructions to phase-specific prompts. For each phase (DISCOVER, DEFINE, DEVELOP, PACKAGE), add instructions telling Claude:
- Which document types are relevant for this phase (use PHASE_DOC_SUGGESTIONS from doc-templates.ts)
- When Claude determines enough information has been gathered for a document, include the marker `<!--GENERATE_DOC:type-->` in its response
- Example: "When you have gathered enough information about the target customer, include <!--GENERATE_DOC:icp--> in your response to suggest generating the ICP document."
- For PACKAGE phase: instruct Claude to suggest all three GSD documents (project, requirements, roadmap)
- Instruction: "Only suggest one document at a time. Wait for user approval before suggesting the next."

**chat/route.ts changes:**
After the existing marker detection logic (phase transitions, readiness checks), add doc generation marker detection:
1. Import `detectDocGenerateMarker` and `stripDocMarkers` from doc-generation.ts
2. After streaming completes and content is assembled, call `detectDocGenerateMarker(fullContent)`
3. If a doc type is detected, emit SSE event: `data: ${JSON.stringify({ type: 'doc_suggestion', docType: detectedType })}\n\n`
4. Strip doc markers from content before saving to database (same as phase markers): call `stripDocMarkers(content)`
5. Can detect multiple markers in one response — emit a separate SSE event for each

Follow the exact pattern used for `<!--PHASE_COMPLETE-->` detection in the existing chat route.
  </action>
  <verify>
    - `cd dashboard && npx tsc --noEmit` passes
    - System prompts include GENERATE_DOC instructions for each phase
    - Chat route imports and calls doc marker detection functions
  </verify>
  <done>Chat route detects doc generation markers, emits SSE events, strips markers before save. System prompts instruct Claude when to suggest documents.</done>
</task>

<task type="auto">
  <name>Task 2: Doc suggestion card and conversation shell wiring</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/doc-suggestion-card.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  </files>
  <action>
**doc-suggestion-card.tsx:**
Create a client component for inline document generation approval. Props:
- `docType: string` — the document type to generate
- `projectId: string` — current project
- `onGenerated: () => void` — callback after successful generation
- `onDismiss: () => void` — callback to remove the card

UI (use shadcn Card):
- Icon (FileText from lucide-react) + label from DOC_TYPE_LABELS
- Text: "Ready to generate your {label}?"
- Two buttons: "Generate" (primary) and dismiss X button
- Loading state: "Generate" button shows spinner while generating, disabled
- On "Generate" click: POST to `/api/planning/documents/generate` with `{ projectId, docType }`
- On success: call onGenerated(), show brief success state, then auto-dismiss after 2s
- On error: show error text inline, keep card visible

**conversation-shell.tsx changes:**
1. Add state: `docSuggestions: Array<{ docType: string; id: string }>` (id for keying)
2. In the SSE event handler (where phase_complete and readiness_result are handled), add handler for `doc_suggestion` event type:
   - Parse event data, extract docType
   - Add to docSuggestions state array
3. Render DocSuggestionCard components below the message list (or inline after the last message) for each pending suggestion
4. On generate success (onGenerated callback): remove from docSuggestions array, trigger router.refresh() to update DocumentsPanel in sidebar
5. On dismiss: remove from docSuggestions array
  </action>
  <verify>
    - `cd dashboard && npx tsc --noEmit` passes
    - DocSuggestionCard renders with generate/dismiss buttons
    - Conversation shell handles doc_suggestion SSE events
  </verify>
  <done>Users see inline approval cards when Claude suggests documents. Approving generates the document and refreshes the sidebar.</done>
</task>

</tasks>

<verification>
- System prompts contain GENERATE_DOC marker instructions per phase
- Chat route detects markers and emits SSE events
- Suggestion cards appear in conversation when Claude suggests a document
- Clicking Generate calls the API and creates a document
- DocumentsPanel refreshes after generation
</verification>

<success_criteria>
- Full flow works: Claude suggests -> SSE event -> card appears -> user approves -> document generated -> sidebar updates
- Markers are stripped from saved messages (invisible to users)
- Each phase instructs Claude about relevant document types
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/07-document-generation/07-03-SUMMARY.md`
</output>

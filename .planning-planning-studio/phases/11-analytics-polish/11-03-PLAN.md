---
phase: 11-analytics-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/planning/error.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/error.tsx
  - dashboard/src/app/dashboard/planning/queue/error.tsx
  - dashboard/src/app/dashboard/planning/goals/error.tsx
  - dashboard/src/app/dashboard/planning/analytics/error.tsx
autonomous: true

must_haves:
  truths:
    - "All Planning Studio routes have error boundaries"
    - "Error pages show friendly message with retry button"
    - "Errors are logged to console for debugging"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/error.tsx"
      provides: "Error boundary for pipeline page"
      exports: ["default"]
    - path: "dashboard/src/app/dashboard/planning/[projectId]/error.tsx"
      provides: "Error boundary for project detail page"
      exports: ["default"]
    - path: "dashboard/src/app/dashboard/planning/queue/error.tsx"
      provides: "Error boundary for queue page"
      exports: ["default"]
    - path: "dashboard/src/app/dashboard/planning/goals/error.tsx"
      provides: "Error boundary for goals page"
      exports: ["default"]
    - path: "dashboard/src/app/dashboard/planning/analytics/error.tsx"
      provides: "Error boundary for analytics page"
      exports: ["default"]
  key_links:
    - from: "planning/error.tsx"
      to: "@/dashboard-kit/components/ui/button"
      via: "Button import for retry"
      pattern: "import.*Button.*@/dashboard-kit"
---

<objective>
Add error boundaries to all Planning Studio routes for graceful error handling.

Purpose: Ensure users see helpful error messages with retry options instead of broken pages when errors occur.

Output: error.tsx files in all 5 Planning Studio route directories following the established lead-intelligence pattern.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/11-analytics-polish/11-CONTEXT.md

# Error boundary pattern to follow
@dashboard/src/app/dashboard/lead-intelligence/error.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error boundaries for pipeline and project detail</name>
  <files>
    dashboard/src/app/dashboard/planning/error.tsx
    dashboard/src/app/dashboard/planning/[projectId]/error.tsx
  </files>
  <action>
Create error.tsx files following the lead-intelligence/error.tsx pattern exactly.

**planning/error.tsx:**
```typescript
'use client';

import { useEffect } from 'react';
import { Button } from '@/dashboard-kit/components/ui/button';

export default function PlanningError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error('Planning pipeline error:', error);
  }, [error]);

  return (
    <div className="flex flex-col items-center justify-center min-h-[400px] gap-4 p-8">
      <h2 className="text-xl font-semibold">Something went wrong</h2>
      <p className="text-muted-foreground text-sm max-w-md text-center">
        The planning pipeline encountered an error. This may be a temporary issue.
      </p>
      <Button onClick={reset} variant="outline">
        Try again
      </Button>
    </div>
  );
}
```

**[projectId]/error.tsx:**
Same pattern with message: "The project detail page encountered an error. This may be a temporary issue."
  </action>
  <verify>Files exist and are valid TypeScript client components</verify>
  <done>Error boundaries created for pipeline and project detail routes</done>
</task>

<task type="auto">
  <name>Task 2: Create error boundaries for queue, goals, and analytics</name>
  <files>
    dashboard/src/app/dashboard/planning/queue/error.tsx
    dashboard/src/app/dashboard/planning/goals/error.tsx
    dashboard/src/app/dashboard/planning/analytics/error.tsx
  </files>
  <action>
Create error.tsx files following the same pattern:

**queue/error.tsx:**
Message: "The build queue page encountered an error. This may be a temporary issue."
Console log: "Build queue error:"

**goals/error.tsx:**
Message: "The goals page encountered an error. This may be a temporary issue."
Console log: "Goals page error:"

**analytics/error.tsx:**
Message: "The analytics page encountered an error. This may be a temporary issue."
Console log: "Analytics page error:"

All files follow identical structure:
1. 'use client' directive
2. useEffect to log error
3. Centered flex container with min-h-[400px]
4. h2 heading "Something went wrong"
5. p with muted text and context-specific message
6. Button with variant="outline" calling reset()
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes with all error files</verify>
  <done>All 5 Planning Studio routes have error boundaries</done>
</task>

</tasks>

<verification>
1. All 5 error.tsx files exist in correct directories
2. TypeScript compiles without errors
3. Each file follows 'use client' directive requirement
4. Console.error logs error with route-specific prefix
5. Retry button calls reset() function
</verification>

<success_criteria>
- /dashboard/planning has error.tsx
- /dashboard/planning/[projectId] has error.tsx
- /dashboard/planning/queue has error.tsx
- /dashboard/planning/goals has error.tsx
- /dashboard/planning/analytics has error.tsx
- All error boundaries show friendly message with retry option
- Errors are logged to console for debugging
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/11-analytics-polish/11-03-SUMMARY.md`
</output>

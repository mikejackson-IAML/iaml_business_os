---
phase: 11-analytics-polish
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/analytics/analytics-content.tsx
  - dashboard/src/app/dashboard/planning/analytics/analytics-skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Analytics page shows Ideas Shipped count with sparkline trend"
    - "Analytics page shows Velocity (average days) with sparkline trend"
    - "Period dropdown changes the displayed metrics"
    - "Funnel shows project distribution across statuses"
    - "Empty state displays when no shipped projects exist"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/analytics/analytics-content.tsx"
      provides: "Complete analytics dashboard implementation"
      min_lines: 80
    - path: "dashboard/src/app/dashboard/planning/analytics/analytics-skeleton.tsx"
      provides: "Loading skeleton matching real layout"
      min_lines: 30
  key_links:
    - from: "analytics-content.tsx"
      to: "planning-queries.ts"
      via: "getAnalyticsMetrics, getFunnelData imports"
      pattern: "import.*getAnalyticsMetrics.*planning-queries"
    - from: "analytics-content.tsx"
      to: "metric-card.tsx"
      via: "MetricCard import"
      pattern: "import.*MetricCard.*components/metric-card"
---

<objective>
Wire up the analytics dashboard page with real data and period selection.

Purpose: Deliver a functional analytics dashboard showing key pipeline metrics with time-based filtering.

Output: Complete analytics-content.tsx with metrics cards, funnel visualization, and period selection. Updated skeleton to match layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/11-analytics-polish/11-CONTEXT.md
@.planning-planning-studio/phases/11-analytics-polish/11-01-SUMMARY.md

# Page structure reference
@dashboard/src/app/dashboard/planning/analytics/page.tsx
@dashboard/src/app/dashboard/planning/queue/queue-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement analytics-content.tsx with server data fetch</name>
  <files>dashboard/src/app/dashboard/planning/analytics/analytics-content.tsx</files>
  <action>
Rewrite analytics-content.tsx as a server component that fetches data and renders a client wrapper:

**Server Component (analytics-content.tsx):**
```typescript
import { getAnalyticsMetrics, getFunnelData } from '@/lib/api/planning-queries';
import { AnalyticsContentClient } from './analytics-content-client';

export async function AnalyticsContent() {
  const [metrics, funnel] = await Promise.all([
    getAnalyticsMetrics('month'), // Default period
    getFunnelData(),
  ]);

  return <AnalyticsContentClient initialMetrics={metrics} funnelData={funnel} />;
}
```

**Client Component (analytics-content-client.tsx):**
Create new file with:
- useState for period selection (default: 'month')
- useTransition for loading state during period changes
- Refetch metrics when period changes via server action or client fetch

Layout (spacious per CONTEXT.md):
1. Header section with back link, badge, title, description
2. Period selector aligned right below header
3. Metric cards grid: 2 columns on mobile, 4 on desktop (grid-cols-2 lg:grid-cols-4)
   - Card 1: "Ideas Shipped" - shippedCount with trend sparkline
   - Card 2: "Avg Velocity" - avgVelocityDays with "days" subtitle, trend sparkline
   - Card 3: "Ideas Captured" - capturedCount with trend sparkline
   - Card 4: "Conversion Rate" - (shipped/captured * 100)% calculated
4. Funnel visualization below cards (full width)

Gap spacing: gap-6 between sections, gap-4 between cards
Padding: p-6 lg:p-8 (matches existing pages)
  </action>
  <verify>Page loads with real data from database</verify>
  <done>Analytics page displays metrics cards with sparklines and funnel visualization</done>
</task>

<task type="auto">
  <name>Task 2: Add period change handler and empty state</name>
  <files>
    dashboard/src/app/dashboard/planning/analytics/analytics-content-client.tsx
    dashboard/src/app/dashboard/planning/analytics/components/empty-analytics.tsx
  </files>
  <action>
**Period Change Handler:**
In analytics-content-client.tsx, add:
1. Client-side fetch function that calls a new API route or uses server action
2. Loading indicator when period changes (subtle opacity reduction or spinner)
3. Replace metrics state with new data on period change

Create `/api/planning/analytics/route.ts` if needed for client fetch:
```typescript
// GET /api/planning/analytics?period=month
export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const period = searchParams.get('period') || 'month';
  const metrics = await getAnalyticsMetrics(period as AnalyticsPeriod);
  return Response.json(metrics);
}
```

**Empty State:**
Create empty-analytics.tsx following empty-queue.tsx pattern:
- BarChart3 icon (lucide-react)
- "No analytics data yet" heading
- "Ship some projects to see your pipeline metrics here." description
- Link to Pipeline page

Show empty state when:
- shippedCount === 0 AND capturedCount === 0 (no activity at all)

If there are captured projects but none shipped, still show the funnel and capture metrics.
  </action>
  <verify>Period dropdown updates metrics without full page reload</verify>
  <done>Period selection works, empty state shows when no data</done>
</task>

<task type="auto">
  <name>Task 3: Update skeleton to match real layout</name>
  <files>dashboard/src/app/dashboard/planning/analytics/analytics-skeleton.tsx</files>
  <action>
Update analytics-skeleton.tsx to match the actual layout:

1. Header skeleton (back link, badge, title, description) - keep existing
2. Period selector skeleton: Skeleton h-10 w-32 aligned right
3. Metric cards grid: 4 cards in grid-cols-2 lg:grid-cols-4
   - Each card: p-6, title skeleton (h-4 w-24), value skeleton (h-9 w-16), sparkline skeleton (h-10 w-28)
4. Funnel card: CardHeader with title skeleton, CardContent with 5 horizontal bar skeletons of decreasing width

Remove the two-column chart layout from existing skeleton (not in final design).
  </action>
  <verify>Skeleton visually matches the shape of the real analytics page</verify>
  <done>Loading skeleton accurately reflects the final layout</done>
</task>

</tasks>

<verification>
1. Navigate to /dashboard/planning/analytics - page loads with real metrics
2. Change period dropdown - metrics update without full page reload
3. Funnel shows accurate counts matching pipeline page
4. With no projects: empty state displays
5. Skeleton layout matches real content structure
</verification>

<success_criteria>
- Analytics dashboard displays shipped count, velocity, captured count, conversion rate
- Sparklines show trends within selected period
- Period dropdown switches between week/month/quarter/all time
- Funnel visualization shows accurate status distribution
- Empty state displays when no analytics data exists
- Loading skeleton matches final layout shape
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/11-analytics-polish/11-02-SUMMARY.md`
</output>

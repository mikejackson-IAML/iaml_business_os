---
phase: 08-deep-research-integration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dashboard/src/app/api/planning/chat/route.ts
  - dashboard/src/app/dashboard/planning/[projectId]/components/research-suggestion-card.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
autonomous: true

must_haves:
  truths:
    - "Claude's research suggestions appear as approval cards in the chat UI"
    - "User can edit the research query before approving"
    - "Approved research triggers API call and shows loading state"
    - "Toast notification appears when research completes"
    - "Conversation remains usable while research runs"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/research-suggestion-card.tsx"
      provides: "Research approval card with editable query"
      min_lines: 40
    - path: "dashboard/src/app/api/planning/chat/route.ts"
      provides: "SSE research_suggestion events"
  key_links:
    - from: "conversation-shell.tsx"
      to: "research-suggestion-card.tsx"
      via: "SSE event handler renders card"
      pattern: "research_suggestion"
    - from: "research-suggestion-card.tsx"
      to: "/api/planning/research"
      via: "fetch POST on approve"
      pattern: "fetch.*api/planning/research"
---

<objective>
Wire research suggestions into the chat flow: SSE marker detection, suggestion cards, and research triggering from conversation.

Purpose: This is the core user-facing integration — Claude suggests research, user sees approval card, edits query, approves, research runs.
Output: ResearchSuggestionCard component, chat route emitting research_suggestion SSE events, conversation shell handling them.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/phases/08-deep-research-integration/08-01-SUMMARY.md
@.planning-planning-studio/phases/08-deep-research-integration/08-RESEARCH.md

@dashboard/src/app/api/planning/chat/route.ts
@dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
@dashboard/src/app/dashboard/planning/[projectId]/components/doc-suggestion-card.tsx
@dashboard/src/lib/planning/research-markers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat route SSE integration for research markers</name>
  <files>
    dashboard/src/app/api/planning/chat/route.ts
  </files>
  <action>
    Follow the exact pattern used for doc generation markers in the chat route:

    1. Import `detectResearchMarkers` and `stripResearchMarkers` from research-markers.ts
    2. After the streaming completes and full content is assembled, call `detectResearchMarkers(fullContent)`
    3. For each detected query, emit an SSE event:
       ```
       { type: 'research_suggestion', query: queryString }
       ```
    4. Strip research markers from the content before saving to database (same as doc markers are stripped)
    5. Place this logic near the existing doc marker detection for consistency

    Important: Research markers should be detected AFTER doc markers. Both can coexist in the same response.
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Chat route detects research markers, emits SSE events, strips markers before DB save.</done>
</task>

<task type="auto">
  <name>Task 2: ResearchSuggestionCard and conversation shell wiring</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/research-suggestion-card.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  </files>
  <action>
    1. Create ResearchSuggestionCard following DocSuggestionCard pattern exactly:
       - Props: `{ query: string, projectId: string, conversationId: string, phaseId: string, onCompleted: (researchId: string) => void, onDismiss: () => void }`
       - State: `editableQuery` (textarea, initialized from props.query), `isRunning` (loading state)
       - UI layout:
         - Icon + "Research Suggestion" header
         - Textarea with the query (editable per CONTEXT.md requirement)
         - "Research" button (primary) and "Dismiss" button
         - While running: spinner + "Researching..." text, buttons disabled
       - On approve: POST to `/api/planning/research` with { projectId, conversationId, phaseId, query: editableQuery }
       - On success: call onCompleted with research id, show toast("Research complete") via sonner
       - On failure: show toast.error("Research failed"), re-enable buttons
       - Style: Match DocSuggestionCard styling (border, padding, colors)

    2. Update conversation-shell.tsx:
       - Add state for research suggestions: `researchSuggestions: string[]`
       - Handle `research_suggestion` SSE event type: push query to researchSuggestions array
       - Render ResearchSuggestionCard components (similar to how DocSuggestionCards are rendered)
       - Place between messages and input, same position as doc suggestion cards
       - On completed/dismiss: remove from researchSuggestions array
       - Import toast from sonner if not already imported
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Research suggestion cards appear in chat when Claude suggests research. User can edit query and approve. Toast shows on completion. Cards dismissed after use.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- ResearchSuggestionCard renders with editable textarea
- SSE event type 'research_suggestion' handled in conversation shell
- Cards appear between messages and input
</verification>

<success_criteria>
- Claude's research markers trigger suggestion cards in chat UI
- User can edit query text before approving
- Approve button triggers research API call with loading state
- Toast notification on completion/failure
- Chat remains fully functional during research
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/08-deep-research-integration/08-02-SUMMARY.md`
</output>

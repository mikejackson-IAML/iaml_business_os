---
phase: 08-deep-research-integration
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/research-results-modal.tsx
  - dashboard/src/lib/api/planning-queries.ts
autonomous: true

must_haves:
  truths:
    - "Research panel shows all research runs for a project"
    - "Can click to view full research results"
    - "Research status (pending/running/complete/failed) is visible"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx"
      provides: "Enhanced research panel with real data"
      min_lines: 50
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/research-results-modal.tsx"
      provides: "Modal for viewing full research results"
      min_lines: 40
  key_links:
    - from: "research-panel.tsx"
      to: "/api/planning/research"
      via: "fetch GET with projectId"
      pattern: "api/planning/research"
    - from: "research-panel.tsx"
      to: "research-results-modal.tsx"
      via: "click handler opens modal"
      pattern: "setSelectedResearch"
---

<objective>
Build the research panel UI and results modal for viewing research history and findings.

Purpose: Users need to browse all research runs for a project and view full results with citations.
Output: Enhanced ResearchPanel with real data, ResearchResultsModal for full result viewing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/phases/08-deep-research-integration/08-01-SUMMARY.md
@.planning-planning-studio/phases/08-deep-research-integration/08-CONTEXT.md

@dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx
@dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
@dashboard/src/lib/api/planning-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Planning queries and research results modal</name>
  <files>
    dashboard/src/lib/api/planning-queries.ts
    dashboard/src/app/dashboard/planning/[projectId]/components/research-results-modal.tsx
  </files>
  <action>
    1. Add to planning-queries.ts:
       - `fetchProjectResearch(projectId: string)` — fetches all research for a project from `/api/planning/research?projectId=xxx`, returns array sorted by created_at desc
       - Ensure the return type matches the Research interface from planning types

    2. Create research-results-modal.tsx:
       - Props: `{ research: Research | null, open: boolean, onClose: () => void }`
       - Uses shadcn Dialog (same pattern as document preview modal from Phase 7)
       - Header: query text as title, status badge, created_at date
       - Body: markdown-rendered summary (use react-markdown, already in project)
       - Citations section: if key_findings.citations exists, render as numbered list of clickable links
       - Footer: Close button
       - Handle null research gracefully (don't render if null)
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Query function fetches research. Modal displays full results with markdown and citations.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ResearchPanel with real data</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx
  </files>
  <action>
    Replace the existing shell ResearchPanel with a fully functional panel:

    1. Fetch research data using fetchProjectResearch on mount (useEffect + state, or pass as prop — match the pattern used by DocumentsPanel and SessionsPanel)
    2. List items showing:
       - Query text (truncated to ~80 chars)
       - Status badge: pending (yellow pulse), running (blue pulse), complete (green), failed (red)
       - Relative time (e.g., "2 min ago") using the inline formatRelativeTime pattern from existing panels
    3. Click item opens ResearchResultsModal with selected research
    4. Empty state: "No research yet. Claude will suggest research during conversations." (match empty state pattern from other panels)
    5. Auto-refresh: poll every 5 seconds if any research has status 'pending' or 'running' (check if any items have those statuses, if so setInterval, clear on unmount or when all complete)
    6. Add a manual "New Research" button at top that opens a small inline form (textarea + submit) for user-initiated research. On submit, POST to /api/planning/research. This supports the CONTEXT.md requirement for user-initiated research (not just Claude-suggested).
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>ResearchPanel shows real research data with status badges, click-to-view, auto-refresh for pending items, and manual research trigger.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- ResearchPanel renders list of research items
- Clicking item opens modal with full results
- Status badges show correct colors
- Polling refreshes pending/running items
</verification>

<success_criteria>
- Research panel displays all research for a project
- Status is clearly visible (pending/running/complete/failed)
- Full results viewable in modal with markdown and citations
- User can initiate research manually from panel
- Auto-refresh while research is in progress
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/08-deep-research-integration/08-03-SUMMARY.md`
</output>

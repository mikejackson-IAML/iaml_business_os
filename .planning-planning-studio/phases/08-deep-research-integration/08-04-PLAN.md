---
phase: 08-deep-research-integration
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - dashboard/src/app/api/planning/chat/route.ts
  - dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  - dashboard/src/lib/planning/chat-helpers.ts
autonomous: true

must_haves:
  truths:
    - "Completed research findings are injected into Claude's next message context"
    - "Research results integrate seamlessly into ongoing conversation"
    - "Key findings can be extracted as memories"
  artifacts:
    - path: "dashboard/src/app/api/planning/chat/route.ts"
      provides: "Research context injection"
    - path: "dashboard/src/lib/planning/chat-helpers.ts"
      provides: "Research context loading helper"
  key_links:
    - from: "dashboard/src/lib/planning/chat-helpers.ts"
      to: "planning_studio.research"
      via: "query for completed research"
      pattern: "status.*complete"
    - from: "dashboard/src/app/api/planning/chat/route.ts"
      to: "chat-helpers.ts"
      via: "context block includes research findings"
      pattern: "Research Findings"
---

<objective>
Integrate completed research results into the conversation context so Claude can reference findings in subsequent messages.

Purpose: Research is only useful if Claude knows about the results. This plan closes the loop: research completes -> findings injected into next chat context -> Claude references them naturally.
Output: Research context injection in chat route, sidebar refresh after research completes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/phases/08-deep-research-integration/08-01-SUMMARY.md
@.planning-planning-studio/phases/08-deep-research-integration/08-02-SUMMARY.md
@.planning-planning-studio/phases/08-deep-research-integration/08-03-SUMMARY.md

@dashboard/src/app/api/planning/chat/route.ts
@dashboard/src/lib/planning/chat-helpers.ts
@dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research context injection into chat</name>
  <files>
    dashboard/src/lib/planning/chat-helpers.ts
    dashboard/src/app/api/planning/chat/route.ts
  </files>
  <action>
    1. In chat-helpers.ts, add a function `getCompletedResearchContext(conversationId: string): Promise<string>`:
       - Query planning_studio.research where conversation_id matches AND status = 'complete'
       - Order by completed_at desc
       - For each result, format as:
         ```
         ### Research: {query}
         {summary}
         Sources: {citations as comma-separated URLs if available}
         ```
       - Return empty string if no completed research
       - Wrap all results under `## Recent Research Findings\n\n`

    2. In chat/route.ts, call getCompletedResearchContext(conversationId) when building the context block for Claude:
       - Append the research context to the existing context block (after documents/memories, before sending to Claude)
       - Only include if non-empty
       - Place this near where other context (memories, documents) is assembled

    3. Also in chat-helpers.ts, add `getCompletedResearchForProject(projectId: string)` for project-wide research (used by research panel, already may exist via API route — but having a direct helper is useful for context building).

    4. Optional memory extraction: After research completes (in the research API route from Plan 01), fire-and-forget extract key findings as memories using the existing memory extraction pattern from Phase 6. Extract the summary as a memory with type 'research_finding'. This follows CONTEXT.md's "Claude's Discretion" on memory extraction from research. If the memory extraction imports are complex, skip this and note it — it can be added later.
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Completed research findings appear in Claude's context. Claude can reference research results in subsequent messages.</done>
</task>

<task type="auto">
  <name>Task 2: Conversation shell research completion handling</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  </files>
  <action>
    1. When a ResearchSuggestionCard completes (onCompleted callback):
       - Trigger a refresh of the research panel sidebar (use router.refresh() pattern from doc generation, or a callback prop)
       - Optionally inject a system-style message into the chat: "Research complete: {query}" as a visual indicator that results are now available to Claude

    2. Ensure that when user sends the next message after research completes, the chat route will naturally pick up the research context (this happens via Task 1's context injection — no extra client work needed, but verify the flow is clean).

    3. If conversation-shell already has a mechanism to refresh sidebar panels (from doc generation), reuse it. The key behavior: after research completes, the research panel in the sidebar should show the new completed item without a full page reload.
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Research completion refreshes sidebar panel. Next message to Claude includes research findings in context.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- Research context appears in Claude's system/context messages after research completes
- Sidebar research panel updates after research completion
- Full flow works: suggest -> approve -> complete -> context injected -> Claude references findings
</verification>

<success_criteria>
- Completed research summary injected into next Claude message context
- Research panel refreshes when research completes
- Claude naturally references research findings in subsequent responses
- End-to-end flow: marker -> card -> approve -> Perplexity -> results stored -> context injected
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/08-deep-research-integration/08-04-SUMMARY.md`
</output>

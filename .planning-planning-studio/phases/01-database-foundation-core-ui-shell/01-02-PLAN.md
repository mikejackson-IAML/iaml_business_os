---
phase: 01-database-foundation-core-ui-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/page.tsx
  - dashboard/src/app/dashboard/planning/planning-skeleton.tsx
  - dashboard/src/app/dashboard/planning/planning-content.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/page.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/project-skeleton.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  - dashboard/src/app/dashboard/planning/goals/page.tsx
  - dashboard/src/app/dashboard/planning/goals/goals-skeleton.tsx
  - dashboard/src/app/dashboard/planning/goals/goals-content.tsx
  - dashboard/src/app/dashboard/planning/analytics/page.tsx
  - dashboard/src/app/dashboard/planning/analytics/analytics-skeleton.tsx
  - dashboard/src/app/dashboard/planning/analytics/analytics-content.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to /dashboard/planning and see a loading skeleton then content"
    - "User can navigate to /dashboard/planning/[projectId] with any UUID"
    - "User can navigate to /dashboard/planning/goals"
    - "User can navigate to /dashboard/planning/analytics"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/page.tsx"
      provides: "Main planning page with Suspense wrapper"
      contains: "Suspense"
      min_lines: 10
    - path: "dashboard/src/app/dashboard/planning/planning-skeleton.tsx"
      provides: "Loading skeleton for planning page"
      contains: "Skeleton"
    - path: "dashboard/src/app/dashboard/planning/planning-content.tsx"
      provides: "Client component for planning UI"
      contains: "use client"
  key_links:
    - from: "dashboard/src/app/dashboard/planning/page.tsx"
      to: "planning-skeleton.tsx"
      via: "Suspense fallback"
      pattern: "Suspense.*fallback.*PlanningSkeleton"
    - from: "dashboard/src/app/dashboard/planning/page.tsx"
      to: "planning-content.tsx"
      via: "content component"
      pattern: "PlanningContent"
---

<objective>
Create shell pages for Planning Studio with proper loading skeletons.

Purpose: Establish the routing and page structure for all Planning Studio views - pipeline, project detail, goals, and analytics. Pages will have skeletons for loading states and shell content components ready for future implementation.

Output: Four navigable routes with loading states and placeholder content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/REQUIREMENTS.md
@.planning-planning-studio/phases/01-database-foundation-core-ui-shell/01-RESEARCH.md

Reference existing page patterns:
@dashboard/src/app/dashboard/development/page.tsx
@dashboard/src/app/dashboard/development/development-skeleton.tsx
@dashboard/src/app/dashboard/development/development-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main planning page with skeleton</name>
  <files>
    dashboard/src/app/dashboard/planning/page.tsx
    dashboard/src/app/dashboard/planning/planning-skeleton.tsx
    dashboard/src/app/dashboard/planning/planning-content.tsx
  </files>
  <action>
Create the main planning page following the exact pattern from development/page.tsx.

**page.tsx** (server component):
```typescript
import { Suspense } from 'react';
import { PlanningSkeleton } from './planning-skeleton';
import { PlanningContent } from './planning-content';

export const dynamic = 'force-dynamic';

export default function PlanningPage() {
  return (
    <Suspense fallback={<PlanningSkeleton />}>
      <PlanningContent />
    </Suspense>
  );
}
```

**planning-skeleton.tsx** (client component with 'use client'):
Create a skeleton representing a Kanban-style pipeline view:
- Header skeleton (title + description)
- Stats row (4 metric cards)
- 5 column headers (Incubating, Active, Ready to Build, Building, Shipped)
- 2-3 card skeletons per column
Use the same imports as development-skeleton.tsx: Card, CardContent, CardHeader, Skeleton from @/dashboard-kit/components/ui/

**planning-content.tsx** (client component with 'use client'):
Create a placeholder shell showing:
- Page title: "Planning Studio"
- Subtitle: "AI-guided idea-to-production pipeline"
- Empty state message: "Planning Studio coming soon. Connect the dots from idea to shipped product."
- Use Card component for the empty state
Style with Tailwind classes matching existing dashboard patterns.
  </action>
  <verify>
1. `ls dashboard/src/app/dashboard/planning/` shows page.tsx, planning-skeleton.tsx, planning-content.tsx
2. `grep "Suspense" dashboard/src/app/dashboard/planning/page.tsx` returns match
3. `grep "'use client'" dashboard/src/app/dashboard/planning/planning-skeleton.tsx` returns match
4. `grep "Planning Studio" dashboard/src/app/dashboard/planning/planning-content.tsx` returns match
  </verify>
  <done>
Main planning route (/dashboard/planning) works with:
- Server component page.tsx using Suspense pattern
- Kanban-style loading skeleton
- Shell content with placeholder message
  </done>
</task>

<task type="auto">
  <name>Task 2: Create project detail page with skeleton</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/page.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-skeleton.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  </files>
  <action>
Create the project detail page for /dashboard/planning/[projectId].

**page.tsx** (server component):
- Follow same Suspense pattern
- Accept params with projectId from URL
- Pass projectId to content component

```typescript
import { Suspense } from 'react';
import { ProjectSkeleton } from './project-skeleton';
import { ProjectContent } from './project-content';

export const dynamic = 'force-dynamic';

interface ProjectPageProps {
  params: Promise<{ projectId: string }>;
}

export default async function ProjectPage({ params }: ProjectPageProps) {
  const { projectId } = await params;
  return (
    <Suspense fallback={<ProjectSkeleton />}>
      <ProjectContent projectId={projectId} />
    </Suspense>
  );
}
```

**project-skeleton.tsx** (client component):
Create a skeleton for the project detail view:
- Phase progress bar (6 circles for phases)
- Left sidebar area (session list, documents, research)
- Main conversation area (chat-like skeleton)
Use Skeleton components matching existing patterns.

**project-content.tsx** (client component):
Accept projectId prop. Display:
- Placeholder title: "Project Detail"
- Show the projectId in a subtle way
- Empty state: "Project view coming soon. Manage conversations, documents, and research for your idea."
  </action>
  <verify>
1. `ls dashboard/src/app/dashboard/planning/\[projectId\]/` shows page.tsx, project-skeleton.tsx, project-content.tsx
2. `grep "projectId" dashboard/src/app/dashboard/planning/\[projectId\]/page.tsx` returns match
3. `grep "ProjectContent" dashboard/src/app/dashboard/planning/\[projectId\]/page.tsx` returns match
  </verify>
  <done>
Project detail route (/dashboard/planning/[projectId]) works with:
- Dynamic route accepting UUID projectId
- Project detail loading skeleton
- Shell content showing projectId
  </done>
</task>

<task type="auto">
  <name>Task 3: Create goals and analytics pages</name>
  <files>
    dashboard/src/app/dashboard/planning/goals/page.tsx
    dashboard/src/app/dashboard/planning/goals/goals-skeleton.tsx
    dashboard/src/app/dashboard/planning/goals/goals-content.tsx
    dashboard/src/app/dashboard/planning/analytics/page.tsx
    dashboard/src/app/dashboard/planning/analytics/analytics-skeleton.tsx
    dashboard/src/app/dashboard/planning/analytics/analytics-content.tsx
  </files>
  <action>
Create goals and analytics pages following the same pattern.

**Goals page** (/dashboard/planning/goals):
- page.tsx: Suspense wrapper
- goals-skeleton.tsx: Grid of goal card skeletons (4-6 cards), each with title, slider, description
- goals-content.tsx: Shell with "Goals Management" title, empty state: "Set your priorities to help AI rank your projects. Coming soon."

**Analytics page** (/dashboard/planning/analytics):
- page.tsx: Suspense wrapper
- analytics-skeleton.tsx: Stats row (4 cards) + 2 chart skeletons + funnel skeleton
- analytics-content.tsx: Shell with "Analytics" title, empty state: "Track your idea-to-shipped pipeline. Coming soon."

All follow the exact same pattern:
1. page.tsx imports Suspense, skeleton, content
2. Uses `export const dynamic = 'force-dynamic';`
3. Returns Suspense with fallback
4. Skeleton is 'use client' with Skeleton components
5. Content is 'use client' with placeholder Card
  </action>
  <verify>
1. `ls dashboard/src/app/dashboard/planning/goals/` shows page.tsx, goals-skeleton.tsx, goals-content.tsx
2. `ls dashboard/src/app/dashboard/planning/analytics/` shows page.tsx, analytics-skeleton.tsx, analytics-content.tsx
3. All 4 page.tsx files contain Suspense
  </verify>
  <done>
Goals route (/dashboard/planning/goals) and Analytics route (/dashboard/planning/analytics) work with:
- Suspense wrappers
- Appropriate loading skeletons
- Shell content with placeholder messages
  </done>
</task>

</tasks>

<verification>
All pages created when:
1. All 12 files exist in the planning directory structure
2. Each page.tsx uses Suspense pattern
3. Each skeleton uses 'use client' and Skeleton components
4. Each content component uses 'use client' and shows placeholder
5. Routes are navigable (verify with `npm run dev` if available, otherwise file existence)
</verification>

<success_criteria>
- 4 routes created: /dashboard/planning, /dashboard/planning/[projectId], /dashboard/planning/goals, /dashboard/planning/analytics
- Each route has page.tsx (server), *-skeleton.tsx (client), *-content.tsx (client)
- All skeletons show meaningful loading state matching the final UI intent
- All content components show placeholder "coming soon" messages
- Follows exact patterns from development/ pages
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/01-database-foundation-core-ui-shell/01-02-SUMMARY.md`
</output>

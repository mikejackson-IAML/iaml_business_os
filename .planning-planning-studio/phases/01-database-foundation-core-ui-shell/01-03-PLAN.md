---
phase: 01-database-foundation-core-ui-shell
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - dashboard/src/app/dashboard/dashboard-content.tsx
autonomous: true

must_haves:
  truths:
    - "User can see Planning Studio link in dashboard header"
    - "Clicking the link navigates to /dashboard/planning"
  artifacts:
    - path: "dashboard/src/app/dashboard/dashboard-content.tsx"
      provides: "Dashboard header with Planning Studio link"
      contains: "Planning"
  key_links:
    - from: "dashboard/src/app/dashboard/dashboard-content.tsx"
      to: "/dashboard/planning"
      via: "Link component"
      pattern: 'href="/dashboard/planning"'
---

<objective>
Add Planning Studio navigation link to dashboard header.

Purpose: Make Planning Studio discoverable from the main CEO Dashboard - users should see it alongside other department links like Development, Digital, Marketing.

Output: New link in the dashboard header quick links section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/01-database-foundation-core-ui-shell/01-RESEARCH.md

Reference existing navigation pattern:
@dashboard/src/app/dashboard/dashboard-content.tsx (lines 137-187 for link structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Planning Studio link to dashboard header</name>
  <files>dashboard/src/app/dashboard/dashboard-content.tsx</files>
  <action>
Edit dashboard-content.tsx to add a Planning Studio link in the Quick Links section (the `<div className="flex flex-wrap gap-3 mt-4">` area around lines 137-187).

1. First, add the Lightbulb icon to the imports at the top:
   - Find the lucide-react import line
   - Add `Lightbulb` to the icon imports

2. Add a new Link component after the existing links (after Web Intel link, before the closing `</div>`):

```tsx
<Link
  href="/dashboard/planning"
  className="flex items-center gap-2 px-4 py-2 rounded-lg bg-amber-500/10 hover:bg-amber-500/20 text-amber-600 dark:text-amber-400 transition-colors"
>
  <Lightbulb className="h-4 w-4" />
  <span className="text-sm font-medium">Planning</span>
  <ArrowRight className="h-3 w-3" />
</Link>
```

Use amber color scheme (amber-500/10 hover, amber-600 text) to differentiate from other links:
- Action Center: orange
- Digital: blue
- Marketing: pink
- Programs: emerald
- Development: purple
- Web Intel: cyan
- Planning: amber (new)

The Lightbulb icon represents ideas/planning well.
  </action>
  <verify>
1. `grep "Lightbulb" dashboard/src/app/dashboard/dashboard-content.tsx` returns matches (import + usage)
2. `grep "/dashboard/planning" dashboard/src/app/dashboard/dashboard-content.tsx` returns match
3. `grep "amber-500" dashboard/src/app/dashboard/dashboard-content.tsx` returns match
  </verify>
  <done>
Planning Studio link appears in dashboard header with:
- Lightbulb icon
- "Planning" label
- Amber color scheme
- Navigates to /dashboard/planning on click
  </done>
</task>

</tasks>

<verification>
Navigation link added when:
1. Lightbulb icon imported from lucide-react
2. New Link component with href="/dashboard/planning"
3. Uses amber color scheme
4. Positioned after existing department links
</verification>

<success_criteria>
- Planning Studio link visible in dashboard header
- Uses Lightbulb icon from lucide-react
- Amber color scheme (amber-500/10 background, amber-600 text)
- Clicking navigates to /dashboard/planning
- Consistent styling with other department links
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/01-database-foundation-core-ui-shell/01-03-SUMMARY.md`
</output>

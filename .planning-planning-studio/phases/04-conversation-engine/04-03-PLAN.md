---
phase: 04-conversation-engine
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx
  - dashboard/src/app/api/planning/conversations/route.ts
  - dashboard/src/app/api/planning/conversations/[conversationId]/messages/route.ts
autonomous: true

must_haves:
  truths:
    - "User can create a new session from the sidebar"
    - "User can click a previous session to load its messages"
    - "User can switch between sessions without page reload"
    - "Active session is visually highlighted in the sidebar"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx"
      provides: "Interactive sessions sidebar with new/switch"
    - path: "dashboard/src/app/api/planning/conversations/[conversationId]/messages/route.ts"
      provides: "GET endpoint to fetch messages for a conversation"
    - path: "dashboard/src/app/api/planning/conversations/route.ts"
      provides: "GET conversations for project, POST to create new"
  key_links:
    - from: "sessions-panel.tsx"
      to: "conversation-shell.tsx"
      via: "callback props onSelectSession, onNewSession"
      pattern: "onSelectSession|onNewSession"
    - from: "sessions-panel.tsx"
      to: "/api/planning/conversations"
      via: "fetch for session operations"
      pattern: "fetch.*api/planning/conversations"
---

<objective>
Add session management: create new sessions, switch between existing sessions, load previous messages.

Purpose: Users need to have multiple conversations per phase and navigate between them.
Output: Interactive sessions sidebar + API routes for conversation/message fetching.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/04-conversation-engine/04-01-SUMMARY.md

@dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx
@dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
@dashboard/src/lib/api/planning-queries.ts
@dashboard/src/lib/api/planning-chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API routes for conversations and messages</name>
  <files>
    dashboard/src/app/api/planning/conversations/route.ts
    dashboard/src/app/api/planning/conversations/[conversationId]/messages/route.ts
  </files>
  <action>
    1. Create `dashboard/src/app/api/planning/conversations/route.ts`:
       - GET: accepts `?projectId=...&phaseId=...` query params. Returns conversations from planning_studio.conversations filtered by projectId, optionally by phaseId. Order by started_at desc.
       - POST: accepts `{ projectId, phaseId, title? }`. Creates a new conversation via the createConversation helper from planning-chat.ts. Returns the new conversation object with 201 status.

    2. Create `dashboard/src/app/api/planning/conversations/[conversationId]/messages/route.ts`:
       - GET: fetches all messages for the given conversationId from planning_studio.messages, ordered by created_at asc. Returns the array.
       - Use Promise<params> pattern for Next.js 15 dynamic route params: `{ params }: { params: Promise<{ conversationId: string }> }` then `const { conversationId } = await params;`.
  </action>
  <verify>
    `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit`
  </verify>
  <done>GET /api/planning/conversations returns conversations. GET /api/planning/conversations/[id]/messages returns messages. POST creates new conversation.</done>
</task>

<task type="auto">
  <name>Task 2: Interactive sessions panel and conversation shell integration</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  </files>
  <action>
    1. Update `sessions-panel.tsx` to be interactive:
       - Props: `{ conversations: PlanningConversation[]; activeConversationId: string | null; onSelectSession: (conversationId: string) => void; onNewSession: () => void }`
       - "New Session" button is now enabled and calls onNewSession.
       - Clicking a conversation item calls onSelectSession(conversation.id).
       - Active conversation has a highlighted background (bg-muted border-l-2 border-primary).
       - Keep existing display logic (title, message count, relative time, summary).

    2. Update `conversation-shell.tsx` to support session switching:
       - Add state: `conversations` (initialized from initialConversations prop), `activeConversationId`.
       - Add `handleSelectSession(conversationId: string)`:
         a. Set activeConversationId.
         b. Fetch messages from `/api/planning/conversations/${conversationId}/messages`.
         c. Set messages state from response.
         d. Set conversationId state.
       - Add `handleNewSession()`:
         a. Clear messages, set conversationId to null, set activeConversationId to null.
         b. User's next message will trigger conversation creation via the chat API.
       - When a conversation_created event comes from SSE, add the new conversation to the conversations list and set it as active.
       - Pass sessions panel props: conversations, activeConversationId, onSelectSession, onNewSession.

    3. Update `project-content.tsx` layout:
       - Remove the separate `<SessionsPanel>` from the left sidebar since it now lives inside ConversationShell (the shell needs to control it).
       - Instead, pass conversations to ConversationShell and let it render SessionsPanel internally as a sidebar within the card, OR keep the current grid layout but lift state up.
       - PREFERRED approach: Keep the grid layout as-is but make ConversationShell a wrapper that includes both SessionsPanel and the chat area. Move SessionsPanel rendering into ConversationShell. Update the grid: left sidebar has Documents + Research only. Main area (lg:col-span-3) has ConversationShell which internally shows sessions sidebar + chat.
       - Actually SIMPLEST approach: Keep sessions in left sidebar but make it a client component that communicates with conversation-shell via shared state. Use a simple pattern: ConversationShell exposes the session state, and project-content passes callbacks down. Since project-content is a server component, create a new client wrapper `project-client-content.tsx` that wraps the interactive parts, OR use URL state (searchParams) for active conversation.
       - BEST approach for minimal changes: Make project-content pass all data to a new `ProjectDetailClient` client component that manages the shared state between SessionsPanel and ConversationShell. The server component fetches data, the client component manages interactivity.
       - Create `dashboard/src/app/dashboard/planning/[projectId]/project-detail-client.tsx`:
         - 'use client' component receiving all the fetched data as props.
         - Manages activeConversationId, conversations list, messages state.
         - Renders the grid layout with SessionsPanel and ConversationShell, passing state and callbacks.
         - project-content.tsx becomes just the server data fetcher + header, delegating the interactive grid to ProjectDetailClient.
  </action>
  <verify>
    `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit`
    Start dev server, navigate to project page:
    1. Click "New Session" - should clear chat and allow new conversation.
    2. Send a message - conversation should appear in sidebar.
    3. Click "New Session" again, send another message - second conversation appears.
    4. Click the first conversation - its messages load.
  </verify>
  <done>Users can create new sessions, switch between sessions, and see the active session highlighted. Messages load when switching sessions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- API routes return correct data
- Sessions panel is interactive (new session, switch session)
- Active session is visually highlighted
- Switching sessions loads the correct messages
- New session clears the chat
</verification>

<success_criteria>
- Can create multiple sessions per phase
- Can switch between sessions and see their messages
- Active session is visually indicated in sidebar
- New session starts with empty chat
- Session list updates when new conversations are created
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/04-conversation-engine/04-03-SUMMARY.md`
</output>

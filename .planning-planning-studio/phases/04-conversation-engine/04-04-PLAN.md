---
phase: 04-conversation-engine
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Full conversation works end-to-end"
    - "Context loads correctly per phase"
    - "Messages persist in database"
    - "Streaming displays properly"
    - "Multiple sessions work"
    - "System prompts differ by phase"
---

<objective>
Verify the complete conversation engine works end-to-end.

Purpose: Confirm all Phase 4 success criteria are met before marking complete.
Output: Verified working conversation engine.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/phases/04-conversation-engine/04-02-SUMMARY.md
@.planning-planning-studio/phases/04-conversation-engine/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete conversation engine: streaming chat with Claude, markdown rendering, session management, message persistence, phase-specific system prompts.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/dashboard/planning and click into a project.
    2. Type a message and send it. Verify:
       - Response streams in as plain text
       - After completion, response renders as formatted Markdown
       - Auto-scroll keeps content visible
    3. Send 2-3 more messages. Verify conversation flows naturally.
    4. Check Supabase: planning_studio.messages table should have the messages. planning_studio.conversations should have a new conversation.
    5. Click "New Session" in the sidebar. Verify chat clears.
    6. Send a message in the new session. Verify a second conversation appears in the sidebar.
    7. Click the first conversation. Verify its messages load.
    8. Check that the system prompt matches the project's current phase (e.g., CAPTURE phase should ask about the idea, DISCOVER should probe deeper).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All Phase 4 success criteria from ROADMAP.md:
- [ ] Can have full conversation with Claude
- [ ] Context is loaded correctly per phase
- [ ] Messages are saved to database
- [ ] Streaming responses display properly
- [ ] Multiple sessions per phase work
- [ ] System prompts differ by phase
</verification>

<success_criteria>
User confirms all 6 success criteria pass.
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/04-conversation-engine/04-04-SUMMARY.md`
</output>

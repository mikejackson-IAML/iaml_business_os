---
phase: 04-conversation-engine
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/message-list.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/chat-input.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
autonomous: true

must_haves:
  truths:
    - "User can type a message and send it"
    - "Streaming response displays as plain text incrementally"
    - "Completed response renders as Markdown"
    - "Messages auto-scroll to bottom during streaming"
    - "Loading indicator shows while waiting for first token"
    - "Previous messages in conversation are displayed"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx"
      provides: "Main chat container with state management"
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/message-list.tsx"
      provides: "Message rendering with markdown"
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/chat-input.tsx"
      provides: "Input field with send button"
  key_links:
    - from: "conversation-shell.tsx"
      to: "/api/planning/chat"
      via: "fetch POST with SSE reading"
      pattern: "fetch.*api/planning/chat"
    - from: "message-list.tsx"
      to: "react-markdown"
      via: "Markdown rendering for completed messages"
      pattern: "ReactMarkdown|react-markdown"
    - from: "project-content.tsx"
      to: "conversation-shell.tsx"
      via: "passes projectId, project, phases, conversations props"
      pattern: "ConversationShell"
---

<objective>
Build the functional conversation UI that replaces the Phase 3 placeholder shell with a working chat interface.

Purpose: Users need to see messages, type, send, and watch streaming responses appear. This is the core interaction.
Output: Working chat interface with SSE streaming, markdown rendering, auto-scroll.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/phases/04-conversation-engine/04-01-SUMMARY.md

@dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx (replace this)
@dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx (update props)
@dashboard/src/dashboard-kit/types/departments/planning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown and create message components</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/message-list.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/chat-input.tsx
  </files>
  <action>
    1. Install dependencies: `cd dashboard && npm install react-markdown remark-gfm`

    2. Create `message-list.tsx`:
       - 'use client' component.
       - Props: `{ messages: Array<{id: string; role: 'user' | 'assistant'; content: string}>; streamingContent?: string; isStreaming: boolean }`
       - Renders list of messages. User messages right-aligned with blue bg. Assistant messages left-aligned with muted bg.
       - For assistant messages that are NOT currently streaming, render content with `<ReactMarkdown remarkPlugins={[remarkGfm]}>`. Apply prose styles via className.
       - For the currently streaming message (last message if isStreaming=true), render `streamingContent` as plain text (no markdown) with a blinking cursor indicator (CSS animation, pulsing dot or block cursor).
       - Use `useRef` for scroll container, `useEffect` to auto-scroll to bottom when messages change or streamingContent changes. Use `scrollIntoView({ behavior: 'smooth' })` on a sentinel div at the bottom.
       - Empty state: show the existing "Start a conversation" prompt with MessageSquare icon (from current conversation-shell.tsx).
       - Style message bubbles with rounded corners, padding. Keep them readable width (max-w-[80%]).

    3. Create `chat-input.tsx`:
       - 'use client' component.
       - Props: `{ onSend: (message: string) => void; disabled: boolean; placeholder?: string }`
       - Textarea (not Input) that auto-resizes up to 4 lines. Use `<textarea>` with rows=1 and onInput resize logic.
       - Send button with Send icon. Disabled when empty or when `disabled` prop is true.
       - Enter sends (without shift). Shift+Enter adds newline.
       - After send, clear the textarea and refocus.
       - Use shadcn Button for send button.
  </action>
  <verify>
    `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit`
  </verify>
  <done>Message list renders markdown for completed messages, plain text for streaming. Chat input handles enter-to-send with auto-resize.</done>
</task>

<task type="auto">
  <name>Task 2: Wire conversation shell with SSE streaming</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  </files>
  <action>
    1. Rewrite `conversation-shell.tsx` completely:
       - 'use client' component.
       - Props: `{ projectId: string; project: PlanningProject; phases: PlanningPhase[]; initialConversations: PlanningConversation[] }`
       - State: `messages` (array), `conversationId` (string | null), `isStreaming` (boolean), `streamingContent` (string), `error` (string | null).
       - On mount: if there's an active conversation (most recent from initialConversations), load its messages via fetch to a new client-side API or directly set conversationId. For now, start with empty messages (user will send first message to create conversation).
       - `handleSend(message: string)`:
         a. Add user message to local messages state immediately (optimistic).
         b. Set isStreaming = true, streamingContent = ''.
         c. POST to `/api/planning/chat` with `{ projectId, phaseType: project.current_phase, conversationId, message }`.
         d. Read the response as SSE stream using `response.body.getReader()` + TextDecoder. Parse each `data: {...}\n\n` line.
         e. For `type: "conversation_created"`: set conversationId from event.
         f. For `type: "text"`: append event.content to streamingContent.
         g. For `type: "done"`: set isStreaming = false, add the accumulated streamingContent as an assistant message to messages array, clear streamingContent.
         h. For `type: "error"`: set error state, set isStreaming = false.
       - Render: Card container (same h-full min-h-[500px] flex flex-col structure as current shell).
         - Header: "Conversation" title (keep simple).
         - Body: `<MessageList>` component with messages, streamingContent, isStreaming.
         - Footer: `<ChatInput>` with onSend={handleSend} disabled={isStreaming}.
       - Show error state as a dismissible banner above the input if error is set.

    2. Update `project-content.tsx`:
       - Pass `projectId={projectId}`, `project={project}`, `phases={phases}`, `initialConversations={conversations}` to `<ConversationShell>`.
       - The ConversationShell is a client component receiving serializable props from the server component.
  </action>
  <verify>
    `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit`
    Then start dev server and navigate to a project page. Type a message and verify streaming response appears.
  </verify>
  <done>User can type a message, send it, see streaming response as plain text, then see it render as markdown when complete. Auto-scroll works during streaming.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- react-markdown and remark-gfm are in package.json
- Conversation shell accepts project data as props
- SSE stream is consumed correctly in the client
- Messages render with markdown after completion
- Auto-scroll works during streaming
</verification>

<success_criteria>
- Can type and send a message from the project detail page
- Streaming response appears incrementally as plain text
- After streaming completes, response renders as formatted Markdown
- Auto-scroll keeps latest content visible
- Loading/streaming state is visually clear
- Error states show a dismissible error banner
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/04-conversation-engine/04-02-SUMMARY.md`
</output>

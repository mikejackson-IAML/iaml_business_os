---
phase: 03-project-detail-layout
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
autonomous: false

must_haves:
  truths:
    - "Conversation area shows message-style layout with input placeholder"
    - "When project is incubating, conversation area is replaced by incubation overlay"
    - "Incubation overlay shows encouraging message with approximate time remaining"
    - "Skip Incubation button opens confirmation dialog"
    - "All sidebar panels are wired into the grid with real data"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx"
      provides: "Message display area with non-functional input"
      min_lines: 30
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx"
      provides: "Incubation state UI with countdown and skip button"
      min_lines: 50
    - path: "dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx"
      provides: "Complete layout wiring all sub-components together"
      min_lines: 60
  key_links:
    - from: "project-content.tsx"
      to: "incubation-overlay.tsx"
      via: "conditional render"
      pattern: "isIncubating.*IncubationOverlay|ConversationShell"
    - from: "project-content.tsx"
      to: "sessions-panel.tsx"
      via: "prop passing"
      pattern: "SessionsPanel.*conversations"
    - from: "incubation-overlay.tsx"
      to: "planning.ts types"
      via: "getApproximateIncubationTime"
      pattern: "getApproximateIncubationTime"
---

<objective>
Build the conversation shell and incubation overlay, then wire all components together in project-content.tsx. Replace placeholder panels with real sidebar components from Plan 02 and add the main content area.

Purpose: Completes the project detail page layout with all visual components connected.
Output: Fully rendered project detail page with sidebar panels, conversation shell, and incubation state handling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/03-project-detail-layout/03-CONTEXT.md
@.planning-planning-studio/phases/03-project-detail-layout/03-RESEARCH.md

@.planning-planning-studio/phases/03-project-detail-layout/03-01-SUMMARY.md
@.planning-planning-studio/phases/03-project-detail-layout/03-02-SUMMARY.md

@dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
@dashboard/src/app/dashboard/planning/[projectId]/project-skeleton.tsx
@dashboard/src/dashboard-kit/types/departments/planning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build conversation shell and incubation overlay</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/conversation-shell.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/incubation-overlay.tsx
  </files>
  <action>
    **Conversation Shell** (`conversation-shell.tsx`):
    - Client component. No props needed yet (placeholder for Phase 4).
    - Card with full height (`h-full min-h-[500px]` matching skeleton)
    - CardHeader with border-b: "Conversation" title
    - CardContent: empty message area with centered empty state:
      - MessageSquare icon (large, muted)
      - "Start a conversation" heading
      - "Begin planning your idea with AI-guided conversations." description
    - Bottom section: input area with a disabled text input ("Type a message..." placeholder) and a disabled Send button. This is non-functional -- just layout for Phase 4.
    - Use flex column layout so messages area grows and input stays at bottom

    **Incubation Overlay** (`incubation-overlay.tsx`):
    - Client component. Props: `project: PlanningProject`
    - Card with full height, centered content, calming visual design
    - Moon icon (lucide `Moon`) -- large, amber/warm colored
    - Heading: "Let this idea marinate" (warm, encouraging tone per CONTEXT.md)
    - Approximate time: use `getApproximateIncubationTime(project)` -- display as subheading (e.g., "Available tomorrow morning")
    - Supporting text: "Great work getting here. Your subconscious is making connections while you wait. Come back refreshed." (calm, not corporate)
    - Two action buttons:
      - "View Documents" (variant="outline") -- placeholder, no action yet
      - "Review Conversations" (variant="outline") -- placeholder, no action yet
    - "Skip Incubation" button: smaller, muted style (variant="ghost", text-muted-foreground), positioned below the main actions
    - Skip button onClick: show a confirmation dialog (use shadcn AlertDialog or simple confirm):
      - Title: "Skip incubation?"
      - Description: "Incubation helps you think more clearly about your idea. Taking a break often leads to better decisions. Are you sure you want to skip?"
      - Cancel + "Yes, skip" buttons
      - On confirm: call a server action (placeholder -- just log for now, actual unlock in Phase 5)
    - Overall feel: spacious, centered, lots of whitespace, feels like a feature not a blocker
  </action>
  <verify>Run `npx tsc --noEmit` from dashboard directory -- no errors</verify>
  <done>Conversation shell shows empty state with disabled input. Incubation overlay shows warm, encouraging UI with approximate countdown and skip confirmation dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all components into project-content layout</name>
  <files>dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx</files>
  <action>
    Update project-content.tsx (from Plan 01) to replace placeholder panels with real components:

    1. Import all sub-components:
       - PhaseProgressBar from ./components/phase-progress-bar
       - SessionsPanel from ./components/sessions-panel
       - DocumentsPanel from ./components/documents-panel
       - ResearchPanel from ./components/research-panel
       - ConversationShell from ./components/conversation-shell
       - IncubationOverlay from ./components/incubation-overlay
       - isIncubating from planning types

    2. Replace placeholder sidebar divs with:
       ```
       <SessionsPanel conversations={conversations} />
       <DocumentsPanel documents={documents} />
       <ResearchPanel research={research} />
       ```

    3. Replace placeholder main area with conditional:
       ```
       {isIncubating(project) ? (
         <IncubationOverlay project={project} />
       ) : (
         <ConversationShell />
       )}
       ```

    4. Ensure all data is passed from the server-fetched variables to client components as props.

    This completes the full layout wiring.
  </action>
  <verify>
    1. Run `npx tsc --noEmit` -- no errors
    2. Navigate to project detail page -- all panels render with seed data
    3. If a project has phase_locked_until in the future, incubation overlay shows instead of conversation shell
  </verify>
  <done>Project detail page renders complete layout: header, progress bar, three sidebar panels with data, and conversation shell (or incubation overlay when locked).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete project detail page layout with phase progress bar, sidebar panels (sessions, documents, research), conversation shell, and incubation overlay</what-built>
  <how-to-verify>
    1. Navigate to /dashboard/planning and click on any project card
    2. Verify: Header shows project title, one-liner, status badge, and back link
    3. Verify: Phase progress bar shows 6 phases with correct visual states
    4. Verify: Hover over a phase to see time tooltip
    5. Verify: Left sidebar shows Sessions, Documents, and Research panels
    6. Verify: Panels show data or appropriate empty states
    7. Verify: Main area shows conversation shell with disabled input
    8. (Optional) To test incubation: temporarily set a project's phase_locked_until to a future date in Supabase, reload -- should show incubation overlay instead of conversation shell
    9. Verify: Skip Incubation button shows confirmation dialog
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compilation passes
2. All 5 sub-components render correctly
3. Data flows from server to client components via props
4. Incubation conditional rendering works
5. Layout matches the skeleton structure
6. Empty states display correctly
7. Skip incubation confirmation dialog appears
</verification>

<success_criteria>
- Project detail page is fully laid out with all visual components
- Sidebar panels display real data from the database
- Incubation overlay replaces conversation area when project is locked
- Skip incubation shows confirmation dialog
- Layout accommodates future left-nav (no conflicting structure)
- User approves visual layout
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/03-project-detail-layout/03-03-SUMMARY.md`
</output>

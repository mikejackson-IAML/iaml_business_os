---
phase: 03-project-detail-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Sessions panel lists conversations with title, date, and message count"
    - "Documents panel lists documents with type label and version number"
    - "Research panel lists research runs with status indicator and query text"
    - "All three panels show meaningful empty states when no data exists"
    - "New Session button is visible but non-functional (placeholder)"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx"
      provides: "Conversation list with metadata display"
      min_lines: 40
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx"
      provides: "Document list with type and version"
      min_lines: 40
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx"
      provides: "Research run list with status badges"
      min_lines: 40
  key_links:
    - from: "sessions-panel.tsx"
      to: "planning.ts types"
      via: "PlanningConversation type"
      pattern: "PlanningConversation"
    - from: "documents-panel.tsx"
      to: "planning.ts types"
      via: "PlanningDocument type"
      pattern: "PlanningDocument"
    - from: "research-panel.tsx"
      to: "planning.ts types"
      via: "PlanningResearch type"
      pattern: "PlanningResearch"
---

<objective>
Build the three sidebar panels: Sessions, Documents, and Research. Each receives typed data as props and renders a list with appropriate metadata, empty states, and action placeholders.

Purpose: Populates the left sidebar column of the project detail layout with real data display.
Output: Three client components ready to be plugged into the project-content grid.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/03-project-detail-layout/03-CONTEXT.md
@.planning-planning-studio/phases/03-project-detail-layout/03-RESEARCH.md

@dashboard/src/dashboard-kit/types/departments/planning.ts
@dashboard/src/app/dashboard/planning/[projectId]/project-skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Sessions panel</name>
  <files>dashboard/src/app/dashboard/planning/[projectId]/components/sessions-panel.tsx</files>
  <action>
    Client component. Props: `conversations: PlanningConversation[]`.

    Structure:
    - Card with CardHeader: title "Sessions" with MessageSquare icon, plus a "New Session" Button (variant="ghost", size="sm") that is disabled/placeholder (no onClick yet)
    - CardContent: list of conversation items
    - Each conversation item: clickable div (no navigation yet, just visual), showing:
      - Title (or "Untitled Session" fallback)
      - Relative date (e.g., "2 days ago") using simple date math -- reuse the inline formatRelativeTime pattern from project-card.tsx or create a small inline helper
      - Message count badge (e.g., "12 msgs")
      - If conversation has summary, show first ~60 chars truncated
    - Empty state: MessageSquare icon, "No sessions yet", "Start a conversation to begin planning" muted text
    - Use shadcn Card, Badge, Button from existing imports
  </action>
  <verify>Run `npx tsc --noEmit` from dashboard directory -- no errors from this file</verify>
  <done>Sessions panel renders conversation list with metadata or empty state</done>
</task>

<task type="auto">
  <name>Task 2: Build Documents and Research panels</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/documents-panel.tsx
    dashboard/src/app/dashboard/planning/[projectId]/components/research-panel.tsx
  </files>
  <action>
    **Documents Panel** (`documents-panel.tsx`):
    - Client component. Props: `documents: PlanningDocument[]`
    - Card with CardHeader: title "Documents" with FileText icon
    - Each document item: clickable div showing:
      - Document type label (map doc_type to readable name: 'icp' -> 'ICP Profile', 'lean_canvas' -> 'Lean Canvas', 'problem_statement' -> 'Problem Statement', 'feature_spec' -> 'Feature Spec', 'technical_scope' -> 'Technical Scope', 'competitive_intel' -> 'Competitive Intel', 'gsd_project' -> 'PROJECT.md', 'gsd_requirements' -> 'REQUIREMENTS.md', 'gsd_roadmap' -> 'ROADMAP.md')
      - Version badge: "v{version}"
      - Updated date (relative)
    - Empty state: FileText icon, "No documents yet", "Documents will be generated during planning conversations"

    **Research Panel** (`research-panel.tsx`):
    - Client component. Props: `research: PlanningResearch[]`
    - Card with CardHeader: title "Research" with Search icon
    - Each research item: div showing:
      - Query text (truncated to ~80 chars)
      - Status badge with color: pending=gray, running=blue (with spinner/pulse), complete=green, failed=red
      - Research type label (map research_type: 'icp_deep_dive' -> 'ICP Deep Dive', 'competitive_analysis' -> 'Competitive Analysis', 'market_research' -> 'Market Research', 'user_workflows' -> 'User Workflows', 'technical_feasibility' -> 'Technical Feasibility', 'custom' -> 'Custom')
      - Created date (relative)
    - Empty state: Search icon, "No research yet", "Research runs will appear here when triggered during conversations"
  </action>
  <verify>Run `npx tsc --noEmit` from dashboard directory -- no errors from these files</verify>
  <done>Documents panel renders document list with type labels and version badges or empty state. Research panel renders research list with status indicators or empty state.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All three panel components export correctly and accept typed props
3. Empty states display when arrays are empty
4. Data items render with all required metadata fields
</verification>

<success_criteria>
- Sessions panel shows conversations with title, date, message count
- Documents panel shows documents with type label, version, date
- Research panel shows research with query, status badge, type
- All panels have polished empty states with helpful text
- New Session button exists (disabled/placeholder)
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/03-project-detail-layout/03-02-SUMMARY.md`
</output>

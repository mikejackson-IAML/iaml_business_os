---
phase: 03-project-detail-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  - dashboard/src/app/dashboard/planning/[projectId]/components/phase-progress-bar.tsx
  - dashboard/src/lib/api/planning-queries.ts
  - dashboard/src/dashboard-kit/types/departments/planning.ts
autonomous: true

must_haves:
  truths:
    - "Project detail page shows project title, one-liner, and back-to-pipeline link"
    - "Phase progress bar displays all 6 phases with current phase highlighted"
    - "Completed phases show checkmark, current shows filled indicator, future shows empty"
    - "Hovering a phase shows time spent (relative date)"
    - "Incubation countdown appears inline in progress bar when project is locked"
  artifacts:
    - path: "dashboard/src/app/dashboard/planning/[projectId]/components/phase-progress-bar.tsx"
      provides: "6-phase visual stepper with time-in-phase and incubation display"
      min_lines: 60
    - path: "dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx"
      provides: "Full layout shell with data fetching, header, progress bar, grid skeleton"
      min_lines: 50
  key_links:
    - from: "project-content.tsx"
      to: "planning-queries.ts"
      via: "server-side data fetching"
      pattern: "getPlanningProject|getProjectPhases|getProjectConversations|getProjectDocuments|getProjectResearch"
    - from: "phase-progress-bar.tsx"
      to: "planning.ts types"
      via: "helper imports"
      pattern: "PHASE_ORDER|getPhaseLabel|getPhaseIcon|getPhaseStatusColor"
---

<objective>
Build the project detail page layout shell: header with project info, phase progress bar component, and data-fetching wiring in project-content.tsx. This replaces the placeholder ProjectContent with a real layout that fetches all project data and renders the grid structure (1+3 columns) matching the existing skeleton.

Purpose: Establishes the page structure that sidebar panels and conversation area will plug into.
Output: Working project detail page with header, progress bar, and 4-column grid layout ready for child components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning-planning-studio/PROJECT.md
@.planning-planning-studio/ROADMAP.md
@.planning-planning-studio/STATE.md
@.planning-planning-studio/phases/03-project-detail-layout/03-CONTEXT.md
@.planning-planning-studio/phases/03-project-detail-layout/03-RESEARCH.md

@dashboard/src/app/dashboard/planning/[projectId]/page.tsx
@dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
@dashboard/src/app/dashboard/planning/[projectId]/project-skeleton.tsx
@dashboard/src/lib/api/planning-queries.ts
@dashboard/src/dashboard-kit/types/departments/planning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getProjectResearch query and getApproximateIncubationTime helper</name>
  <files>
    dashboard/src/lib/api/planning-queries.ts
    dashboard/src/dashboard-kit/types/departments/planning.ts
  </files>
  <action>
    1. Add `getProjectResearch(projectId: string): Promise<PlanningResearch[]>` to planning-queries.ts following the exact same pattern as `getProjectDocuments` -- uses `.schema('planning_studio')`, queries `research` table, orders by `created_at` descending, returns `PlanningResearch[]`.

    2. Add `getApproximateIncubationTime(project: PlanningProject): string | null` to the planning.ts types file in the HELPER FUNCTIONS section. Logic:
       - Return null if no `phase_locked_until` or if lock has passed
       - Calculate hours remaining
       - Map to approximate strings: >36h = "Available in a few days", >20h = "Available tomorrow morning", >12h = "Available tomorrow", >4h = "Available later today", >1h = "Available in a couple hours", else "Available soon"

    This follows the code examples from 03-RESEARCH.md exactly.
  </action>
  <verify>
    Run `npx tsc --noEmit` from the dashboard directory. No new type errors from these additions.
  </verify>
  <done>
    `getProjectResearch` function exists and follows the established query pattern. `getApproximateIncubationTime` returns approximate human-friendly strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build phase progress bar and rewire project-content layout</name>
  <files>
    dashboard/src/app/dashboard/planning/[projectId]/components/phase-progress-bar.tsx
    dashboard/src/app/dashboard/planning/[projectId]/project-content.tsx
  </files>
  <action>
    **Phase Progress Bar** (`components/phase-progress-bar.tsx`):
    - Client component receiving `phases: PlanningPhase[]`, `currentPhase: PhaseType`, `project: PlanningProject` as props
    - Render horizontal stepper using `PHASE_ORDER` array (6 items)
    - For each phase in PHASE_ORDER:
      - Find matching PlanningPhase record from `phases` array (match on `phase_type`)
      - Completed: green checkmark icon (CheckCircle2 from lucide), filled connector line
      - Current/in_progress: blue filled circle with phase icon (use `getPhaseIcon` to get icon name, render corresponding lucide icon), pulsing ring animation
      - Incubating: amber moon icon with approximate countdown from `getApproximateIncubationTime(project)` displayed below phase label
      - Not started: gray empty circle, muted connector line
      - Phase label below each circle using `getPhaseLabel()`
    - On hover (Tooltip), show time spent: if `started_at` exists, show duration formatted as "3 days" or "2 hours"; if `completed_at` exists, show "Completed 2 days ago" using relative date
    - Connector lines between circles (horizontal line segments, colored based on completion)
    - Use shadcn Tooltip for hover info
    - Make past completed phases clickable (onClick callback prop `onPhaseClick?: (phase: PhaseType) => void`) -- just wire the callback, actual filtering is deferred

    **Project Content** (`project-content.tsx`):
    - Convert from placeholder to real layout. Keep it as a client component (receives projectId prop from server page.tsx).
    - BUT: since data fetching needs server context, convert project-content.tsx to fetch data. Two approaches:
      - Option A (preferred): Make project-content.tsx a server component that fetches data and passes to a client wrapper. BUT page.tsx already uses Suspense with ProjectContent as a client component.
      - Option B (simpler): Use `useEffect` + fetch from API route, or...
      - Option C (best, matches codebase): Make project-content.tsx a SERVER async component. Remove 'use client' directive. Fetch all data server-side. Render client sub-components with data as props. The page.tsx already wraps in Suspense which works with async server components.
    - Go with Option C. Remove 'use client' from project-content.tsx. Make it async:
      ```
      export async function ProjectContent({ projectId }: { projectId: string })
      ```
    - Fetch: `getPlanningProject(projectId)`, `getProjectPhases(projectId)`, `getProjectConversations(projectId)`, `getProjectDocuments(projectId)`, `getProjectResearch(projectId)`
    - If project is null, show a "Project not found" message with back link
    - Render layout:
      1. Header: Back to Pipeline link (ArrowLeft icon), project title (h1), one-liner (muted text), status badge
      2. PhaseProgressBar component with fetched data
      3. Grid: `grid grid-cols-1 lg:grid-cols-4 gap-6`
         - Left column (1 col): placeholder divs for sidebar panels with text "Sessions Panel", "Documents Panel", "Research Panel" -- these will be replaced in Plan 02
         - Right column (`lg:col-span-3`): placeholder div for conversation area -- replaced in Plan 03
    - Import and use Badge for status display, use `getStatusLabel()` and `getStatusColor()`
  </action>
  <verify>
    1. Run `npx tsc --noEmit` from dashboard directory -- no new errors
    2. Navigate to a project detail page in the browser -- should show header, progress bar, and grid layout with placeholder panels
  </verify>
  <done>
    Project detail page renders with: back link, project title/one-liner, status badge, 6-phase progress bar with correct states, and 4-column grid layout. Phase tooltips show time information on hover.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. Project detail page loads without runtime errors
3. Progress bar shows 6 phases with correct visual states
4. Phase tooltips display time information
5. Grid layout matches the skeleton structure (1 + 3 columns)
6. Back to Pipeline link navigates correctly
</verification>

<success_criteria>
- Project detail page shows real project data (title, one-liner, status)
- Phase progress bar renders all 6 phases with appropriate visual states
- Layout grid is in place for sidebar and conversation panels
- getProjectResearch query function exists
- getApproximateIncubationTime helper exists and returns friendly strings
</success_criteria>

<output>
After completion, create `.planning-planning-studio/phases/03-project-detail-layout/03-01-SUMMARY.md`
</output>

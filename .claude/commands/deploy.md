# Production Deployment

Deploy to Vercel production (www.iaml.com).

## Instructions

1. Run `vercel --prod` from the project root to deploy to production
2. Return the production URL to the user
3. Confirm the deployment is live at www.iaml.com

## Command

```bash
cd "/Users/mike/IAML Business OS" && npx vercel --prod
```

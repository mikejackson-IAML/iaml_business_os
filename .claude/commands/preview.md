# Preview Deployment

Deploy to Vercel preview environment for testing before production.

## Instructions

1. Run `vercel` (without --prod flag) from the project root to deploy a preview
2. Return the preview URL to the user
3. Remind them they can run `/deploy` when ready for production

## Command

```bash
cd "/Users/mike/IAML Business OS" && npx vercel
```

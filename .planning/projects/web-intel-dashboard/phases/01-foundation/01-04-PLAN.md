---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/dashboard-content.tsx
autonomous: true

must_haves:
  truths:
    - "Web Intel appears in dashboard navigation sidebar"
    - "Navigation link uses Globe icon from lucide-react"
    - "Clicking Web Intel navigates to /dashboard/web-intel"
  artifacts:
    - path: "dashboard/src/app/dashboard/dashboard-content.tsx"
      provides: "Dashboard navigation with Web Intel link"
      contains: "Web Intel"
  key_links:
    - from: "dashboard/src/app/dashboard/dashboard-content.tsx"
      to: "/dashboard/web-intel"
      via: "Link href"
      pattern: "href.*dashboard/web-intel"
---

<objective>
Add Web Intel to the dashboard navigation.

Purpose: Make the Web Intel dashboard accessible from the main navigation sidebar.

Output: Updated dashboard-content.tsx with Web Intel navigation entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/projects/web-intel-dashboard/ROADMAP.md
@dashboard/src/app/dashboard/dashboard-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Web Intel to navigation links</name>
  <files>dashboard/src/app/dashboard/dashboard-content.tsx</files>
  <action>
Update dashboard-content.tsx to add Web Intel navigation:

1. Add Globe import to the lucide-react imports (if not already present):
   ```typescript
   import { Users, Target, TrendingUp, MessageSquare, Mail, Phone, Linkedin, Monitor, BarChart3, GraduationCap, ArrowRight, CheckSquare, FolderCode, Globe } from 'lucide-react';
   ```

2. Find the Quick Links section in the header (around line 125-166 where the Link components are).

3. Add a new Link for Web Intel after the existing department links. Insert it in a logical position (after Digital or after Development):

   ```tsx
   <Link
     href="/dashboard/web-intel"
     className="flex items-center gap-2 px-4 py-2 rounded-lg bg-cyan-500/10 hover:bg-cyan-500/20 text-cyan-600 dark:text-cyan-400 transition-colors"
   >
     <Globe className="h-4 w-4" />
     <span className="text-sm font-medium">Web Intel</span>
     <ArrowRight className="h-3 w-3" />
   </Link>
   ```

4. Use cyan color scheme to differentiate from other departments:
   - Action Center: orange
   - Digital: blue
   - Marketing: pink
   - Programs: emerald
   - Development: purple
   - Web Intel: cyan (new)

The link should appear in the Quick Links row in the header section, maintaining consistent styling with other department links.
  </action>
  <verify>
1. Search file for "Web Intel" text - should appear
2. Search file for "web-intel" in href - should appear
3. Search file for Globe import - should be in import line
4. Visual check: Run `npm run dev` and verify link appears in dashboard header
  </verify>
  <done>
- Globe icon imported from lucide-react
- Web Intel Link added to Quick Links section
- Link uses cyan color scheme
- href points to /dashboard/web-intel
- Styling consistent with other department links
  </done>
</task>

</tasks>

<verification>
- [ ] Globe icon imported in dashboard-content.tsx
- [ ] Link to /dashboard/web-intel exists
- [ ] Link shows "Web Intel" text
- [ ] Link uses consistent styling with other nav items
- [ ] File compiles without TypeScript errors
</verification>

<success_criteria>
Dashboard navigation includes "Web Intel" option that links to /dashboard/web-intel with a Globe icon.
</success_criteria>

<output>
After completion, create `.planning/projects/web-intel-dashboard/phases/01-foundation/01-04-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/dashboard-kit/types/departments/web-intel.ts
autonomous: true

must_haves:
  truths:
    - "TypeScript types exist for all major web_intel database tables"
    - "WebIntelMetrics interface defines key dashboard metrics"
    - "WebIntelDashboardData interface structures the complete data payload"
    - "webIntelDepartmentConfig export provides dashboard configuration"
  artifacts:
    - path: "dashboard/src/dashboard-kit/types/departments/web-intel.ts"
      provides: "Web Intel TypeScript type definitions"
      exports: ["DailyTraffic", "TrackedKeyword", "DailyRanking", "CoreWebVitals", "WebIntelAlert", "WebIntelMetrics", "WebIntelDashboardData", "webIntelDepartmentConfig"]
  key_links:
    - from: "dashboard/src/dashboard-kit/types/departments/web-intel.ts"
      to: "dashboard/src/dashboard-kit/types/dashboard.ts"
      via: "import base types"
      pattern: "import.*from.*dashboard"
---

<objective>
Create TypeScript type definitions for the Web Intel dashboard.

Purpose: Establish type-safe data structures matching the web_intel Supabase schema so queries and components have proper typing.

Output: `dashboard/src/dashboard-kit/types/departments/web-intel.ts` with interfaces for all major data structures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/projects/web-intel-dashboard/ROADMAP.md
@supabase/migrations/20260121_create_web_intel_schema.sql
@dashboard/src/dashboard-kit/types/dashboard.ts
@dashboard/src/dashboard-kit/types/departments/lead-intelligence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Web Intel type definitions file</name>
  <files>dashboard/src/dashboard-kit/types/departments/web-intel.ts</files>
  <action>
Create the web-intel.ts types file following the lead-intelligence.ts pattern:

1. Import base types from '../dashboard':
   - DepartmentConfig, MetricValue, AlertItem, HealthStatus

2. Define interfaces matching web_intel schema tables:

   **Traffic Types:**
   - DailyTraffic: sessions, users, pageviews, bounce_rate, avg_session_duration, new_users, returning_users, pages_per_session
   - PageTraffic: page_path, page_title, sessions, pageviews, unique_pageviews, avg_time_on_page, bounce_rate, exit_rate, entrances
   - TrafficSource: source, medium, sessions, users, new_users, bounce_rate

   **Rankings Types:**
   - TrackedKeyword: keyword, search_volume, difficulty, cpc, priority, target_url, category, status
   - DailyRanking: keyword_id, position, ranking_url, has_featured_snippet, featured_snippet_owner, competitor_positions (JSONB as array)
   - RankingChangeEvent: previous_position, current_position, change_amount, change_type, is_significant

   **Technical Types:**
   - IndexCoverage: indexed_count, crawled_not_indexed, discovered_not_indexed, excluded_count, error_count
   - CoreWebVitals: device_type, lcp_good_pct/needs_improvement_pct/poor_pct, fid_*, cls_*, overall_status
   - SearchPerformance: query, page, clicks, impressions, ctr, position

   **Content Types:**
   - ContentInventory: url, title, meta_description, word_count, content_type, status, avg_monthly_sessions
   - ContentDecay: baseline_sessions, current_sessions, decay_percentage, severity, is_addressed

   **Competitor Types:**
   - Competitor: domain, name, is_active
   - CompetitorRanking: competitor_id, keyword_id, position, url
   - SerpShare: our_share, competitor_shares (JSONB), keywords_tracked, keywords_ranking

   **Backlink Types:**
   - BacklinkProfile: total_backlinks, referring_domains, dofollow_links, domain_rating, new_links_7d, lost_links_7d

   **Alert Type:**
   - WebIntelAlert: alert_type, severity, title, message, metadata

3. Define WebIntelMetrics interface with MetricValue fields:
   - dailySessions, weeklyTraffic, bounceRate, avgKeywordPosition, topPageviews, coreWebVitalsScore, indexedPages, backlinkCount

4. Define WebIntelDashboardData interface:
   - metrics: WebIntelMetrics
   - traffic: DailyTraffic[]
   - topPages: PageTraffic[]
   - keywords: TrackedKeyword[]
   - rankings: DailyRanking[]
   - rankingChanges: RankingChangeEvent[]
   - coreWebVitals: CoreWebVitals[]
   - indexCoverage: IndexCoverage | null
   - searchPerformance: SearchPerformance[]
   - contentDecay: ContentDecay[]
   - competitors: Competitor[]
   - backlinkProfile: BacklinkProfile | null
   - alerts: AlertItem[]
   - overallHealth: HealthStatus

5. Export webIntelDepartmentConfig matching DepartmentConfig type:
   - department: 'web-intel'
   - title: 'Web Intelligence'
   - summaryPrompt describing what to report
   - keyMetrics array with 6 key metrics (sessions, rankings, CWV, indexed pages, backlinks, alerts)
   - quickActions: Check Rankings, Traffic Report, Content Health
   - statusIndicators with healthy/warning/critical conditions
   - dashboardSections: traffic overview, rankings, technical health, content, alerts
   - refreshIntervalSeconds: 3600 (hourly)
  </action>
  <verify>
Run: `cd /Users/mike/IAML\ Business\ OS/dashboard && npx tsc --noEmit src/dashboard-kit/types/departments/web-intel.ts`
File compiles without TypeScript errors.
  </verify>
  <done>
- web-intel.ts exists with all interfaces
- Imports DepartmentConfig, MetricValue, AlertItem, HealthStatus from base types
- All 15+ interfaces defined matching schema
- WebIntelMetrics has 8 metric fields
- WebIntelDashboardData structures complete payload
- webIntelDepartmentConfig exported
- No TypeScript errors
  </done>
</task>

</tasks>

<verification>
- [ ] File exists at dashboard/src/dashboard-kit/types/departments/web-intel.ts
- [ ] TypeScript compiles without errors
- [ ] Interfaces match web_intel schema table structures
- [ ] DepartmentConfig follows lead-intelligence.ts pattern
</verification>

<success_criteria>
TypeScript types defined for all major web_intel data structures, enabling type-safe queries and components.
</success_criteria>

<output>
After completion, create `.planning/projects/web-intel-dashboard/phases/01-foundation/01-01-SUMMARY.md`
</output>

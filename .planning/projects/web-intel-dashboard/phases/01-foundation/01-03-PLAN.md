---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - dashboard/src/app/dashboard/web-intel/page.tsx
  - dashboard/src/app/dashboard/web-intel/web-intel-content.tsx
  - dashboard/src/app/dashboard/web-intel/web-intel-skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "/dashboard/web-intel route renders a page with Web Intel title"
    - "Page fetches data using getWebIntelDashboardData"
    - "Loading state shows skeleton component"
  artifacts:
    - path: "dashboard/src/app/dashboard/web-intel/page.tsx"
      provides: "Server component page entry point"
      exports: ["default", "metadata"]
    - path: "dashboard/src/app/dashboard/web-intel/web-intel-content.tsx"
      provides: "Client component with dashboard UI"
      exports: ["WebIntelContent"]
    - path: "dashboard/src/app/dashboard/web-intel/web-intel-skeleton.tsx"
      provides: "Loading state skeleton"
      exports: ["WebIntelSkeleton"]
  key_links:
    - from: "dashboard/src/app/dashboard/web-intel/page.tsx"
      to: "dashboard/src/lib/api/web-intel-queries.ts"
      via: "getWebIntelDashboardData import"
      pattern: "import.*getWebIntelDashboardData"
    - from: "dashboard/src/app/dashboard/web-intel/page.tsx"
      to: "dashboard/src/app/dashboard/web-intel/web-intel-content.tsx"
      via: "WebIntelContent import"
      pattern: "import.*WebIntelContent"
---

<objective>
Create the dashboard route and page structure for Web Intel.

Purpose: Establish the `/dashboard/web-intel` route with proper server/client component separation and loading states.

Output: Three files creating a working route that fetches and displays Web Intel data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/projects/web-intel-dashboard/ROADMAP.md
@.planning/projects/web-intel-dashboard/phases/01-foundation/01-01-SUMMARY.md
@.planning/projects/web-intel-dashboard/phases/01-foundation/01-02-SUMMARY.md
@dashboard/src/app/dashboard/leads/page.tsx
@dashboard/src/app/dashboard/leads/leads-content.tsx
@dashboard/src/app/dashboard/leads/leads-skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Web Intel skeleton component</name>
  <files>dashboard/src/app/dashboard/web-intel/web-intel-skeleton.tsx</files>
  <action>
Create the skeleton loading component following leads-skeleton.tsx pattern:

```typescript
import { Card, CardContent, CardHeader } from '@/dashboard-kit/components/ui/card';
import { Skeleton } from '@/dashboard-kit/components/ui/skeleton';

export function WebIntelSkeleton() {
  return (
    <div className="p-6 lg:p-8 space-y-6">
      {/* Header skeleton */}
      <div className="space-y-2">
        <Skeleton className="h-8 w-48" />
        <Skeleton className="h-4 w-96" />
      </div>

      {/* Metrics grid skeleton - 4 cards */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        {[1, 2, 3, 4].map((i) => (
          <Card key={i}>
            <CardContent className="pt-6">
              <Skeleton className="h-4 w-24 mb-2" />
              <Skeleton className="h-8 w-16" />
            </CardContent>
          </Card>
        ))}
      </div>

      {/* Main content area skeleton */}
      <div className="grid grid-cols-12 gap-6">
        {/* Left column - charts */}
        <div className="col-span-12 lg:col-span-8 space-y-6">
          <Card>
            <CardHeader>
              <Skeleton className="h-6 w-32" />
            </CardHeader>
            <CardContent>
              <Skeleton className="h-64 w-full" />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <Skeleton className="h-6 w-32" />
            </CardHeader>
            <CardContent>
              <Skeleton className="h-48 w-full" />
            </CardContent>
          </Card>
        </div>

        {/* Right column - sidebar */}
        <div className="col-span-12 lg:col-span-4 space-y-6">
          <Card>
            <CardHeader>
              <Skeleton className="h-6 w-24" />
            </CardHeader>
            <CardContent>
              <Skeleton className="h-32 w-full" />
            </CardContent>
          </Card>
          <Card>
            <CardHeader>
              <Skeleton className="h-6 w-24" />
            </CardHeader>
            <CardContent className="space-y-3">
              {[1, 2, 3, 4].map((i) => (
                <Skeleton key={i} className="h-12 w-full" />
              ))}
            </CardContent>
          </Card>
        </div>
      </div>
    </div>
  );
}
```

The skeleton should mirror the expected layout of the content component.
  </action>
  <verify>File exists and exports WebIntelSkeleton component.</verify>
  <done>Skeleton component created with proper loading UI structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create Web Intel content component</name>
  <files>dashboard/src/app/dashboard/web-intel/web-intel-content.tsx</files>
  <action>
Create the client content component:

```typescript
'use client';

import { Globe, TrendingUp, Activity, FileText, Link2, AlertTriangle } from 'lucide-react';
import { MetricCard } from '@/dashboard-kit/components/dashboard/metric-card';
import { HealthScore } from '@/dashboard-kit/components/dashboard/health-score';
import { Card, CardContent, CardHeader, CardTitle } from '@/dashboard-kit/components/ui/card';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/dashboard-kit/components/ui/tabs';
import type { WebIntelDashboardData } from '@/dashboard-kit/types/departments/web-intel';

interface WebIntelContentProps {
  data: WebIntelDashboardData;
}

export function WebIntelContent({ data }: WebIntelContentProps) {
  return (
    <div className="p-6 lg:p-8 space-y-6">
      {/* Header */}
      <header>
        <div className="flex items-center gap-3 mb-2">
          <Globe className="h-8 w-8 text-blue-500" />
          <h1 className="text-display-sm text-foreground">Web Intelligence</h1>
        </div>
        <p className="text-muted-foreground">
          Traffic analytics, keyword rankings, technical SEO health, and content performance
        </p>
      </header>

      {/* Key Metrics Row */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        <MetricCard
          label="Daily Sessions"
          value={data.metrics.dailySessions.value}
          icon={TrendingUp}
          format="number"
          status={data.metrics.dailySessions.status}
        />
        <MetricCard
          label="Avg Position"
          value={data.metrics.avgKeywordPosition.value}
          icon={Activity}
          format="number"
          status={data.metrics.avgKeywordPosition.status}
        />
        <MetricCard
          label="Indexed Pages"
          value={data.metrics.indexedPages.value}
          icon={FileText}
          format="number"
          status={data.metrics.indexedPages.status}
        />
        <MetricCard
          label="Backlinks"
          value={data.metrics.backlinkCount.value}
          icon={Link2}
          format="number"
          status={data.metrics.backlinkCount.status}
        />
      </div>

      {/* Main Content with Tabs */}
      <Tabs defaultValue="overview" className="space-y-6">
        <TabsList>
          <TabsTrigger value="overview">Overview</TabsTrigger>
          <TabsTrigger value="rankings">Rankings</TabsTrigger>
          <TabsTrigger value="technical">Technical</TabsTrigger>
          <TabsTrigger value="content">Content</TabsTrigger>
        </TabsList>

        <TabsContent value="overview" className="space-y-6">
          <div className="grid grid-cols-12 gap-6">
            {/* Left column */}
            <div className="col-span-12 lg:col-span-8 space-y-6">
              {/* Traffic Overview Card */}
              <Card>
                <CardHeader>
                  <CardTitle>Traffic Overview</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-muted-foreground">
                    Traffic chart will be implemented in Phase 2.
                    {data.traffic.length > 0 && (
                      <span className="block mt-2">
                        Latest: {data.traffic[0]?.sessions?.toLocaleString() ?? 0} sessions
                      </span>
                    )}
                  </p>
                </CardContent>
              </Card>

              {/* Top Pages Card */}
              <Card>
                <CardHeader>
                  <CardTitle>Top Pages</CardTitle>
                </CardHeader>
                <CardContent>
                  {data.topPages.length === 0 ? (
                    <p className="text-muted-foreground text-center py-4">
                      No page traffic data available.
                    </p>
                  ) : (
                    <div className="space-y-2">
                      {data.topPages.slice(0, 5).map((page, i) => (
                        <div
                          key={page.pagePath || i}
                          className="flex justify-between items-center p-2 rounded bg-background-card-light"
                        >
                          <span className="truncate max-w-[70%]">{page.pagePath}</span>
                          <span className="text-muted-foreground">
                            {page.pageviews?.toLocaleString() ?? 0} views
                          </span>
                        </div>
                      ))}
                    </div>
                  )}
                </CardContent>
              </Card>
            </div>

            {/* Right column */}
            <div className="col-span-12 lg:col-span-4 space-y-6">
              {/* Health Score */}
              <HealthScore
                score={75}
                status={data.overallHealth}
                label="SEO Health"
                description="Based on rankings, technical health, and content quality"
              />

              {/* Alerts */}
              <Card>
                <CardHeader>
                  <CardTitle className="flex items-center gap-2">
                    <AlertTriangle className="h-4 w-4" />
                    Active Alerts
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  {data.alerts.length === 0 ? (
                    <p className="text-muted-foreground text-center py-4">
                      No active alerts.
                    </p>
                  ) : (
                    <div className="space-y-2">
                      {data.alerts.slice(0, 5).map((alert) => (
                        <div
                          key={alert.id}
                          className={`p-3 rounded border-l-4 ${
                            alert.severity === 'critical'
                              ? 'border-red-500 bg-red-500/10'
                              : alert.severity === 'warning'
                              ? 'border-yellow-500 bg-yellow-500/10'
                              : 'border-blue-500 bg-blue-500/10'
                          }`}
                        >
                          <p className="font-medium text-sm">{alert.title}</p>
                          {alert.description && (
                            <p className="text-xs text-muted-foreground mt-1">
                              {alert.description}
                            </p>
                          )}
                        </div>
                      ))}
                    </div>
                  )}
                </CardContent>
              </Card>
            </div>
          </div>
        </TabsContent>

        <TabsContent value="rankings">
          <Card>
            <CardHeader>
              <CardTitle>Keyword Rankings</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-muted-foreground">
                Rankings tab content will be implemented in Phase 3.
              </p>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="technical">
          <Card>
            <CardHeader>
              <CardTitle>Technical SEO Health</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-muted-foreground">
                Technical health tab will be implemented in Phase 4.
              </p>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="content">
          <Card>
            <CardHeader>
              <CardTitle>Content Performance</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-muted-foreground">
                Content tab will be implemented in Phase 5.
              </p>
            </CardContent>
          </Card>
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

This creates a basic structure with tabs for future content. The Overview tab shows real data.
  </action>
  <verify>File exports WebIntelContent component with proper TypeScript types.</verify>
  <done>Content component created with tabs structure and basic data display.</done>
</task>

<task type="auto">
  <name>Task 3: Create Web Intel page entry point</name>
  <files>dashboard/src/app/dashboard/web-intel/page.tsx</files>
  <action>
Create the page.tsx server component following leads/page.tsx pattern:

```typescript
import { Suspense } from 'react';
import { WebIntelSkeleton } from './web-intel-skeleton';
import { WebIntelContent } from './web-intel-content';
import { getWebIntelDashboardData } from '@/lib/api/web-intel-queries';

export const metadata = {
  title: 'Web Intelligence | IAML Business OS',
  description: 'Traffic analytics, keyword rankings, technical SEO, and content performance',
};

// Revalidate every hour (data updates less frequently than leads)
export const revalidate = 3600;

async function WebIntelDataLoader() {
  const data = await getWebIntelDashboardData();
  return <WebIntelContent data={data} />;
}

export default function WebIntelDashboardPage() {
  return (
    <Suspense fallback={<WebIntelSkeleton />}>
      <WebIntelDataLoader />
    </Suspense>
  );
}
```

Key elements:
- Server component with metadata export
- revalidate = 3600 (1 hour cache)
- Suspense boundary with skeleton fallback
- Async data loader component
  </action>
  <verify>
Run: `cd /Users/mike/IAML\ Business\ OS/dashboard && npx tsc --noEmit src/app/dashboard/web-intel/page.tsx`
Page compiles and imports resolve correctly.
  </verify>
  <done>
- page.tsx exists with metadata and revalidate exports
- Uses Suspense with WebIntelSkeleton fallback
- Calls getWebIntelDashboardData and passes to WebIntelContent
  </done>
</task>

</tasks>

<verification>
- [ ] Directory exists: dashboard/src/app/dashboard/web-intel/
- [ ] page.tsx exports default function and metadata
- [ ] web-intel-content.tsx is client component ('use client')
- [ ] web-intel-skeleton.tsx provides loading UI
- [ ] Page fetches data from getWebIntelDashboardData
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
`/dashboard/web-intel` route renders a page with "Web Intelligence" title, shows loading skeleton during data fetch, and displays basic metrics when data loads.
</success_criteria>

<output>
After completion, create `.planning/projects/web-intel-dashboard/phases/01-foundation/01-03-SUMMARY.md`
</output>

---
phase: 7
plan: 6
title: "Workflow Header with Progress"
wave: 3
depends_on: ["07-01"]
files_modified:
  - dashboard/src/app/dashboard/action-center/workflows/[id]/workflow-detail-content.tsx
  - dashboard/src/app/dashboard/action-center/components/workflow-progress.tsx
  - dashboard/src/app/dashboard/action-center/components/index.ts
autonomous: true
estimated_minutes: 10
---

# Plan 07-06: Workflow Header with Progress

## Goal
Build the workflow detail header section with progress indicator, status badge, and metadata display.

## Context
Per 07-CONTEXT: progress indicator in header area (progress bar or ring alongside workflow title). Reuse patterns from workflow-context.tsx which already has progress display logic.

## Tasks

<task id="1">
Create `workflow-progress.tsx` component with:
- Progress ring (circular) showing percentage
- Text display: "X of Y complete (Z%)"
- Props: completed, total, className
- Uses existing Progress component or custom SVG ring
</task>

<task id="2">
Create `workflow-detail-content.tsx` with header section:
- Back link to /dashboard/action-center/workflows
- Workflow name as h1
- Status badge (reuse workflowStatusConfig from workflow-row)
- WorkflowProgress component
- Description (if exists)
- Metadata row: Target date, Department, Started/Completed timestamps
</task>

<task id="3">
Add task count breakdown badges:
- Open: X, In Progress: X, Waiting: X, Done: X, Dismissed: X
- Use subtle badge variants
- Show counts from task_count_by_status
</task>

<task id="4">
Add export to components/index.ts:
- export { WorkflowProgress } from "./workflow-progress";
</task>

<task id="5">
Add placeholder section for task list (to be built in 07-07):
- Section header: "Tasks"
- Comment indicating where WorkflowTaskList will go
</task>

## Verification
- [ ] Workflow name displays as prominent header
- [ ] Status badge shows correct color and text
- [ ] Progress ring/bar shows correct percentage
- [ ] Progress text shows "X of Y complete"
- [ ] Task count badges display all statuses
- [ ] Metadata (date, department) displays correctly
- [ ] Description renders when present

## Must Haves
- must_have_1: Progress indicator prominently visible in header area (WF-03)
- must_have_2: Workflow status computed and displayed correctly (WF-06)
- must_have_3: Task count breakdown provides at-a-glance status

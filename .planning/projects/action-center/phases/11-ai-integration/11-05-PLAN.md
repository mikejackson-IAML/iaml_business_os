---
wave: 3
depends_on:
  - 11-04
files_modified:
  - dashboard/src/app/dashboard/action-center/components/confidence-badge.tsx
  - dashboard/src/app/dashboard/action-center/components/task-row.tsx
  - dashboard/src/app/dashboard/action-center/tasks/[id]/task-detail-content.tsx
  - dashboard/src/app/dashboard/action-center/components/index.ts
autonomous: true
---

# Plan 11-05: Confidence Score Display

## Goal
Display AI confidence scores on suggested tasks with visual indicators and reasoning when confidence is low.

## Requirements Covered
- AI-07: Confidence score display

## Context
- Confidence stored as NUMERIC(3,2) = 0.00 to 1.00
- Display as percentage (0-100%)
- Show reasoning when confidence < 80%
- Per CONTEXT.md: "obvious suggestions skip explanation"

## Tasks

<task autonomous="true">
<title>Create ConfidenceBadge component</title>
<file path="dashboard/src/app/dashboard/action-center/components/confidence-badge.tsx" action="create" />
<description>
'use client';

import { Bot, Sparkles, AlertCircle } from 'lucide-react';
import { Badge } from '@/dashboard-kit/components/ui/badge';
import { Tooltip, TooltipContent, TooltipTrigger, TooltipProvider } from '@/dashboard-kit/components/ui/tooltip';

interface ConfidenceBadgeProps {
  confidence: number | null;  // 0.00 - 1.00
  reasoning?: string | null;
  showLabel?: boolean;  // "AI 85%" vs just "85%"
  size?: 'sm' | 'md';
}

export function ConfidenceBadge({
  confidence,
  reasoning,
  showLabel = true,
  size = 'sm'
}: ConfidenceBadgeProps) {
  if (confidence === null || confidence === undefined) {
    return null;  // Not an AI suggestion
  }

  const percentage = Math.round(confidence * 100);

  // Color coding based on confidence level
  let bgColor: string;

  if (percentage >= 80) {
    bgColor = 'bg-emerald-500/10 text-emerald-600';  // High confidence - green
  } else if (percentage >= 60) {
    bgColor = 'bg-amber-500/10 text-amber-600';  // Medium confidence - amber
  } else {
    bgColor = 'bg-slate-500/10 text-slate-600';  // Lower confidence - neutral
  }

  const label = showLabel ? `AI ${percentage}%` : `${percentage}%`;

  // If has reasoning (low confidence), show tooltip
  if (reasoning && percentage < 80) {
    return (
      <TooltipProvider>
        <Tooltip>
          <TooltipTrigger>
            <Badge className={`${bgColor} gap-1 cursor-help`}>
              <Sparkles className={size === 'sm' ? 'h-3 w-3' : 'h-4 w-4'} />
              {label}
              <AlertCircle className="h-3 w-3 ml-1" />
            </Badge>
          </TooltipTrigger>
          <TooltipContent className="max-w-xs">
            <p className="text-sm"><strong>Why AI suggests this:</strong></p>
            <p className="text-sm text-muted-foreground">{reasoning}</p>
          </TooltipContent>
        </Tooltip>
      </TooltipProvider>
    );
  }

  return (
    <Badge className={`${bgColor} gap-1`}>
      <Sparkles className={size === 'sm' ? 'h-3 w-3' : 'h-4 w-4'} />
      {label}
    </Badge>
  );
}
</description>
</task>

<task autonomous="true">
<title>Add confidence display to task row</title>
<file path="dashboard/src/app/dashboard/action-center/components/task-row.tsx" action="modify" />
<description>
Import ConfidenceBadge component.

In TaskRow, after source badge, add confidence display:

{/* Source */}
<div className="flex items-center gap-1.5 text-sm text-muted-foreground">
  <SourceIcon className="h-3.5 w-3.5" />
  <span className="capitalize">{task.source || 'Manual'}</span>
</div>

{/* Confidence (AI tasks only) */}
{task.source === 'ai' && task.ai_confidence !== null && (
  <div className="flex items-center">
    <ConfidenceBadge
      confidence={task.ai_confidence}
      showLabel={false}
      size="sm"
    />
  </div>
)}

Show confidence inline with source for AI tasks to keep layout consistent.
</description>
</task>

<task autonomous="true">
<title>Add confidence display to task detail</title>
<file path="dashboard/src/app/dashboard/action-center/tasks/[id]/task-detail-content.tsx" action="modify" />
<description>
Import ConfidenceBadge.

In the badges section (after Source Badge), add AI confidence:

{/* Source Badge */}
<Badge variant="outline" className="gap-1">
  <SourceIcon className="h-3 w-3" />
  <span className="capitalize">{task.source || 'Manual'}</span>
</Badge>

{/* AI Confidence (for AI-suggested tasks) */}
{task.source === 'ai' && task.ai_confidence !== null && (
  <ConfidenceBadge
    confidence={task.ai_confidence}
    reasoning={task.description?.match(/\*\*Why AI suggests this:\*\* (.+)/)?.[1] || null}
    showLabel={true}
    size="md"
  />
)}

Also add in metadata sidebar, under "Created":
{task.source === 'ai' && task.ai_suggested_at && (
  <div>
    <p className="text-xs text-muted-foreground uppercase tracking-wide mb-1">AI Suggested</p>
    <p className="text-sm">{format(new Date(task.ai_suggested_at), 'MMM d, yyyy')}</p>
    {task.ai_confidence !== null && (
      <ConfidenceBadge confidence={task.ai_confidence} showLabel={true} />
    )}
  </div>
)}
</description>
</task>

<task autonomous="true">
<title>Add component export</title>
<file path="dashboard/src/app/dashboard/action-center/components/index.ts" action="modify" />
<description>
Add to exports:
export { ConfidenceBadge } from './confidence-badge';
</description>
</task>

## Verification
1. Confidence badge shows on AI-suggested tasks
2. Color coding: green >= 80%, amber 60-79%, neutral < 60%
3. Tooltip shows reasoning when confidence < 80%
4. Badge visible in task row and task detail
5. Works for both list and detail views

## must_haves
- [ ] ConfidenceBadge component created
- [ ] Confidence shown on task rows (AI tasks)
- [ ] Confidence shown on task detail page
- [ ] Reasoning tooltip for low confidence
- [ ] Color coding by confidence level

---
wave: 2
depends_on:
  - 11-02
files_modified:
  - dashboard/src/components/widgets/weekly-focus-widget.tsx
  - dashboard/src/app/dashboard/dashboard-content.tsx
  - dashboard/src/lib/api/task-queries.ts
autonomous: true
---

# Plan 11-03: Weekly Focus Dashboard Widget

## Goal
Create dashboard widget that displays the current week's AI-generated focus summary with encouraging coach tone.

## Requirements Covered
- AI-03: Dashboard widget delivery of Weekly Focus Review

## Context
- Pattern: Follow action-center-widget.tsx structure
- Widget fetches latest task_type='review' with source='ai'
- Display summary and top suggestions
- Link to full Weekly Focus task

## Tasks

<task autonomous="true">
<title>Add query for latest weekly focus task</title>
<file path="dashboard/src/lib/api/task-queries.ts" action="modify" />
<description>
Add new function:

export async function getLatestWeeklyFocus(): Promise<TaskExtended | null> {
  const supabase = getServerClient();

  const { data, error } = await supabase
    .from('tasks_extended')
    .select('*')
    .eq('task_type', 'review')
    .eq('source', 'ai')
    .like('title', 'Weekly Focus Review%')
    .order('created_at', { ascending: false })
    .limit(1)
    .single();

  if (error) {
    if (error.code === 'PGRST116') return null;
    throw error;
  }

  return data as TaskExtended;
}

Also add getAISuggestionCount():

export async function getAISuggestionCount(): Promise<number> {
  const supabase = getServerClient();

  const { count, error } = await supabase
    .from('tasks')
    .select('id', { count: 'exact', head: true })
    .eq('source', 'ai')
    .eq('status', 'open');

  if (error) throw error;
  return count || 0;
}
</description>
</task>

<task autonomous="true">
<title>Create Weekly Focus Widget component</title>
<file path="dashboard/src/components/widgets/weekly-focus-widget.tsx" action="create" />
<description>
'use client';

import Link from 'next/link';
import { Sparkles, ChevronRight, Lightbulb, TrendingUp } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from '@/dashboard-kit/components/ui/card';
import { Skeleton } from '@/dashboard-kit/components/ui/skeleton';
import type { TaskExtended } from '@/lib/api/task-types';

interface WeeklyFocusWidgetProps {
  focusTask: TaskExtended | null;
  suggestionCount: number;
  isLoading?: boolean;
}

Component structure:
1. Card header with Sparkles icon and "Weekly Focus" title
2. If no focusTask, show placeholder "AI Focus analysis coming Sunday evening"
3. If focusTask exists:
   - Parse description to extract summary section
   - Display summary with encouraging tone styling
   - Show suggestion count chip
   - "View full analysis" link to task detail
4. Loading skeleton state

Extract summary from markdown:
- Look for ## This Week's Focus section
- Take first 2-3 sentences as summary preview

Styling:
- Use accent-primary for highlights
- Soft gradient background for encouraging feel
- Suggestion count badge with AI icon

Skeleton component for loading state.
</description>
</task>

<task autonomous="true">
<title>Integrate widget into dashboard</title>
<file path="dashboard/src/app/dashboard/dashboard-content.tsx" action="modify" />
<description>
1. Import WeeklyFocusWidget
2. Add to DashboardContentProps:
   - weeklyFocusTask: TaskExtended | null
   - aiSuggestionCount: number
3. Render WeeklyFocusWidget after ActionCenterWidget:

{/* Action Center Widget */}
<ActionCenterWidget counts={taskCounts} />

{/* Weekly Focus Widget */}
<WeeklyFocusWidget
  focusTask={weeklyFocusTask}
  suggestionCount={aiSuggestionCount}
/>

Position: Second widget in left column, after Action Center widget.
</description>
</task>

<task autonomous="true">
<title>Update dashboard page data fetching</title>
<file path="dashboard/src/app/dashboard/page.tsx" action="modify" />
<description>
Add data fetching for weekly focus:

import { getLatestWeeklyFocus, getAISuggestionCount } from '@/lib/api/task-queries';

In getServerSideProps or server component:

const [weeklyFocusTask, aiSuggestionCount] = await Promise.all([
  getLatestWeeklyFocus(),
  getAISuggestionCount(),
]);

Pass to DashboardContent.
</description>
</task>

## Verification
1. Widget displays when Weekly Focus task exists
2. Placeholder shown when no Weekly Focus task
3. Summary extracted and displayed correctly
4. Suggestion count shown
5. Link to task detail works
6. Widget positioned after Action Center widget

## must_haves
- [ ] WeeklyFocusWidget component created
- [ ] getLatestWeeklyFocus query function
- [ ] Widget integrated into dashboard
- [ ] Encouraging visual styling
- [ ] Link to full analysis works

---
wave: 5
depends_on:
  - 11-06
  - 11-07
files_modified:
  - supabase/migrations/20260125_ai_suggestion_expiry.sql
  - business-os/workflows/weekly-ai-focus.json
  - dashboard/src/app/dashboard/action-center/components/task-row.tsx
  - dashboard/src/app/dashboard/action-center/components/index.ts
autonomous: true
---

# Plan 11-08: Suggestion Expiry and Final Integration

## Goal
Implement auto-expiry of unreviewed AI suggestions after 7 days and complete final integration.

## Requirements Covered
- AI-04: Auto-expire suggestions after 7 days

## Context
- Unreviewed suggestions (source='ai', status='open') expire after 7 days
- Expiry sets status='dismissed', dismissed_reason='ai_expired'
- Can be handled by database function or n8n workflow

## Tasks

<task autonomous="true">
<title>Create suggestion expiry database function</title>
<file path="supabase/migrations/20260125_ai_suggestion_expiry.sql" action="create" />
<description>
-- AI Suggestion Expiry
-- Auto-expire unreviewed AI suggestions after 7 days
-- Date: 2026-01-25

-- Function to expire old AI suggestions
CREATE OR REPLACE FUNCTION action_center.expire_ai_suggestions()
RETURNS INTEGER AS $$
DECLARE
  expired_count INTEGER;
BEGIN
  WITH expired AS (
    UPDATE action_center.tasks
    SET
      status = 'dismissed',
      dismissed_reason = 'ai_expired: Suggestion not reviewed within 7 days',
      dismissed_at = NOW(),
      updated_at = NOW()
    WHERE
      source = 'ai'
      AND status = 'open'
      AND ai_suggested_at IS NOT NULL
      AND ai_suggested_at < NOW() - INTERVAL '7 days'
    RETURNING id
  )
  SELECT COUNT(*) INTO expired_count FROM expired;

  RETURN expired_count;
END;
$$ LANGUAGE plpgsql;

-- Comment
COMMENT ON FUNCTION action_center.expire_ai_suggestions IS
  'Expire AI suggestions that have not been reviewed within 7 days';

-- Grant execute to authenticated users (for RPC call)
GRANT EXECUTE ON FUNCTION action_center.expire_ai_suggestions TO authenticated;
</description>
</task>

<task autonomous="true">
<title>Add expiry check to weekly workflow</title>
<file path="business-os/workflows/weekly-ai-focus.json" action="modify" />
<description>
Add a node to call expire_ai_suggestions before generating new suggestions:

Insert after Schedule Trigger, before Determine Mode:

{
  "parameters": {
    "query": "SELECT action_center.expire_ai_suggestions() as expired_count;",
    "options": {}
  },
  "id": "expire-old-suggestions",
  "name": "Expire Old Suggestions",
  "type": "n8n-nodes-base.postgres",
  "typeVersion": 2.4,
  "position": [360, 300],
  "credentials": {
    "postgres": {
      "id": "{{SUPABASE_CREDENTIAL_ID}}",
      "name": "Supabase Postgres"
    }
  }
}

Update connections:
- Schedule -> Expire Old Suggestions -> Determine Mode -> ...

Add to Slack success log:
- Include expired_count in message if > 0
</description>
</task>

<task autonomous="true">
<title>Add expiry indicator to task row</title>
<file path="dashboard/src/app/dashboard/action-center/components/task-row.tsx" action="modify" />
<description>
Show a visual indicator for AI suggestions nearing expiry:

Add to TaskRow for AI tasks:

// Calculate days until expiry
const getDaysUntilExpiry = (suggestedAt: string | null): number | null => {
  if (!suggestedAt) return null;
  const suggested = new Date(suggestedAt);
  const expiryDate = new Date(suggested.getTime() + 7 * 24 * 60 * 60 * 1000);
  const now = new Date();
  return Math.ceil((expiryDate.getTime() - now.getTime()) / (24 * 60 * 60 * 1000));
};

// Show warning badge if expiring soon (2 days or less)
{task.source === 'ai' && task.status === 'open' && task.ai_suggested_at && (
  (() => {
    const daysLeft = getDaysUntilExpiry(task.ai_suggested_at);
    if (daysLeft !== null && daysLeft <= 2) {
      return (
        <span className="px-1.5 py-0.5 text-xs rounded bg-amber-100 text-amber-700">
          Expires in {daysLeft}d
        </span>
      );
    }
    return null;
  })()
)}
</description>
</task>

<task autonomous="true">
<title>Add components to index exports</title>
<file path="dashboard/src/app/dashboard/action-center/components/index.ts" action="modify" />
<description>
Ensure all new components are exported:

export { ConfidenceBadge } from './confidence-badge';
export { AISuggestionActions } from './ai-suggestion-actions';
export { RejectSuggestionDialog } from './reject-suggestion-dialog';
</description>
</task>

<task autonomous="true">
<title>Final verification testing</title>
<file path="dashboard/src/app/dashboard/action-center/page.tsx" action="verify" />
<description>
Verify complete integration:

1. AI Suggested view filters work (source='ai', status='open')
2. Weekly Focus widget displays on dashboard
3. Confidence badges show on AI tasks
4. Accept/Reject flow works end-to-end
5. Expiry indicator shows for suggestions nearing 7 days
6. Pattern detection runs during AI analysis

Manual testing checklist:
- [ ] Run weekly-ai-focus workflow manually
- [ ] Verify Weekly Focus Review task created
- [ ] Verify AI suggestions created with confidence
- [ ] Accept a suggestion - becomes in_progress
- [ ] Reject a suggestion with reason - becomes dismissed
- [ ] Verify dashboard widget shows weekly summary
- [ ] Check expiry badge on old suggestion
</description>
</task>

## Verification
1. expire_ai_suggestions() function exists
2. Workflow calls expiry before generating new suggestions
3. Expired tasks have dismissed_reason='ai_expired'
4. Expiry indicator shows on tasks nearing 7 days
5. All components properly exported and integrated

## must_haves
- [ ] expire_ai_suggestions database function
- [ ] Expiry integrated into weekly workflow
- [ ] Expiry indicator on task rows
- [ ] All new components exported
- [ ] End-to-end flow verified

---
phase: 04
plan: 04-01
title: Page Foundation and Skeleton
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/action-center/page.tsx
  - dashboard/src/app/dashboard/action-center/action-center-skeleton.tsx
autonomous: true
---

# Plan 04-01: Page Foundation and Skeleton

## Objective

Create the Action Center page structure with Suspense wrapper and loading skeleton.

## Context

This is the first UI page for Action Center. Follow the established dashboard page pattern (Suspense + data loader + client content). The skeleton should show the table structure to set user expectations during load.

## Reference Files

- `dashboard/src/app/dashboard/digital/workflows/page.tsx` - Page structure pattern
- `dashboard/src/app/dashboard/leads/leads-skeleton.tsx` - Skeleton pattern
- `dashboard/src/dashboard-kit/components/ui/skeleton.tsx` - Skeleton component

## Tasks

<task id="1">
Create page.tsx with Suspense wrapper

File: `dashboard/src/app/dashboard/action-center/page.tsx`

```typescript
import { Suspense } from 'react';
import ActionCenterSkeleton from './action-center-skeleton';
import ActionCenterDataLoader from './action-center-data-loader';

export default function ActionCenterPage() {
  return (
    <Suspense fallback={<ActionCenterSkeleton />}>
      <ActionCenterDataLoader />
    </Suspense>
  );
}
```

Simple Suspense wrapper. Data loader will be created in plan 04-05.
</task>

<task id="2">
Create action-center-skeleton.tsx

File: `dashboard/src/app/dashboard/action-center/action-center-skeleton.tsx`

Shows loading state with:
- Header skeleton (title + view tabs placeholder)
- Filter toolbar skeleton (row of dropdown placeholders)
- Table skeleton (header row + 5 skeleton rows with pulses)

Use Skeleton component from dashboard-kit. Match table structure: 5 columns for Priority, Title, Due Date, Department, Source.

Structure:
```typescript
'use client';

import { Skeleton, TableRowSkeleton } from '@/dashboard-kit/components/ui/skeleton';
import { Card } from '@/dashboard-kit/components/ui/card';

export default function ActionCenterSkeleton() {
  return (
    <div className="space-y-6">
      {/* Header */}
      <div className="flex justify-between items-center">
        <Skeleton className="h-8 w-48" /> {/* Title */}
        <Skeleton className="h-10 w-96" /> {/* View tabs */}
      </div>

      {/* Filter toolbar */}
      <div className="flex gap-4">
        {[1, 2, 3, 4, 5].map((i) => (
          <Skeleton key={i} className="h-10 w-32" />
        ))}
        <Skeleton className="h-10 w-64 ml-auto" /> {/* Search */}
      </div>

      {/* Table */}
      <Card>
        {/* Header */}
        <div className="flex border-b border-border px-6 py-3">
          <Skeleton className="h-4 w-20" />
          <Skeleton className="h-4 w-48 ml-8" />
          <Skeleton className="h-4 w-24 ml-auto" />
          <Skeleton className="h-4 w-24 ml-8" />
          <Skeleton className="h-4 w-24 ml-8" />
        </div>

        {/* Rows */}
        {[1, 2, 3, 4, 5].map((i) => (
          <div key={i} className="flex items-center border-b border-border last:border-0 px-6 py-4">
            <Skeleton className="h-6 w-20" />
            <Skeleton className="h-6 w-64 ml-8" />
            <Skeleton className="h-6 w-24 ml-auto" />
            <Skeleton className="h-6 w-24 ml-8" />
            <Skeleton className="h-6 w-24 ml-8" />
          </div>
        ))}
      </Card>
    </div>
  );
}
```
</task>

<task id="3">
Create placeholder data loader

File: `dashboard/src/app/dashboard/action-center/action-center-data-loader.tsx`

Temporary placeholder that shows the skeleton content works. Will be replaced in plan 04-05.

```typescript
import ActionCenterSkeleton from './action-center-skeleton';

export default async function ActionCenterDataLoader() {
  // Placeholder - will be replaced in 04-05
  return <ActionCenterSkeleton />;
}
```
</task>

## Verification

```bash
# Build check
cd dashboard && npm run build

# Files exist
ls -la src/app/dashboard/action-center/
```

Manual verification:
- Navigate to /dashboard/action-center
- See skeleton loading state

## Must-Haves

- [ ] page.tsx exists with Suspense wrapper
- [ ] action-center-skeleton.tsx shows loading state
- [ ] Skeleton matches table structure (5 columns)
- [ ] No build errors

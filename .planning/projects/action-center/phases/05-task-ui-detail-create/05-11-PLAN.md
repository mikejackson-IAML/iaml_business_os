---
wave: 7
depends_on: ["05-03", "05-04", "05-05", "05-06", "05-07", "05-08", "05-09", "05-10"]
files_modified:
  - dashboard/src/app/dashboard/action-center/action-center-content.tsx
  - dashboard/src/app/dashboard/action-center/components/task-row.tsx
  - dashboard/src/app/dashboard/action-center/components/task-table.tsx
  - dashboard/src/app/dashboard/action-center/tasks/[id]/task-detail-content.tsx
autonomous: true
estimated_minutes: 15
---

# Plan 05-11: Final Integration

## Objective
Wire up all components: add create button to list page, make task rows navigate to detail page, and ensure all imports are correct.

## Tasks

<task id="1">
Update `action-center-content.tsx` to add Create Task button and modal:

1. Import CreateTaskModal from './components/create-task-modal'
2. Import Plus from lucide-react
3. Add state: showCreateModal (boolean)
4. Add Create Task button in header next to Refresh button
5. Add CreateTaskModal at bottom with isOpen, onClose, and departments props
</task>

<task id="2">
Update `task-row.tsx` to navigate to detail page on click:

1. Import Link from 'next/link'
2. Replace the expandable button/div with Link component
3. Link href to `/dashboard/action-center/tasks/${task.id}`
4. Keep same grid layout and styling
5. Remove ChevronRight/ChevronDown toggle icon (navigation replaces expansion)
</task>

<task id="3">
Update `task-table.tsx` to remove expansion tracking:

1. Remove expandedRows state
2. Remove toggleExpand handler
3. Remove isExpanded prop from TaskRow calls
4. Keep the TaskRow component simple - it just links now
</task>

<task id="4">
Verify all imports in `task-detail-content.tsx` are correct:

Required imports:
- TaskMetadataSidebar from '../../components/task-metadata-sidebar'
- TaskComments from '../../components/task-comments'
- TaskActivityList from '../../components/task-activity'
- CompleteTaskDialog from '../../components/complete-task-dialog'
- DismissTaskDialog from '../../components/dismiss-task-dialog'
- ApprovalActions, RecommendationCallout from '../../components/approval-actions'
- WorkflowContext from '../../components/workflow-context'

Verify component usage:
- RecommendationCallout shown for approval tasks with recommendation
- ApprovalActions instead of Complete/Dismiss for approval tasks
- WorkflowContext when task.workflow_id exists
</task>

<task id="5">
Add dependencies section to task-detail-content.tsx (if not already present):

Show card when task.blocked_by_count > 0 OR task.blocking_count > 0:
- "Dependencies" heading
- "Blocked by: X task(s)" in warning color if blocked_by_count > 0
- "Blocking: X task(s)" if blocking_count > 0

This satisfies UI-17: Dependencies section: blocked by / blocking.
</task>

## Verification
- [ ] Create Task button visible on list page header
- [ ] Create Task modal opens when button clicked
- [ ] Clicking a task row navigates to detail page
- [ ] Detail page shows all components correctly
- [ ] Dependencies section shows when task has dependencies (UI-17)
- [ ] Approval tasks show recommendation callout and approval buttons
- [ ] Non-approval tasks show Complete/Dismiss buttons
- [ ] Workflow context shows when task has workflow_id
- [ ] All imports resolve correctly
- [ ] No TypeScript errors
- [ ] Navigation works (back button, task links)

## must_haves
- Create button on list page opens modal (UI-20)
- Task row click navigates to detail page
- Dependencies section (UI-17)
- All components integrated correctly

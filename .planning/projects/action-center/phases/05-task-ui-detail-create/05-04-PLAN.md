---
wave: 2
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/action-center/components/task-metadata-sidebar.tsx
autonomous: true
estimated_minutes: 15
---

# Plan 05-04: Metadata Sidebar

## Objective
Create the metadata sidebar component showing status, priority, due date, department, workflow info, and related entity link.

## Tasks

<task id="1">
Create `task-metadata-sidebar.tsx` with imports:

- useState, useTransition from 'react'
- Link from 'next/link'
- Card, CardContent from dashboard-kit
- Badge from dashboard-kit
- Icons: ExternalLink, Calendar, Building, Workflow, Link as LinkIcon from lucide-react
- TaskExtended, TaskStatus types
- updateTaskStatusAction from actions
- format from date-fns
</task>

<task id="2">
Implement status dropdown using native select with custom styling:

- Define statusOptions array: open, in_progress, waiting, done, dismissed
- Use useTransition for pending state during update
- handleStatusChange calls updateTaskStatusAction
- Disable select during isPending
- This satisfies UI-12: Status change dropdown
</task>

<task id="3">
Implement all metadata fields as a vertical list in Card:

1. **Status** (with dropdown from task 2)

2. **Priority** (display only with colored indicator):
   - Reuse priorityConfig from task-row.tsx pattern

3. **Due Date**:
   - Calendar icon
   - Format using date-fns: 'MMM d, yyyy'
   - Show time if due_time exists
   - Show "No due date" if null

4. **Department**:
   - Building icon
   - Show department or "Unassigned"

5. **Task Type**:
   - Display task_type with capitalize

6. **Source**:
   - Display source with icon (reuse sourceIcons pattern)

7. **Workflow** (if task.workflow_id):
   - Workflow icon
   - Link to `/dashboard/action-center/workflows/${task.workflow_id}`
   - Show workflow_name or "View workflow"
   - ExternalLink icon
   - This satisfies UI-16: Workflow link

8. **Related Entity** (if task.related_entity_type):
   - LinkIcon
   - If related_entity_url: external link with ExternalLink icon
   - Else: plain text with entity type
   - This satisfies UI-15: Related entity link
</task>

<task id="4">
Wrap everything in Card with proper spacing:

- CardContent with p-4 space-y-4
- Each field: label (text-xs text-muted-foreground uppercase tracking-wide) + value
</task>

## Verification
- [ ] Component displays all task metadata
- [ ] Status dropdown allows changing status (UI-12)
- [ ] Priority displays with colored indicator
- [ ] Due date shows formatted date
- [ ] Workflow link works when workflow_id exists (UI-16)
- [ ] Related entity link works when URL exists (UI-15)
- [ ] Pending state disables dropdown during update
- [ ] TypeScript compiles without errors

## must_haves
- Status change dropdown (UI-12)
- Related entity link (UI-15)
- Workflow link (UI-16)
- All task metadata displayed

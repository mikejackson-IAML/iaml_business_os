---
wave: 4
depends_on: ["05-01"]
files_modified:
  - dashboard/src/app/dashboard/action-center/components/task-comments.tsx
autonomous: true
estimated_minutes: 15
---

# Plan 05-06: Comments Tab

## Objective
Create the comments tab component with comment list and add comment form.

## Tasks

<task id="1">
Create `task-comments.tsx` with imports:

- useState, useTransition from 'react'
- MessageSquare, Send, User from lucide-react
- Button from dashboard-kit
- Card, CardContent from dashboard-kit
- TaskComment type
- addCommentAction from actions
- formatDistanceToNow from date-fns
</task>

<task id="2">
Implement CommentItem component:

- Display author avatar (placeholder icon)
- Author name (or 'System' for system comments)
- Timestamp using formatDistanceToNow with addSuffix
- Comment type badge for system/status_change comments (visually distinct, opacity-70)
- Comment content with whitespace-pre-wrap
</task>

<task id="3">
Implement AddCommentForm component:

- Input field for comment content
- Submit button with Send icon
- useTransition for pending state
- Validate content is not empty
- Call addCommentAction on submit
- Clear input on success
- Show error inline if fails
</task>

<task id="4">
Implement main TaskComments component:

Props:
- taskId: string
- comments: TaskComment[]

Layout:
- Card wrapper with CardContent p-4
- AddCommentForm at top
- Comments list below (space-y-4)
- Empty state: MessageSquare icon, "No comments yet", "Be the first to add a comment"
</task>

## Verification
- [ ] Comments list displays all comments
- [ ] Each comment shows author, timestamp, and content
- [ ] System/status_change comments are visually distinct
- [ ] Add comment form works with Enter key submission
- [ ] Form clears on successful submission
- [ ] Empty state shows when no comments
- [ ] Loading state during submission
- [ ] TypeScript compiles without errors

## must_haves
- Comments thread with add comment (UI-18)
- Comment form with submit functionality
- Visual distinction for different comment types

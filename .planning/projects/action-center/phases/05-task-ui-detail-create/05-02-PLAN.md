---
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/action-center/tasks/[id]/page.tsx
  - dashboard/src/app/dashboard/action-center/tasks/[id]/task-detail-skeleton.tsx
autonomous: true
estimated_minutes: 15
---

# Plan 05-02: Detail Page Route with Skeleton

## Objective
Create the detail page route with Suspense boundary and loading skeleton following the workflow-detail pattern.

## Tasks

<task id="1">
Create directory structure: `dashboard/src/app/dashboard/action-center/tasks/[id]/`
</task>

<task id="2">
Create `task-detail-skeleton.tsx` with loading skeleton matching two-column layout:

- Back navigation skeleton
- Header skeleton (title, badges, action buttons)
- Two-column grid: main content card + sidebar card
- Use Skeleton component from dashboard-kit
</task>

<task id="3">
Create `page.tsx` as server component with Suspense:

- Import Suspense from 'react', notFound from 'next/navigation'
- Import TaskDetailSkeleton and TaskDetailContent
- Import getTaskById, getTaskComments, getTaskActivity from task-queries
- Set metadata: title 'Task Detail | Action Center'
- Set revalidate = 60 for ISR
- Create async TaskDetailLoader that fetches task, comments, activity in parallel
- Return notFound() if task doesn't exist
- Wrap TaskDetailLoader in Suspense with TaskDetailSkeleton fallback
</task>

## Verification
- [ ] Directory `tasks/[id]/` exists
- [ ] `page.tsx` is a server component with Suspense
- [ ] `TaskDetailSkeleton` renders loading state matching two-column layout
- [ ] Page fetches task, comments, and activity in parallel
- [ ] Returns 404 if task not found
- [ ] TypeScript compiles without errors

## must_haves
- Task detail page accessible at `/action-center/tasks/:id` (UI-11)
- Loading skeleton for good UX
- Server-side data fetching

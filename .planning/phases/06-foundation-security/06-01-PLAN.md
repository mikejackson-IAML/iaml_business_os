---
phase: 06-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BusinessCommandCenter/App/BusinessCommandCenterApp.swift
  - BusinessCommandCenter/App/ContentView.swift
  - BusinessCommandCenter/Features/Home/HomeView.swift
  - BusinessCommandCenter/Features/Chat/ChatView.swift
  - BusinessCommandCenter/Features/Settings/SettingsView.swift
  - BusinessCommandCenter/Info.plist
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can launch app and see three tabs"
    - "Tapping each tab switches to that view"
    - "Tab bar shows Home, Chat, Settings with correct icons"
  artifacts:
    - path: "BusinessCommandCenter/App/BusinessCommandCenterApp.swift"
      provides: "@main entry point with WindowGroup"
      contains: "@main"
    - path: "BusinessCommandCenter/App/ContentView.swift"
      provides: "TabView with three tabs"
      contains: "TabView"
    - path: "BusinessCommandCenter/Features/Home/HomeView.swift"
      provides: "Home tab placeholder"
      contains: "struct HomeView"
    - path: "BusinessCommandCenter/Features/Chat/ChatView.swift"
      provides: "Chat tab placeholder"
      contains: "struct ChatView"
    - path: "BusinessCommandCenter/Features/Settings/SettingsView.swift"
      provides: "Settings tab placeholder"
      contains: "struct SettingsView"
  key_links:
    - from: "BusinessCommandCenterApp.swift"
      to: "ContentView.swift"
      via: "WindowGroup root view"
      pattern: "ContentView\\(\\)"
    - from: "ContentView.swift"
      to: "HomeView, ChatView, SettingsView"
      via: "TabView children"
      pattern: "tabItem"
---

<objective>
Create the Xcode project with SwiftUI app structure and tab-based navigation.

Purpose: Establish the iOS app skeleton that all subsequent features build upon. This is the foundation - if the project structure is wrong, everything else will be harder.

Output: Working Xcode project with three-tab navigation (Home, Chat, Settings) that builds and runs in simulator.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation-security/06-RESEARCH.md

Key patterns from research:
- Use SwiftUI TabView with tabItem modifier (iOS 17 compatible)
- Project structure: App/, Core/, Features/, Shared/, Resources/
- Minimum deployment target: iOS 17.0
- Use SF Symbols for tab icons: house, message, gear
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with SwiftUI</name>
  <files>
    BusinessCommandCenter.xcodeproj
    BusinessCommandCenter/App/BusinessCommandCenterApp.swift
  </files>
  <action>
Create a new iOS App project in Xcode via command line:

1. Create the project directory structure:
   - BusinessCommandCenter/ (root)
   - BusinessCommandCenter/App/
   - BusinessCommandCenter/Core/Security/
   - BusinessCommandCenter/Core/Utilities/
   - BusinessCommandCenter/Features/Home/
   - BusinessCommandCenter/Features/Chat/
   - BusinessCommandCenter/Features/Settings/
   - BusinessCommandCenter/Shared/Components/
   - BusinessCommandCenter/Resources/

2. Create the main app entry point (BusinessCommandCenterApp.swift):
```swift
import SwiftUI

@main
struct BusinessCommandCenterApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

3. Create Info.plist with:
   - Bundle name: Business Command Center
   - Bundle identifier: com.iaml.businesscommandcenter
   - Minimum deployment target: iOS 17.0
   - NSFaceIDUsageDescription: "Authenticate to access your Business Command Center"
   - UILaunchScreen with background color

4. Generate the .xcodeproj using swift package init or manual xcodeproj creation with proper build settings:
   - SWIFT_VERSION = 5.9
   - IPHONEOS_DEPLOYMENT_TARGET = 17.0
   - PRODUCT_BUNDLE_IDENTIFIER = com.iaml.businesscommandcenter
   - PRODUCT_NAME = BusinessCommandCenter

Note: If xcodebuild/xcodeproj tools are limited, create the project structure and Swift files - Xcode can generate the project file when opened.
  </action>
  <verify>
Directory structure exists with all folders. BusinessCommandCenterApp.swift compiles with `swiftc -parse` or opens without errors in Xcode.
  </verify>
  <done>
Xcode project directory exists with proper structure. Main app file contains @main entry point.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement TabView navigation with three tabs</name>
  <files>
    BusinessCommandCenter/App/ContentView.swift
    BusinessCommandCenter/Features/Home/HomeView.swift
    BusinessCommandCenter/Features/Chat/ChatView.swift
    BusinessCommandCenter/Features/Settings/SettingsView.swift
  </files>
  <action>
Create ContentView.swift with TabView:

```swift
import SwiftUI

struct ContentView: View {
    @State private var selectedTab = 0

    var body: some View {
        TabView(selection: $selectedTab) {
            HomeView()
                .tabItem {
                    Label("Home", systemImage: "house")
                }
                .tag(0)

            ChatView()
                .tabItem {
                    Label("Chat", systemImage: "message")
                }
                .tag(1)

            SettingsView()
                .tabItem {
                    Label("Settings", systemImage: "gear")
                }
                .tag(2)
        }
    }
}
```

Create placeholder views for each tab:

HomeView.swift:
```swift
import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Image(systemName: "chart.bar.fill")
                    .font(.system(size: 60))
                    .foregroundStyle(.secondary)
                Text("Dashboard")
                    .font(.title)
                Text("Health scores will appear here")
                    .foregroundStyle(.secondary)
            }
            .navigationTitle("Home")
        }
    }
}
```

ChatView.swift:
```swift
import SwiftUI

struct ChatView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Image(systemName: "bubble.left.and.bubble.right")
                    .font(.system(size: 60))
                    .foregroundStyle(.secondary)
                Text("AI Assistant")
                    .font(.title)
                Text("Chat interface will appear here")
                    .foregroundStyle(.secondary)
            }
            .navigationTitle("Chat")
        }
    }
}
```

SettingsView.swift:
```swift
import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            List {
                Section("Security") {
                    NavigationLink(destination: Text("API Key settings")) {
                        Label("API Key", systemImage: "key")
                    }
                }

                Section("About") {
                    HStack {
                        Text("Version")
                        Spacer()
                        Text("2.0.0")
                            .foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle("Settings")
        }
    }
}
```

Each view uses NavigationStack for proper navigation hierarchy within tabs.
  </action>
  <verify>
All four Swift files exist and compile. Visual inspection shows TabView with three tabItem modifiers.
  </verify>
  <done>
ContentView shows three-tab navigation. Home, Chat, and Settings tabs display placeholder content with navigation titles.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure project for iOS 17 and verify build</name>
  <files>
    BusinessCommandCenter/Info.plist
    BusinessCommandCenter.xcodeproj/project.pbxproj
  </files>
  <action>
Ensure Info.plist contains required keys:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>Business Command Center</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleShortVersionString</key>
    <string>2.0.0</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string>LaunchBackground</string>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>NSFaceIDUsageDescription</key>
    <string>Authenticate to access your Business Command Center</string>
</dict>
</plist>
```

Create Assets.xcassets with:
- AppIcon (placeholder or empty iconset)
- AccentColor (system blue)
- LaunchBackground color (for light/dark)

Verify project builds:
- If xcodeproj exists: `xcodebuild -project BusinessCommandCenter.xcodeproj -scheme BusinessCommandCenter -destination 'platform=iOS Simulator,name=iPhone 15' build`
- If not: Open in Xcode and let it generate project, then build

Note: Xcode project file generation may require manual Xcode interaction. Document the files created and verify Swift files have no syntax errors.
  </action>
  <verify>
`swiftc -parse` passes on all .swift files, or project builds in Xcode without errors. Info.plist contains NSFaceIDUsageDescription.
  </verify>
  <done>
Project configured for iOS 17 with Face ID usage description. All Swift files compile without errors.
  </done>
</task>

</tasks>

<verification>
1. Project structure matches recommended layout (App/, Core/, Features/, Shared/, Resources/)
2. All Swift files parse without syntax errors
3. Info.plist includes NSFaceIDUsageDescription
4. TabView has three tabs with correct icons (house, message, gear)
5. Each tab view has NavigationStack with title
</verification>

<success_criteria>
- IOS-01 PARTIAL: Tab bar exists with Home, Chat, Settings tabs (navigation works)
- Project structure supports MVVM architecture for future features
- Face ID permission string ready for AUTH-01
- App builds for iOS 17+ simulator
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-security/06-01-SUMMARY.md`
</output>

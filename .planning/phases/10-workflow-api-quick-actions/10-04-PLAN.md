---
phase: 10-workflow-api-quick-actions
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - dashboard/src/lib/api/mobile-chat.ts
  - BusinessCommandCenter/Core/Network/NetworkManager.swift
autonomous: true

must_haves:
  truths:
    - "Chat tool trigger_workflow executes real webhook triggers"
    - "Chat tool query_workflows returns real workflow list"
    - "iOS NetworkManager can fetch workflows and trigger them"
  artifacts:
    - path: "dashboard/src/lib/api/mobile-chat.ts"
      provides: "Updated chat tools with real implementations"
      contains: "triggerWorkflow"
    - path: "BusinessCommandCenter/Core/Network/NetworkManager.swift"
      provides: "Workflow network methods"
      exports: ["fetchWorkflows", "triggerWorkflow"]
  key_links:
    - from: "mobile-chat.ts executeTriggerWorkflow"
      to: "workflow-triggers.ts triggerWorkflow"
      via: "function import"
      pattern: "import.*triggerWorkflow"
    - from: "NetworkManager.swift"
      to: "/api/mobile/workflows"
      via: "HTTP requests"
      pattern: "appendingPathComponent.*workflows"
---

<objective>
Replace chat tool placeholders with real implementations and add workflow methods to iOS NetworkManager.

Purpose: Completes the API integration so both chat-based and button-based workflow triggers work.
Output: Working chat tools and iOS network methods.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-workflow-api-quick-actions/10-CONTEXT.md
@.planning/phases/10-workflow-api-quick-actions/10-RESEARCH.md
@.planning/phases/08-chat-api/08-03-SUMMARY.md
@dashboard/src/lib/api/mobile-chat.ts
@BusinessCommandCenter/Core/Network/NetworkManager.swift
@BusinessCommandCenter/Core/Models/QuickActionModels.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chat tools with real workflow implementations</name>
  <files>dashboard/src/lib/api/mobile-chat.ts</files>
  <action>
Update mobile-chat.ts to use real workflow triggers:

1. Add imports at top:
   ```typescript
   import { triggerWorkflow, getAvailableWorkflows, getWorkflowById } from './workflow-triggers';
   ```

2. Replace `executeTriggerWorkflow` implementation:
   - Look up workflow with getWorkflowById(input.workflow_id)
   - If not found or no webhook_url: return JSON error
   - Call triggerWorkflow(workflow_id, webhook_url, parameters)
   - Return JSON with status, workflow_name, execution_id

3. Replace `executeQueryWorkflows` implementation:
   - Call getAvailableWorkflows()
   - Apply filter if provided (all/active/failed/recent - for now, just return all)
   - Apply limit if provided (default 10)
   - Return JSON with workflows array

Keep existing error handling pattern (try/catch returning JSON error).
  </action>
  <verify>
TypeScript compiles: `cd dashboard && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>Chat tools use real workflow data instead of placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow methods to iOS NetworkManager</name>
  <files>BusinessCommandCenter/Core/Network/NetworkManager.swift</files>
  <action>
Add workflow methods to NetworkManager actor following fetchHealth pattern:

1. `fetchWorkflows(context: LAContext) async throws -> WorkflowListResponse`:
   - Get API key from Keychain
   - Build URL: baseURL + "/workflows"
   - Set X-API-Key header
   - Decode WorkflowListResponse
   - Same error handling as fetchHealth

2. `triggerWorkflow(workflowId: String, parameters: [String: Any]?, context: LAContext) async throws -> WorkflowTriggerResponse`:
   - Get API key from Keychain
   - Build URL: baseURL + "/workflows/trigger"
   - Set X-API-Key header
   - Set Content-Type: application/json
   - Encode request body: { "workflow_id": workflowId, "parameters": parameters }
   - Use JSONSerialization for parameters (Any type, not Codable)
   - Decode WorkflowTriggerResponse
   - Same error handling as fetchHealth

Use existing patterns:
- Actor isolation
- LAContext passthrough
- NetworkError mapping
- JSONDecoder for responses
  </action>
  <verify>
Build check: `xcodebuild -project BusinessCommandCenter.xcodeproj -scheme BusinessCommandCenter -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(error:|BUILD)' | tail -10`
  </verify>
  <done>NetworkManager has fetchWorkflows and triggerWorkflow methods</done>
</task>

</tasks>

<verification>
- [ ] mobile-chat.ts imports from workflow-triggers.ts
- [ ] executeTriggerWorkflow calls real triggerWorkflow function
- [ ] executeQueryWorkflows calls real getAvailableWorkflows function
- [ ] NetworkManager.fetchWorkflows returns WorkflowListResponse
- [ ] NetworkManager.triggerWorkflow returns WorkflowTriggerResponse
- [ ] Both projects build without errors
</verification>

<success_criteria>
Both chat-based and direct API workflow triggers work with real n8n webhooks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-api-quick-actions/10-04-SUMMARY.md`
</output>

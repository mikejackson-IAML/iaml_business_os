---
phase: 10-workflow-api-quick-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260121_add_quick_action_columns.sql
  - BusinessCommandCenter/Core/Models/QuickActionModels.swift
autonomous: true

must_haves:
  truths:
    - "Workflow registry has columns for webhook_url, icon, and risk_level"
    - "iOS can decode quick action data from API responses"
  artifacts:
    - path: "supabase/migrations/20260121_add_quick_action_columns.sql"
      provides: "Schema migration for quick action support"
      contains: "ALTER TABLE n8n_brain.workflow_registry"
    - path: "BusinessCommandCenter/Core/Models/QuickActionModels.swift"
      provides: "Codable models for quick actions"
      exports: ["QuickAction", "RiskLevel", "WorkflowListResponse", "WorkflowTriggerResponse"]
  key_links: []
---

<objective>
Add database columns for quick action support and create iOS Codable models for workflow data.

Purpose: Establish the data foundation that both the API endpoints (Plan 02) and iOS networking layer (Plan 04) will use.
Output: Database migration script and Swift model files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-workflow-api-quick-actions/10-CONTEXT.md
@.planning/phases/10-workflow-api-quick-actions/10-RESEARCH.md
@supabase/migrations/20260111_create_n8n_brain_schema.sql
@BusinessCommandCenter/Core/Models/HealthModels.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for quick action columns</name>
  <files>supabase/migrations/20260121_add_quick_action_columns.sql</files>
  <action>
Create SQL migration that adds columns to n8n_brain.workflow_registry:

1. Add `webhook_url TEXT` - The n8n webhook URL for triggering this workflow
2. Add `quick_action_icon TEXT DEFAULT 'bolt.fill'` - SF Symbol name for iOS display
3. Add `risk_level TEXT DEFAULT 'safe' CHECK (risk_level IN ('safe', 'risky', 'destructive'))` - Determines if confirmation is required
4. Add `quick_action_enabled BOOLEAN DEFAULT false` - Whether to show in quick actions grid

Add comment explaining each column's purpose.
  </action>
  <verify>
Read the migration file and confirm it has:
- ALTER TABLE statement for n8n_brain.workflow_registry
- All 4 columns with correct types and defaults
- CHECK constraint on risk_level
  </verify>
  <done>Migration file exists with all required columns and constraints</done>
</task>

<task type="auto">
  <name>Task 2: Create iOS Codable models for quick actions</name>
  <files>BusinessCommandCenter/Core/Models/QuickActionModels.swift</files>
  <action>
Create Swift models following the HealthModels.swift pattern:

1. `RiskLevel` enum (String, Codable): `safe`, `risky`, `destructive`

2. `QuickAction` struct (Codable, Identifiable):
   - id: String (workflow_id)
   - name: String
   - description: String?
   - icon: String (SF Symbol name)
   - riskLevel: RiskLevel
   - category: String?
   - canTrigger: Bool (has webhook_url)

3. `WorkflowListResponse` struct (Codable):
   - workflows: [QuickAction]

4. `WorkflowTriggerResponse` struct (Codable):
   - success: Bool
   - executionId: String? (optional, webhook may not return it)
   - message: String

Use CodingKeys for snake_case API mapping (risk_level -> riskLevel, can_trigger -> canTrigger).
  </action>
  <verify>
Build the iOS project: `xcodebuild -project BusinessCommandCenter.xcodeproj -scheme BusinessCommandCenter -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`
  </verify>
  <done>Models compile without errors and match API response format</done>
</task>

</tasks>

<verification>
- [ ] Migration file parses as valid SQL
- [ ] iOS project builds with new models
- [ ] RiskLevel enum has all 3 cases
- [ ] QuickAction is Identifiable (can be used in ForEach)
</verification>

<success_criteria>
Database schema extended with quick action columns. iOS models ready for networking layer.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-api-quick-actions/10-01-SUMMARY.md`
</output>

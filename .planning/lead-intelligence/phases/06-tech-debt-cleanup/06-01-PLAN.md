---
phase: 06-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/company-tab.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/contacts-tab.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-skeleton.tsx
  - dashboard/src/app/dashboard/lead-intelligence/companies/[id]/company-profile-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/notes-tab.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-detail.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-notes-section.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/attachment-upload.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-contacts-section.tsx
autonomous: true

must_haves:
  truths:
    - "VIP toggle marks contact as VIP via API and shows toast feedback"
    - "DNC toggle sets contact status to do_not_contact via API and shows toast feedback"
    - "Link Company and Add Contact stubs are removed (no 'coming soon' text)"
    - "Breadcrumb 'Contacts' link navigates to /dashboard/lead-intelligence (not /contacts 404)"
    - "Opportunities page uses Suspense + skeleton pattern like all other pages"
    - "No x-api-key: 'internal' headers remain in client-side fetches"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-skeleton.tsx"
      provides: "Skeleton loading state for opportunities page"
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx"
      provides: "Working VIP/DNC toggles"
  key_links:
    - from: "contact-row-actions.tsx VIP onClick"
      to: "PUT /api/lead-intelligence/contacts/:id"
      via: "fetch with { is_vip: true }"
      pattern: "fetch.*contacts.*is_vip"
    - from: "contact-row-actions.tsx DNC onClick"
      to: "PUT /api/lead-intelligence/contacts/:id"
      via: "fetch with { status: 'do_not_contact' }"
      pattern: "fetch.*contacts.*do_not_contact"
---

<objective>
Clean up 5 low-severity tech debt items from the v1.0 milestone audit.

Purpose: Polish the shipped v1.0 by removing stubs, fixing broken links, and aligning patterns.
Output: All 5 audit items resolved. Zero console.log stubs, zero "coming soon" text, zero 404 breadcrumbs, consistent loading patterns, no redundant headers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/milestones/v1.0-MILESTONE-AUDIT.md
@dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
@dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx
@dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/company-tab.tsx
@dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/contacts-tab.tsx
@dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx
@dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx
@dashboard/src/app/dashboard/lead-intelligence/page.tsx (reference: Suspense/skeleton pattern)
@dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-skeleton.tsx (reference: skeleton example)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire VIP/DNC toggles, fix stubs, fix breadcrumbs</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/company-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/contacts-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx
  </files>
  <action>
    **contact-row-actions.tsx (lines 122-141):** Replace the two console.log TODO stubs with real API calls:

    - "Mark as VIP" onClick: call `PUT /api/lead-intelligence/contacts/${contact.id}` with body `{ is_vip: !contact.is_vip }`. Use `toast.promise()` pattern (already used for Enrich on line 93). Toggle label between "Mark as VIP" / "Remove VIP" based on `contact.is_vip`. Call `onContactsChanged?.()` on success to refresh the list.

    - "Mark Do Not Contact" onClick: call `PUT /api/lead-intelligence/contacts/${contact.id}` with body `{ status: 'do_not_contact' }`. Use `toast.promise()`. Show confirmation before executing (use `window.confirm('Mark as Do Not Contact? This will change their status.')`). Call `onContactsChanged?.()` on success.

    The PUT endpoint already exists at `dashboard/src/app/api/lead-intelligence/contacts/[id]/route.ts` and the validation at `lead-intelligence-contacts-validation.ts` already accepts `is_vip` (boolean) and `status` including 'do_not_contact'.

    **company-tab.tsx (lines 82-87):** Remove the "Link Company (coming soon)" disabled button entirely. Keep just the "No company associated" message with the Building2 icon.

    Also remove the "Opportunities coming in Phase 5" placeholder card at lines 191-198 — Phase 5 is shipped, opportunities are accessed via the company profile page's OpportunitiesTab, not here.

    **contacts-tab.tsx:** Remove both "Add Contact (Coming soon)" disabled buttons:
    - Line 53-55: the button in the empty state — leave just the "No contacts at this company" message
    - Line 68-69: the button in the header actions — remove it entirely
    - Line 65-66: Also remove the "Bulk actions coming in Phase 4" disabled button — Phase 4 shipped

    **contact-profile-content.tsx (line 45):** Change breadcrumb href from `/dashboard/lead-intelligence/contacts` to `/dashboard/lead-intelligence`. There is no /contacts page — the contact list IS the /lead-intelligence page.
  </action>
  <verify>
    Run `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx next build 2>&1 | tail -20` to confirm build passes.
    Grep for "coming soon" (case-insensitive) in the lead-intelligence directory — should return zero results.
    Grep for "console.log.*VIP\|console.log.*DNC\|console.log.*Mark" — should return zero results.
    Grep for "lead-intelligence/contacts'" (the 404 breadcrumb path) — should return zero results.
  </verify>
  <done>
    VIP toggle calls PUT with is_vip, DNC toggle calls PUT with status change, all "coming soon" stubs removed, breadcrumb links to /dashboard/lead-intelligence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suspense/skeleton for opportunities page + remove x-api-key headers</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-skeleton.tsx
    dashboard/src/app/dashboard/lead-intelligence/companies/[id]/company-profile-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/notes-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/companies/[id]/tabs/contacts-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-detail.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-notes-section.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/attachment-upload.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-contacts-section.tsx
  </files>
  <action>
    **Opportunities Suspense pattern:** Follow the exact pattern from `page.tsx` (the main lead-intelligence page):

    1. Create `opportunities-skeleton.tsx`: Export `OpportunitiesSkeleton` component. Render the page header skeleton (same p-6 lg:p-8 layout), a skeleton row for tabs, and a grid of 5 skeleton cards (representing kanban columns) using `@/dashboard-kit/components/ui/skeleton`. Reference `lead-intelligence-skeleton.tsx` for style consistency.

    2. Update `opportunities/page.tsx`: Import Suspense from react. Import OpportunitiesSkeleton. Wrap `<OpportunitiesContent />` in `<Suspense fallback={<OpportunitiesSkeleton />}>`. Note: OpportunitiesContent is a client component that fetches its own data, so the Suspense won't actually stream — but this matches the codebase pattern and replaces the inline spinner. The spinner div (lines 104-106 of opportunities-content.tsx) should be replaced with a proper skeleton inline as well: replace the `{loading ? (<div className="flex items-center...` spinner with a reuse of a simpler skeleton grid (import Skeleton, render 5 skeleton cards in a flex row matching kanban layout).

    **Remove x-api-key headers:** In ALL of these files, remove the `'x-api-key': 'internal'` header from every fetch call. The API routes no longer validate this header (it was removed during Phase 2 and Phase 5 verification). If removing it leaves an empty headers object like `headers: { }` or `headers: { 'Content-Type': 'application/json' }`, keep the Content-Type where it exists (for POST/PUT/PATCH calls with JSON body) but remove empty headers objects entirely.

    Files with x-api-key to clean (13 occurrences total):
    - `companies/[id]/company-profile-content.tsx` (1 occurrence, line 36)
    - `companies/[id]/tabs/notes-tab.tsx` (2 occurrences, lines 27, 52)
    - `companies/[id]/tabs/contacts-tab.tsx` (1 occurrence, line 29)
    - `opportunities/components/opportunity-detail.tsx` (4 occurrences, lines 90, 104, 134, 165)
    - `opportunities/components/opportunity-notes-section.tsx` (1 occurrence, line 26)
    - `opportunities/components/attachment-upload.tsx` (2 occurrences, lines 60, 94)
    - `opportunities/components/opportunity-contacts-section.tsx` (2 occurrences, lines 69, 94)
  </action>
  <verify>
    Run `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx next build 2>&1 | tail -20` to confirm build passes.
    Grep for "x-api-key" in the lead-intelligence directory — should return zero results.
    Grep for "animate-spin.*border-b-2.*border-primary" in opportunities directory — should return zero results (spinner replaced with skeleton).
    Confirm `opportunities-skeleton.tsx` exists.
  </verify>
  <done>
    Opportunities page wrapped in Suspense with skeleton fallback, inline spinner replaced with skeleton, all 13 x-api-key: 'internal' headers removed from client-side fetches.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx next build` passes clean
2. `grep -ri "coming soon" dashboard/src/app/dashboard/lead-intelligence/` returns nothing
3. `grep -r "x-api-key" dashboard/src/app/dashboard/lead-intelligence/` returns nothing
4. `grep -r "console.log.*Mark\|console.log.*VIP\|console.log.*DNC" dashboard/src/app/dashboard/lead-intelligence/` returns nothing
5. `grep -r "lead-intelligence/contacts'" dashboard/src/app/dashboard/lead-intelligence/` returns nothing (the href breadcrumb, not the API path)
6. File `opportunities-skeleton.tsx` exists
</verification>

<success_criteria>
All 5 tech debt items from v1.0 audit resolved:
1. VIP/DNC toggles call PUT API with toast feedback
2. "Coming soon" stubs removed from company-tab and contacts-tab
3. Breadcrumb links to /dashboard/lead-intelligence (not /contacts)
4. Opportunities page uses Suspense + skeleton pattern
5. Zero x-api-key: 'internal' headers in client fetches
Build passes. No regressions.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/06-tech-debt-cleanup/06-01-SUMMARY.md`
</output>

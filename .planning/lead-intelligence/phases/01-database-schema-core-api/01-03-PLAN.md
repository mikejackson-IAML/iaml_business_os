---
phase: 01-database-schema-core-api
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - dashboard/src/lib/api/lead-intelligence-companies-types.ts
  - dashboard/src/lib/api/lead-intelligence-companies-validation.ts
  - dashboard/src/lib/api/lead-intelligence-companies-queries.ts
  - dashboard/src/lib/api/lead-intelligence-companies-mutations.ts
  - dashboard/src/app/api/lead-intelligence/companies/route.ts
  - dashboard/src/app/api/lead-intelligence/companies/[id]/route.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/lead-intelligence/companies returns paginated results with sorting"
    - "POST /api/lead-intelligence/companies creates a company and returns 201"
    - "GET /api/lead-intelligence/companies/[id] returns a single company"
    - "PUT /api/lead-intelligence/companies/[id] updates a company"
    - "DELETE /api/lead-intelligence/companies/[id] deletes a company"
  artifacts:
    - path: "dashboard/src/app/api/lead-intelligence/companies/route.ts"
      provides: "GET list + POST create"
      exports: ["GET", "POST"]
    - path: "dashboard/src/app/api/lead-intelligence/companies/[id]/route.ts"
      provides: "GET detail + PUT update + DELETE"
      exports: ["GET", "PUT", "DELETE"]
    - path: "dashboard/src/lib/api/lead-intelligence-companies-types.ts"
      provides: "Company, CreateCompanyInput, UpdateCompanyInput interfaces"
  key_links:
    - from: "companies/route.ts"
      to: "lead-intelligence-companies-queries.ts"
      via: "import getCompanies"
      pattern: "import.*getCompanies"
---

<objective>
Create complete CRUD API for companies with pagination, sorting, validation, and structured errors.

Purpose: Companies API supports company profiles and is the second core entity in Lead Intelligence.
Output: 6 files — types, validation, queries, mutations, list route, detail route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/01-database-schema-core-api/01-RESEARCH.md
@dashboard/src/lib/api/lead-intelligence-contacts-types.ts
@dashboard/src/lib/api/lead-intelligence-contacts-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create companies types and validation</name>
  <files>
    dashboard/src/lib/api/lead-intelligence-companies-types.ts
    dashboard/src/lib/api/lead-intelligence-companies-validation.ts
  </files>
  <action>
**Types file** — Define interfaces:
- `Company` — all columns from DB schema: id, name, website, industry, employee_count, revenue_range, city, state, country, linkedin_url, enrichment_source, enrichment_data (Record<string, unknown>), enriched_at, created_at, updated_at.
- `CreateCompanyInput` — required: name. Optional: all other fields except id/timestamps.
- `UpdateCompanyInput` — Partial of CreateCompanyInput.
- `CompanyListParams` — page, limit, sort, order (same pattern as contacts).
- `CompanyListResponse` — data: Company[], meta: { page, limit, total, total_pages }.

**Validation file** — Functions:
- `validateCreateCompany(body: unknown)` — validates name required and non-empty, website is valid URL if provided, employee_count is positive integer if provided.
- `validateUpdateCompany(body: unknown)` — same rules, all optional.
- Reuse `createValidationError` pattern from contacts validation (or import shared utility).

Mirror the contacts validation pattern exactly.
  </action>
  <verify>Types compile: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit --pretty 2>&1 | head -20`</verify>
  <done>Company types and validation functions defined and compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create companies queries, mutations, and API routes</name>
  <files>
    dashboard/src/lib/api/lead-intelligence-companies-queries.ts
    dashboard/src/lib/api/lead-intelligence-companies-mutations.ts
    dashboard/src/app/api/lead-intelligence/companies/route.ts
    dashboard/src/app/api/lead-intelligence/companies/[id]/route.ts
  </files>
  <action>
**Queries file:**
- `getCompanies(params: CompanyListParams)` — paginated select with count, order by sort/order. No joins needed for list.
- `getCompanyById(id: string)` — single select, returns Company or null.

**Mutations file:**
- `createCompany(input: CreateCompanyInput)` — insert + select + single.
- `updateCompany(id: string, input: UpdateCompanyInput)` — update + select + single.
- `deleteCompany(id: string)` — delete, return boolean.

**List route (companies/route.ts):**
- GET: paginated list. POST: create with validation.
- validateApiKey, try/catch, structured errors — identical pattern to contacts route.

**Detail route (companies/[id]/route.ts):**
- GET: by id, 404 if not found. PUT: validate + update. DELETE: 204.
- Async params pattern: `const { id } = await context.params;`

Mirror contacts routes exactly — same auth, error handling, response structure.
  </action>
  <verify>
Test with curl:
```
curl -s http://localhost:3000/api/lead-intelligence/companies | jq '.meta'
curl -s -X POST http://localhost:3000/api/lead-intelligence/companies -H 'Content-Type: application/json' -d '{"name":"Acme Corp"}' | jq '.'
```
  </verify>
  <done>GET /api/lead-intelligence/companies returns paginated response. POST creates company with 201. GET/PUT/DELETE on [id] work. Invalid input returns 400.</done>
</task>

</tasks>

<verification>
- All 6 files compile without TypeScript errors
- GET companies returns paginated JSON with meta
- POST companies with valid body returns 201
- POST companies with missing name returns 400
- GET/PUT/DELETE on companies/[id] work correctly
</verification>

<success_criteria>
Complete companies CRUD API operational, matching the contacts API pattern exactly.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/01-database-schema-core-api/01-03-SUMMARY.md`
</output>

---
phase: 02-contact-list-profiles-company-pages
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/page.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-skeleton.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/attendance-tab.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/email-campaigns-tab.tsx
autonomous: true

must_haves:
  truths:
    - "Contact profile page loads at /dashboard/lead-intelligence/contacts/[id]"
    - "Profile header shows contact info, avatar, status badges, and breadcrumbs"
    - "Overview tab shows key stats, recent activity, and upcoming follow-ups"
    - "Attendance tab shows sortable table of program attendance"
    - "Email & Campaigns tab shows email activity timeline and engagement metrics"
    - "Tabs lazy-load data on first activation"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/page.tsx"
      provides: "Dynamic route page with async params"
      min_lines: 15
    - path: "dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx"
      provides: "Tab container with lazy loading"
      min_lines: 60
    - path: "dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx"
      provides: "Overview tab with stats, activity, follow-ups"
      min_lines: 50
  key_links:
    - from: "contact-profile-content.tsx"
      to: "/api/lead-intelligence/contacts/[id]/*"
      via: "fetch per-tab on activation"
      pattern: "fetch.*api/lead-intelligence/contacts/"
    - from: "page.tsx"
      to: "getContactById"
      via: "server-side data load"
      pattern: "getContactById"
---

<objective>
Build the contact profile page with header and the first 3 tabs: Overview, Attendance, and Email & Campaigns.

Purpose: The contact profile is the core detail view — where users understand a contact's full relationship with IAML. Splitting into 2 plans (04 + 05) keeps each within context budget.
Output: Contact profile page with header, tab navigation, and 3 of 6 tabs functional (PROF-01, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-RESEARCH.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-CONTEXT.md

@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-01-SUMMARY.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-02-SUMMARY.md

@dashboard/src/dashboard-kit/components/ui/tabs.tsx
@dashboard/src/lib/api/lead-intelligence-contacts-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contact profile page, header, and tab container</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/page.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/contact-profile-skeleton.tsx
  </files>
  <action>
    1. **page.tsx** — Server component. Async params (Next.js 16 pattern: `params: Promise<{ id: string }>`). Await params, call getContactById(id). If !contact, call notFound(). Wrap ContactProfileContent in Suspense with ContactProfileSkeleton fallback.

    2. **contact-profile-content.tsx** — 'use client'. Props: { contact: Contact }. Layout:
       - Breadcrumbs: Lead Intelligence > Contacts > {first_name} {last_name}
       - Profile header section:
         - Large ContactAvatar (size='lg')
         - Name (h1), title, company (linked to company profile)
         - StatusBadge(s) for status + VIP
         - Contact info: email, phone, LinkedIn URL, location (city, state)
         - Quick action buttons: Edit (stub), Add Note (scrolls to notes tab), Set Follow-up (stub)
       - Tab bar using Radix Tabs: Overview, Attendance, Email & Campaigns, Company, Notes, Enrichment Data
       - Tab content area: render active tab component
       - Lazy loading: useState for activeTab (default 'overview'). Each tab component receives contactId and fetches its own data on mount. Use a pattern where each tab has a loading state internally.

    3. **contact-profile-skeleton.tsx** — Skeleton for header + tab placeholder.
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes.</verify>
  <done>Contact profile page renders with header, breadcrumbs, avatar, badges, and 6-tab navigation. Tab switching works.</done>
</task>

<task type="auto">
  <name>Task 2: Create Overview, Attendance, and Email & Campaigns tabs</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/attendance-tab.tsx
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/email-campaigns-tab.tsx
  </files>
  <action>
    1. **overview-tab.tsx** — 'use client'. Props: { contactId: string; contact: Contact }. Fetches activity and follow-ups from API on mount. Layout:
       - Key stats grid (4 cards): Programs Attended (count from contact.programs_attended or fetch), Avg Rating (from contact fields or compute), Last Attended (date), Engagement Score (from contact.engagement_score). Use MetricCard or simple stat cards.
       - Recent Activity timeline (last 10): fetch from /api/lead-intelligence/contacts/{id}/activity. Render as vertical timeline with icon per activity_type, description, and relative timestamp.
       - Upcoming Follow-ups: fetch from /api/lead-intelligence/contacts/{id}/follow-ups. Show as cards with due date, description, and "Complete" button (stub — marks complete in future phase). If none, show "No upcoming follow-ups" with "Create Follow-up" button (stub).

    2. **attendance-tab.tsx** — 'use client'. Props: { contactId: string }. Fetches from /api/lead-intelligence/contacts/{id}/attendance on mount. Renders sortable table (use DataTable or simple table):
       - Columns: Program Name, Event Date, Rating (stars or number), Satisfaction Score, Feedback (truncated with expand), Status
       - Sort by event_date desc default. Allow sort by rating, date.
       - Empty state: "No attendance records found"

    3. **email-campaigns-tab.tsx** — 'use client'. Props: { contactId: string }. Fetches from /api/lead-intelligence/contacts/{id}/email-activities on mount. Layout:
       - Engagement metrics summary at top: total emails, open rate, click rate, reply rate, bounce count (computed from activities data)
       - Email activity timeline below: chronological list of email events. Each shows: campaign_name, activity_type (sent/opened/clicked/replied/bounced) with icon, activity_date, subject line if available.
       - Empty state: "No email activity recorded"
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes. Navigate to a contact profile, each tab loads and displays data (or empty state).</verify>
  <done>Overview tab shows stats grid, activity timeline, and follow-ups. Attendance tab shows sortable table. Email tab shows engagement metrics and timeline. All lazy-load on activation.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- /dashboard/lead-intelligence/contacts/[id] renders profile with header
- Tab switching loads different content
- Overview shows stats, activity, follow-ups
- Attendance shows sortable table
- Email shows engagement metrics and timeline
</verification>

<success_criteria>
Contact profile page renders at dynamic route with header (PROF-01), overview tab with stats/activity/follow-ups (PROF-03, PROF-04, PROF-05), attendance table (PROF-06), and email timeline (PROF-07). All tabs lazy-load data.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-04-SUMMARY.md`
</output>

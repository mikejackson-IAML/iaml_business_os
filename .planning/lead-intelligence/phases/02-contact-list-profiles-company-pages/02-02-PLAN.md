---
phase: 02-contact-list-profiles-company-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-avatar.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/breadcrumbs.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/status-badge.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/metrics-bar.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/data-health-section.tsx
autonomous: true

must_haves:
  truths:
    - "Avatar component renders profile image with initials fallback on error"
    - "Status badges show correct colors (VIP gold, Customer green, Lead blue)"
    - "Metrics bar displays total contacts, customers, companies, data quality score"
    - "Data health section is collapsible and each metric links to filtered contact list"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/contact-avatar.tsx"
      provides: "3-tier avatar (Supabase, LinkedIn CDN, initials)"
      min_lines: 20
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/metrics-bar.tsx"
      provides: "Top metrics bar component"
      min_lines: 25
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/data-health-section.tsx"
      provides: "Collapsible data health with metric links"
      min_lines: 40
  key_links:
    - from: "data-health-section.tsx"
      to: "URL search params"
      via: "router.push with filter params"
      pattern: "router\\.push.*\\?"
---

<objective>
Build the shared UI components used across the contact list, contact profile, and company profile pages.

Purpose: These components are shared across plans 03-06. Building them first in a parallel wave avoids duplication and ensures consistency.
Output: Avatar, breadcrumbs, status badge, metrics bar, and data health section components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-RESEARCH.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-CONTEXT.md

@dashboard/src/dashboard-kit/components/ui/badge.tsx
@dashboard/src/dashboard-kit/components/dashboard/metric-card.tsx
@dashboard/src/dashboard-kit/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create avatar, breadcrumbs, and status badge components</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/contact-avatar.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/breadcrumbs.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/status-badge.tsx
  </files>
  <action>
    1. **contact-avatar.tsx** — Client component ('use client'). Props: { contact: { first_name?: string; last_name?: string; profile_image_url?: string | null; status?: string }; size?: 'sm' | 'md' | 'lg' }. Sizes: sm=8(32px), md=10(40px), lg=16(64px). Logic:
       - If profile_image_url exists and no error: render img with rounded-full, onError sets error state -> show initials
       - Initials fallback: colored circle (generate color from name string hash), white text, uppercase initials
       - Use useState for imgError

    2. **breadcrumbs.tsx** — Server component. Props: { items: { label: string; href?: string }[] }. Render nav with flex items-center gap-2 text-sm text-muted-foreground. ChevronRight (Lucide) between items. Last item (no href) in text-foreground. Use Next.js Link for href items.

    3. **status-badge.tsx** — Props: { status: string; isVip?: boolean }. Render Badge components from dashboard-kit. Color mapping: 'customer' -> green (bg-green-100 text-green-800), 'lead' -> blue (bg-blue-100 text-blue-800), 'prospect' -> purple, 'inactive' -> gray. If isVip, also render a gold 'VIP' badge (bg-amber-100 text-amber-800). Use the existing Badge component with variant="outline" and add className for colors.
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes.</verify>
  <done>Three shared components created. Avatar handles image error fallback. Status badges show correct colors. Breadcrumbs render with links.</done>
</task>

<task type="auto">
  <name>Task 2: Create metrics bar and data health section components</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/metrics-bar.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/data-health-section.tsx
  </files>
  <action>
    1. **metrics-bar.tsx** — Client component. Props: { metrics: { total_contacts: number; total_customers: number; total_companies: number; data_quality_score: number } }. Render 4 MetricCard components (from dashboard-kit) in a grid (grid-cols-4 on lg, grid-cols-2 on sm). Icons from Lucide: Users, UserCheck, Building2, ShieldCheck. Data quality score as percentage with color coding (green >80%, yellow 50-80%, red <50%).

    2. **data-health-section.tsx** — Client component ('use client'). Props: { dataHealth: { email_valid_pct: number; email_invalid_count: number; stale_count: number; missing_email_count: number; missing_phone_count: number; missing_title_count: number; last_calculated: string } }.
       - Collapsible section using a button toggle (ChevronDown/ChevronUp icon). Default collapsed.
       - Inside: grid of metric items. Each item shows label, value (count or percentage), and a small progress bar or badge.
       - Each metric is a clickable link that navigates using router.push with URL search params to filter the contact list. Examples:
         - "Invalid emails" -> `?email_status=invalid`
         - "Stale contacts" -> `?created_before=YYYY-MM-DD` (6 months ago)
         - "Missing email" -> `?email_status=missing`
         - "Missing title" -> `?title=_missing_` (special value the filter handler interprets as IS NULL)
       - Show "Last calculated: {formatDate(last_calculated)}" at bottom
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes.</verify>
  <done>Metrics bar shows 4 cards. Data health section collapses/expands. Each metric links to filtered view via URL params.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- All 5 components exist and export correctly
</verification>

<success_criteria>
All shared components are created and TypeScript-valid. Avatar handles 3-tier fallback. Badges use correct colors. Data health links generate correct URL params.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-02-SUMMARY.md`
</output>

---
phase: 02-contact-list-profiles-company-pages
plan: 07
type: execute
wave: 3
depends_on: ["02-03", "02-04", "02-05", "02-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 2 pages render without errors"
    - "Navigation between list, contact profile, and company profile works"
    - "Filters, tabs, and data health links function correctly"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 2 pages.

Purpose: Confirm the complete read experience works end-to-end before moving to Phase 3.
Output: Verified Phase 2 or list of issues to fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and smoke test</name>
  <files></files>
  <action>
    Run `cd dashboard && npm run build` to verify no build errors.
    Then start dev server and verify these URLs return 200:
    - /dashboard/lead-intelligence (contact list)
    - /api/lead-intelligence/data-health (data health endpoint)
    - /api/lead-intelligence/contacts (contacts with default params)
    Fix any build errors found.
  </action>
  <verify>`npm run build` completes without errors.</verify>
  <done>Build passes. All key pages/endpoints respond.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Lead Intelligence read experience: contact list with metrics/filters/data health, contact profiles with 6 tabs, company profiles with 3 tabs.</what-built>
  <how-to-verify>
    1. Navigate to /dashboard/lead-intelligence — verify metrics bar, data health section, and contact table render
    2. Click "Advanced Filters" — verify filter panel opens with all filter types
    3. Click a data health metric — verify it filters the table
    4. Click a contact name — verify profile page loads with header and 6 tabs
    5. Click through each tab: Overview (stats + activity), Attendance (table), Email (timeline), Company (card + colleagues), Notes (add form + list), Enrichment (status + JSON)
    6. Navigate to a company profile — verify header, metrics, and 3 tabs
    7. On company Contacts tab — click a contact name to verify it links to contact profile
    8. Add a note on both contact and company profiles — verify it appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- All pages render without errors
- Navigation flow works: list -> contact profile -> company profile -> back
</verification>

<success_criteria>
Phase 2 success criteria met:
1. Contact list with metrics, data health, filters, and pagination
2. Contact profile with all 6 tabs
3. Company profile with all 3 tabs
4. Navigation and linking between all pages
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-07-SUMMARY.md`
</output>

---
phase: 02-contact-list-profiles-company-pages
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/page.tsx
  - dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-skeleton.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-table.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-filters.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
autonomous: true

must_haves:
  truths:
    - "User sees paginated contact table at /dashboard/lead-intelligence"
    - "Metrics bar shows above the table with 4 key metrics"
    - "Data health section is below metrics bar, collapsible"
    - "Advanced filters panel allows filtering by all types"
    - "Row actions menu appears on each contact row"
    - "Clicking a contact name navigates to contact profile"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/page.tsx"
      provides: "Main page with Suspense boundary"
      min_lines: 15
    - path: "dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx"
      provides: "Client content with table, filters, metrics"
      min_lines: 80
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/contact-filters.tsx"
      provides: "Advanced filter panel"
      min_lines: 60
  key_links:
    - from: "lead-intelligence-content.tsx"
      to: "/api/lead-intelligence/contacts"
      via: "fetch with URL search params"
      pattern: "fetch.*api/lead-intelligence/contacts"
    - from: "lead-intelligence-content.tsx"
      to: "/api/lead-intelligence/data-health"
      via: "fetch on mount"
      pattern: "fetch.*api/lead-intelligence/data-health"
    - from: "contact-table.tsx"
      to: "/dashboard/lead-intelligence/contacts/[id]"
      via: "Link on contact name"
      pattern: "lead-intelligence/contacts/"
---

<objective>
Build the main contact list page at /dashboard/lead-intelligence with paginated table, metrics bar, data health section, advanced filters, and row actions.

Purpose: This is the primary entry point for the Lead Intelligence module — the page users land on to browse and find contacts.
Output: Complete contact list page with all LIST requirements (LIST-01, LIST-04, LIST-06, LIST-07, LIST-08, LIST-09).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-RESEARCH.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-CONTEXT.md

@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-01-SUMMARY.md
@.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-02-SUMMARY.md

@dashboard/src/app/dashboard/leads/page.tsx
@dashboard/src/dashboard-kit/components/dashboard/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contact list page structure (page, content, skeleton)</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/page.tsx
    dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-skeleton.tsx
  </files>
  <action>
    1. **page.tsx** — Server component following existing leads/page.tsx pattern. Suspense boundary wrapping a DataLoader component. DataLoader fetches initial contacts (page 1, limit 25) and data health metrics via internal API calls (using fetch with absolute URL or direct query imports). Pass to LeadIntelligenceContent. Set `export const revalidate = 300;`.

    2. **lead-intelligence-content.tsx** — 'use client'. Props: { initialContacts, initialMetrics, initialDataHealth }. This is the main orchestrator:
       - Uses useSearchParams and useRouter for filter state (URL-param driven)
       - On mount and on search param change, fetches contacts from /api/lead-intelligence/contacts with current params
       - Layout (top to bottom):
         a. Page header: "Lead Intelligence" title with contact count
         b. MetricsBar component (from plan 02)
         c. DataHealthSection component (from plan 02), collapsible
         d. Filter bar with search input + "Advanced Filters" toggle button
         e. ContactFilters component (expanded/collapsed)
         f. ContactTable component with pagination
       - Pagination: track page/totalPages, prev/next buttons update URL params
       - Search: debounced text input updates `search` URL param

    3. **lead-intelligence-skeleton.tsx** — Loading skeleton matching the layout: 4 metric card skeletons + table skeleton rows.
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes. Navigate to /dashboard/lead-intelligence in browser shows the page structure.</verify>
  <done>Contact list page renders with metrics bar, data health section, and table structure. URL params drive filtering and pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Create contact table, filters, and row actions</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/contact-table.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/contact-filters.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
  </files>
  <action>
    1. **contact-table.tsx** — Client component. Props: { contacts: Contact[]; onSort: (field, order) => void; currentSort; currentOrder }. Use DataTable from dashboard-kit. Column definitions:
       - Name: ContactAvatar + first_name + last_name as Link to /dashboard/lead-intelligence/contacts/{id}. Comfortable row height.
       - Company: company name (from joined companies data), linked to /dashboard/lead-intelligence/companies/{company_id}
       - Title: job title text
       - Status: StatusBadge component (from plan 02)
       - Last Activity: formatted date using formatDate util
       - Actions: ContactRowActions component (three-dot menu)
       All columns sortable. Use existing DataTable column definition pattern.

    2. **contact-filters.tsx** — 'use client'. Props: { isOpen: boolean; currentFilters: Record<string, string> }. Render filter controls in a collapsible panel (hidden when !isOpen). Filter controls:
       - Status: select dropdown (customer, lead, prospect, inactive, do_not_contact)
       - State: text input (US state abbreviation)
       - Company: searchable text input (filters by company name — will need company_id lookup or ilike)
       - Title: text input (ilike search)
       - Department: select dropdown (populate from common values or free text)
       - Seniority: select dropdown (c_suite, vp, director, manager, senior, mid, junior, entry)
       - Company Size: select dropdown (1-10, 11-50, 51-200, 201-500, 500+)
       - Email Status: select dropdown (valid, invalid, catch_all, unknown, missing)
       - Date Range: two date inputs (created_after, created_before)
       - Engagement Score: min/max number inputs
       Each filter change calls router.push with updated URL params + resets page to 1.
       "Clear All Filters" button removes all filter params.
       Show active filter count on the "Advanced Filters" toggle button in parent.

    3. **contact-row-actions.tsx** — Client component. Props: { contact: Contact }. Radix DropdownMenu with trigger (MoreHorizontal icon button). Menu items:
       - View Profile -> Link to /dashboard/lead-intelligence/contacts/{id}
       - Add to Campaign -> disabled with tooltip "Coming in Phase 4"
       - Enrich Contact -> disabled with tooltip "Coming in Phase 4"
       - Set Follow-up -> disabled with tooltip "Coming in Phase 4"
       - Find Colleagues -> disabled with tooltip "Coming in Phase 4"
       - Separator
       - Mark as VIP -> TODO stub (console.log for now)
       - Mark Do Not Contact -> TODO stub (console.log for now)
       Phase 4 actions are intentionally stubbed — only "View Profile", "Mark VIP", and "Mark DNC" are functional in Phase 2.
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes. Contact table renders with all 6 columns. Filter panel expands/collapses. Row actions dropdown opens.</verify>
  <done>Contact table shows sortable columns with avatars and links. Advanced filters panel has all filter types. Row actions menu renders with appropriate items (some stubbed for future phases).</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx tsc --noEmit` passes
- /dashboard/lead-intelligence renders contact table with data
- Filters modify URL params and refetch data
- Data health metric links apply correct filters
- Row actions dropdown opens with menu items
</verification>

<success_criteria>
Complete contact list page with: metrics bar (LIST-06), data health section (LIST-07, LIST-08), paginated table (LIST-01), advanced filters (LIST-04), row actions (LIST-09). All URL-param driven for shareability and data health integration.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/02-contact-list-profiles-company-pages/02-03-SUMMARY.md`
</output>

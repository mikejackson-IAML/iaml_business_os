---
phase: 04-integrations-bulk-actions
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - dashboard/src/app/api/lead-intelligence/companies/[id]/find-colleagues/route.ts
  - dashboard/src/app/api/lead-intelligence/contacts/[id]/follow-up/route.ts
  - dashboard/src/app/api/lead-intelligence/bulk/follow-up/route.ts
  - dashboard/src/app/dashboard/lead-intelligence/components/find-colleagues-modal.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/follow-up-form.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/bulk-confirm-dialog.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx
autonomous: true

must_haves:
  truths:
    - "Find Colleagues triggers n8n webhook and shows results in modal"
    - "User can select and add colleagues from results"
    - "Existing CRM contacts are badged in results"
    - "Follow-up creates task in follow_up_tasks table"
    - "Bulk follow-up creates tasks for all selected contacts"
  artifacts:
    - path: "dashboard/src/app/api/lead-intelligence/companies/[id]/find-colleagues/route.ts"
      provides: "POST trigger n8n find-colleagues webhook"
      exports: ["POST"]
    - path: "dashboard/src/app/api/lead-intelligence/contacts/[id]/follow-up/route.ts"
      provides: "POST create follow-up task"
      exports: ["POST"]
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/find-colleagues-modal.tsx"
      provides: "Results modal with checkboxes and Add Selected"
  key_links:
    - from: "find-colleagues-modal.tsx"
      to: "/api/lead-intelligence/companies/[id]/find-colleagues"
      via: "fetch POST"
      pattern: "fetch.*find-colleagues"
    - from: "follow-up-form.tsx"
      to: "/api/lead-intelligence/contacts/[id]/follow-up"
      via: "fetch POST"
      pattern: "fetch.*follow-up"
---

<objective>
Implement Find Colleagues (n8n webhook + results modal) and Follow-up Tasks (create + bulk), completing all remaining Phase 4 integrations.

Purpose: Find Colleagues discovers new contacts at a company; Follow-ups ensure action items are tracked.
Output: 3 API routes, 3 UI components, row action + bulk action wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-RESEARCH.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-01-SUMMARY.md

@dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
@dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find Colleagues API and modal</name>
  <files>
    dashboard/src/app/api/lead-intelligence/companies/[id]/find-colleagues/route.ts
    dashboard/src/app/dashboard/lead-intelligence/components/find-colleagues-modal.tsx
  </files>
  <action>
    Create find-colleagues/route.ts (POST):
    - validateApiKey
    - Fetch company from Supabase by ID to get company_name and domain
    - POST to `${process.env.N8N_WEBHOOK_BASE_URL || 'https://n8n.realtyamp.ai'}/webhook/find-colleagues` with { company_name, company_domain: domain }
    - n8n returns array of people: [{ name, title, linkedin_url, email? }]
    - Cross-reference with existing contacts table: query contacts where company_id matches or email matches
    - Return { results: Array<{ name, title, linkedin_url, email, existsInCrm: boolean, contactId?: string }> }
    - On n8n error: return 502

    Create find-colleagues-modal.tsx:
    - Uses shadcn Dialog
    - Props: `open`, `onOpenChange`, `companyId: string`, `companyName: string`, `onContactsAdded: () => void`
    - On open: POST to find-colleagues API, show loading spinner
    - Display results as list with checkboxes: name, title, linkedin_url
    - Existing CRM contacts show "Already in CRM" badge and are unchecked by default
    - "Add Selected" button: POST new contacts to /api/lead-intelligence/contacts (existing CRUD endpoint) for each selected result
    - For existing contacts with `existsInCrm: true` that are checked: offer update via enrichment merge logic (fill blanks)
    - On success: toast.success(`Added ${N} colleagues`), call onContactsAdded
  </action>
  <verify>`cd dashboard && npx tsc --noEmit` passes</verify>
  <done>Find Colleagues triggers webhook, shows results with CRM badge, and adds selected contacts</done>
</task>

<task type="auto">
  <name>Task 2: Follow-up tasks and remaining wiring</name>
  <files>
    dashboard/src/app/api/lead-intelligence/contacts/[id]/follow-up/route.ts
    dashboard/src/app/api/lead-intelligence/bulk/follow-up/route.ts
    dashboard/src/app/dashboard/lead-intelligence/components/follow-up-form.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/bulk-confirm-dialog.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/contact-row-actions.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx
  </files>
  <action>
    Create contacts/[id]/follow-up/route.ts (POST):
    - validateApiKey
    - Accept body: { title: string, description?: string, due_date: string, priority?: 'low'|'medium'|'high' }
    - INSERT into follow_up_tasks: title, description, due_date, priority (default 'medium'), status='pending', contact_id=id
    - Return created task object

    Create bulk/follow-up/route.ts (POST):
    - Accept { contactIds: string[], title, description?, due_date, priority? }
    - Insert one follow_up_task per contactId (batch insert)
    - Return { success: true, created: N }

    Create follow-up-form.tsx:
    - Small Dialog with form fields: title (required), due date (required, date picker or input type="date"), description (optional textarea), priority (select: low/medium/high, default medium)
    - Props: `open`, `onOpenChange`, `contactIds: string[]`, `onSuccess: () => void`
    - For single contact: POST to contacts/{id}/follow-up
    - For multiple: POST to bulk/follow-up
    - On success: toast.success, call onSuccess

    Create bulk-confirm-dialog.tsx:
    - Reusable AlertDialog for confirming any bulk action
    - Props: `open`, `onOpenChange`, `title`, `description`, `onConfirm`, `loading`
    - Used by bulk enrich (plan 03) and any future bulk actions

    Wire into contact-row-actions.tsx:
    - Replace disabled "Set Follow-up" with action opening follow-up-form (single contact)
    - Replace disabled "Find Colleagues" with action opening find-colleagues-modal (uses contact's company_id)

    Wire into bulk-actions-bar.tsx:
    - "Set Follow-up" button opens follow-up-form with all selectedIds
    - On success: clear selection
  </action>
  <verify>`cd dashboard && npm run build` succeeds</verify>
  <done>Follow-up creation works single + bulk; Find Colleagues accessible from row actions; all row action placeholders replaced</done>
</task>

</tasks>

<verification>
- Build passes
- Find Colleagues row action opens modal (needs n8n webhook to fully test)
- Set Follow-up row action opens form, creates task
- Bulk Set Follow-up opens form for multiple contacts
- All 4 row action placeholders now functional
</verification>

<success_criteria>
INT-05, INT-06, BULK-03, API-09, API-10 complete: Find Colleagues discovers and adds contacts; follow-ups create tracked tasks.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-04-SUMMARY.md`
</output>

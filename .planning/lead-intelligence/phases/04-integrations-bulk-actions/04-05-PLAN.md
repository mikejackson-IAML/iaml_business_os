---
phase: 04-integrations-bulk-actions
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Build compiles with zero errors"
    - "All Phase 4 features are visually accessible"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 4 build and visually confirm all integrations and bulk actions work.

Purpose: Catch build errors and visual issues before marking phase complete.
Output: Verified build + human approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-01-SUMMARY.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-02-SUMMARY.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-03-SUMMARY.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and fix any issues</name>
  <files></files>
  <action>
    Run `cd dashboard && npm run build` and fix any TypeScript or build errors.
    Run `cd dashboard && npx tsc --noEmit` for type checking.
    Fix any issues found — likely candidates: missing imports, type mismatches on new props, unused variables.
  </action>
  <verify>`cd dashboard && npm run build` exits with code 0</verify>
  <done>Clean build with zero errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4: Integrations and Bulk Actions
    - Checkbox selection on contact table with bulk actions bar
    - Add to Campaign modal (SmartLead integration with duplicate detection)
    - Contact enrichment (single + bulk, fill-blanks merge, conflict flagging)
    - Find Colleagues (n8n webhook, results modal, add selected)
    - Follow-up task creation (single + bulk)
    - All row actions enabled (no more disabled placeholders)
  </what-built>
  <how-to-verify>
    1. Go to /dashboard/lead-intelligence
    2. Verify checkboxes appear on each contact row
    3. Select 2-3 contacts — bulk actions bar should appear with count
    4. Click "Add to Campaign" — modal should open and load campaigns from SmartLead
    5. Click row actions (three-dot menu) on any contact — all 4 actions should be enabled
    6. Click "Set Follow-up" from row actions — form should open with title, date, priority fields
    7. Navigate to a contact profile, Enrichment tab — "Enrich Now" button should be enabled
    8. Deselect all, verify bulk bar disappears
    9. Change page/filters, verify selection clears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- Human confirms all features accessible
</verification>

<success_criteria>
Phase 4 complete: all 12 requirements (LIST-05, INT-01-03, INT-05-06, BULK-01-03, API-07-10) verified.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-05-SUMMARY.md`
</output>

---
phase: 04-integrations-bulk-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/components/contact-table.tsx
  - dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx
  - dashboard/src/components/ui/dialog.tsx
  - dashboard/src/components/ui/checkbox.tsx
autonomous: true

must_haves:
  truths:
    - "Checkboxes appear on each contact row and in the header"
    - "Selecting contacts shows a bulk actions bar with count"
    - "Selection clears on page/filter/sort change"
  artifacts:
    - path: "dashboard/src/components/ui/dialog.tsx"
      provides: "Dialog component from shadcn"
    - path: "dashboard/src/components/ui/checkbox.tsx"
      provides: "Checkbox component from shadcn"
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx"
      provides: "Bulk actions bar with action buttons"
  key_links:
    - from: "lead-intelligence-content.tsx"
      to: "contact-table.tsx"
      via: "selectedIds state + callbacks"
      pattern: "selectedIds|onSelectOne|onSelectAll"
    - from: "lead-intelligence-content.tsx"
      to: "bulk-actions-bar.tsx"
      via: "selectedIds + clear callback"
      pattern: "selectedIds.*size.*>.*0"
---

<objective>
Add multi-select checkbox support to the contact table and a bulk actions bar that appears when contacts are selected.

Purpose: Foundation for all bulk operations (campaign, enrich, follow-up) — nothing else in Phase 4 works without selection.
Output: Checkbox column on contact table, selection state management, sticky bulk actions bar with placeholder buttons.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/ROADMAP.md
@.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-RESEARCH.md

@dashboard/src/app/dashboard/lead-intelligence/components/contact-table.tsx
@dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and add checkbox selection to contact table</name>
  <files>
    dashboard/src/components/ui/dialog.tsx
    dashboard/src/components/ui/checkbox.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/contact-table.tsx
  </files>
  <action>
    Run `cd dashboard && npx shadcn@latest add dialog checkbox` to generate Dialog and Checkbox components.

    Modify contact-table.tsx:
    - Add a checkbox column as the first column in the table header (select all) and each row (select one)
    - Accept new props: `selectedIds: Set<string>`, `onSelectOne: (id: string, checked: boolean) => void`, `onSelectAll: (checked: boolean) => void`
    - Header checkbox shows indeterminate state when some (not all) are selected
    - Use the shadcn Checkbox component for consistent styling
    - Do NOT move any existing table logic — just add the checkbox column
  </action>
  <verify>TypeScript compiles: `cd dashboard && npx tsc --noEmit`</verify>
  <done>Contact table accepts selection props and renders checkbox column</done>
</task>

<task type="auto">
  <name>Task 2: Add selection state and bulk actions bar</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/bulk-actions-bar.tsx
  </files>
  <action>
    Create bulk-actions-bar.tsx:
    - Sticky bar (fixed bottom or top of table area) showing "{N} contacts selected"
    - Three buttons: "Add to Campaign", "Enrich Selected", "Set Follow-up" — all disabled/placeholder for now (onClick will be wired in later plans)
    - "Clear Selection" button/link
    - Accept props: `selectedCount: number`, `onClear: () => void`, `onAddToCampaign: () => void`, `onBulkEnrich: () => void`, `onBulkFollowUp: () => void`
    - Use Tailwind for styling: bg-primary text-white rounded shadow, animate in/out

    Modify lead-intelligence-content.tsx:
    - Add `const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set())`
    - Add handleSelectAll and handleSelectOne callbacks per research Pattern 2
    - Clear selectedIds on searchParams or aiFilters change (useEffect)
    - Pass selection props to ContactTable
    - Render BulkActionsBar when selectedIds.size > 0
    - Wire onClear to clear selection; other callbacks as no-ops for now
  </action>
  <verify>`cd dashboard && npm run build` succeeds</verify>
  <done>Selecting contacts shows bulk actions bar with count; changing page/filters clears selection</done>
</task>

</tasks>

<verification>
- `cd dashboard && npm run build` passes
- Contact table renders checkbox column (visible in browser)
- Selecting rows shows bulk actions bar with correct count
- Select all checks all visible rows
- Changing filters/page clears selection
</verification>

<success_criteria>
LIST-05 complete: users can select multiple contacts via checkboxes and see bulk actions bar.
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/04-integrations-bulk-actions/04-01-SUMMARY.md`
</output>

---
phase: 03-ai-search-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/components/ai-search-bar.tsx
  - dashboard/src/app/dashboard/lead-intelligence/components/filter-pills.tsx
  - dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
autonomous: true

must_haves:
  truths:
    - "User can type a natural language query and see a loading shimmer"
    - "AI returns structured filters displayed as removable pills"
    - "User can remove individual filter pills"
    - "If AI cannot parse, user sees friendly error with rephrase suggestion"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/ai-search-bar.tsx"
      provides: "AI search input with rotating placeholder and shimmer"
      min_lines: 60
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/filter-pills.tsx"
      provides: "Removable filter pill display"
      min_lines: 40
  key_links:
    - from: "ai-search-bar.tsx"
      to: "/api/lead-intelligence/ai/parse-search"
      via: "fetch POST on submit"
      pattern: "fetch.*ai/parse-search"
    - from: "filter-pills.tsx"
      to: "lead-intelligence-content.tsx"
      via: "callback to update URL params / contact list filters"
      pattern: "onRemove|onClear"
    - from: "lead-intelligence-content.tsx"
      to: "ai-search-bar.tsx"
      via: "renders AI search bar and passes filter callback"
      pattern: "AISearchBar|AiSearchBar"
---

<objective>
Add AI-powered natural language search to the contact list page with removable filter pills.

Purpose: Users can search contacts conversationally instead of manually setting filters.
Output: AI search bar component, filter pills component, integrated into contact list page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/ROADMAP.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-RESEARCH.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-CONTEXT.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-01-SUMMARY.md
@dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
@dashboard/src/lib/api/lead-intelligence-ai-types.ts
@dashboard/src/lib/api/lead-intelligence-contacts-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI search bar + filter pills components</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/ai-search-bar.tsx
    dashboard/src/app/dashboard/lead-intelligence/components/filter-pills.tsx
  </files>
  <action>
1. Create `ai-search-bar.tsx`:
   - Input field with Sparkles icon (lucide-react) indicating AI
   - Rotating placeholder text cycling through examples every 3-4 seconds: "Try: attorneys in Florida", "Try: VIP contacts at large companies", "Try: directors with high engagement", "Try: leads with invalid emails". Use framer-motion AnimatePresence for smooth transitions.
   - On Enter/submit: POST to /api/lead-intelligence/ai/parse-search with { query }
   - While loading: show inline shimmer animation below the search bar (framer-motion). Keep the search bar and table visible. Text: "Understanding your search..."
   - On success: call onFiltersApplied(filters: Partial<ContactListParams>) callback
   - On error (AI returns { error, suggestion }): show toast via sonner with the suggestion text
   - On network/timeout error: show toast "Search failed, try again"
   - Props: { onFiltersApplied: (filters: Partial<ContactListParams>) => void; isLoading?: boolean }
   - Style: match existing filter area aesthetic. Tailwind classes.

2. Create `filter-pills.tsx`:
   - Renders active filters as removable pills/badges
   - Props: { filters: Partial<ContactListParams>; onRemove: (key: string) => void; onClearAll: () => void }
   - Each pill shows: human-readable label (e.g., "Status: Customer", "State: FL", "Seniority: Director") with X button
   - Map ContactListParams keys to display labels (status -> "Status", seniority_level -> "Seniority", etc.)
   - Map values to display values (c_suite -> "C-Suite", is_vip true -> "VIP Only", etc.)
   - "Clear all" button when 2+ pills active
   - Animate pill removal with framer-motion (exit animation)
   - Only render when there are active AI-applied filters (not manual filters from the existing filter panel)
  </action>
  <verify>TypeScript compiles: cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -20</verify>
  <done>Both components render, search bar calls API and returns filters, pills display with remove functionality</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AI search into contact list page</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/lead-intelligence-content.tsx
  </files>
  <action>
1. Read the existing lead-intelligence-content.tsx to understand current structure.

2. Add AI search bar above the existing filters/table area:
   - Import AISearchBar and FilterPills components
   - Add state for AI-applied filters: useState<Partial<ContactListParams>>({})
   - Add loading state for AI search
   - Place AISearchBar at the top of the page content (above metrics bar or between metrics and filters)
   - Place FilterPills below search bar, above table
   - When AI filters applied: merge with URL params / existing filter state to trigger contact list refetch
   - When pill removed: remove that key from AI filters, update contact list
   - When clear all: reset AI filters to {}
   - Ensure existing manual filters still work alongside AI filters (they should combine)

3. Wire the filter flow:
   - AI search returns filters -> set AI filter state -> this triggers contact list to refetch with new params
   - Use the existing filter/URL-param mechanism if the page uses useAdvancedFilters or similar
   - If the page fetches contacts via a hook or function, pass merged filters (manual + AI) to that function
  </action>
  <verify>
    Build passes: cd dashboard && npm run build 2>&1 | tail -10
    Visit /dashboard/lead-intelligence -- AI search bar visible above filters
  </verify>
  <done>AI search bar visible on contact list page. Typing a query and pressing Enter calls the API, returns filters as pills, and filters the contact list. Removing a pill updates the list. Existing manual filters still work.</done>
</task>

</tasks>

<verification>
- Contact list page shows AI search bar with rotating placeholder
- Submitting a query shows shimmer, then filter pills appear
- Each pill has an X to remove it
- Removing all pills restores unfiltered list
- Existing manual filters continue to work
- Build passes without errors
</verification>

<success_criteria>
- AI search bar renders with rotating placeholder examples
- Query submission shows loading shimmer then filter pills
- Pills are removable individually and via "clear all"
- Failed parse shows toast with suggestion
- Contact list updates in response to AI filter changes
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/03-ai-search-intelligence/03-02-SUMMARY.md`
</output>

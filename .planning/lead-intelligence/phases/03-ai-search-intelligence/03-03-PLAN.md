---
phase: 03-ai-search-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/components/ai-summary-card.tsx
  - dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx
autonomous: true

must_haves:
  truths:
    - "Contact profile Overview tab shows AI-generated intelligence summary"
    - "Summary covers attendance, satisfaction, engagement, company context"
    - "Summary is cached and shows age indicator"
    - "User can regenerate summary on demand"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/components/ai-summary-card.tsx"
      provides: "AI summary display with headline, expandable sections, regenerate button"
      min_lines: 80
  key_links:
    - from: "ai-summary-card.tsx"
      to: "/api/lead-intelligence/ai/generate-summary"
      via: "fetch POST on mount and on regenerate"
      pattern: "fetch.*ai/generate-summary"
    - from: "overview-tab.tsx"
      to: "ai-summary-card.tsx"
      via: "renders AISummaryCard component"
      pattern: "AISummaryCard|AiSummaryCard"
---

<objective>
Add AI-generated intelligence summary to the contact profile Overview tab with caching and regeneration.

Purpose: Users see an AI narrative about each contact's history, engagement, and suggested actions.
Output: AI summary card component integrated into the overview tab.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/ROADMAP.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-RESEARCH.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-CONTEXT.md
@.planning/lead-intelligence/phases/03-ai-search-intelligence/03-01-SUMMARY.md
@dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx
@dashboard/src/lib/api/lead-intelligence-ai-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI summary card component</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/components/ai-summary-card.tsx
  </files>
  <action>
1. Create `ai-summary-card.tsx`:
   - Props: { contactId: string }
   - On mount: POST to /api/lead-intelligence/ai/generate-summary with { contactId } (NOT force)
   - This returns cached summary if available, or generates fresh one
   - Loading state: skeleton/shimmer card with Sparkles icon and "Generating intelligence summary..." text. Use framer-motion for shimmer.
   - Display structure:
     a. Card header: Sparkles icon + "AI Intelligence Summary" title + age indicator ("Generated 3 days ago") + Regenerate button (RefreshCw icon from lucide-react)
     b. Headline: the summary.headline as a prominent paragraph at top, conversational tone
     c. Expandable sections: each summary.sections item as a collapsible section (click title to expand/collapse). Default: first section expanded, rest collapsed.
     d. Use Collapsible from radix or simple disclosure with ChevronDown icon
   - Age indicator styling: green text if < 7 days, yellow if 7-30 days, red/orange if > 30 days with "Stale - consider refreshing" text
   - Regenerate button: onClick -> POST with { contactId, force: true }. While regenerating, show spinner on button. Keep old summary visible during regeneration (don't show skeleton again).
   - Error state: if API fails, show "Couldn't generate summary" with retry button
   - Style: Card component from dashboard-kit. Conversational, friendly feel -- not clinical.

2. Ensure the component is client-side ('use client') since it fetches on mount with useEffect.
  </action>
  <verify>TypeScript compiles: cd dashboard && npx tsc --noEmit --pretty 2>&1 | head -20</verify>
  <done>AI summary card component renders with skeleton on load, shows headline + expandable sections, displays age, and has working regenerate button</done>
</task>

<task type="auto">
  <name>Task 2: Integrate summary into Overview tab</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/contacts/[id]/tabs/overview-tab.tsx
  </files>
  <action>
1. Read the existing overview-tab.tsx fully to understand current layout.

2. Add the AI summary card at the TOP of the Overview tab content (before existing activity/follow-up sections):
   - Import AISummaryCard from components/ai-summary-card
   - Render <AISummaryCard contactId={contactId} /> as the first element in the tab
   - The summary card handles its own data fetching, loading, and error states -- no prop drilling needed beyond contactId
   - Ensure it doesn't block the rest of the tab from rendering (it loads async via useEffect)

3. Keep all existing Overview tab content below the summary card unchanged.
  </action>
  <verify>
    Build passes: cd dashboard && npm run build 2>&1 | tail -10
    Visit a contact profile -> Overview tab shows AI summary card at top
  </verify>
  <done>Overview tab shows AI summary card at top. Summary auto-generates on first view. Regenerate button works. Age indicator displays. Rest of tab content unchanged below.</done>
</task>

</tasks>

<verification>
- Contact profile Overview tab shows AI summary card at the top
- First visit triggers summary generation (shows loading shimmer)
- Second visit shows cached summary with age indicator
- Regenerate button fetches fresh summary
- Expandable sections work (click to expand/collapse)
- Age indicator colors: green < 7d, yellow 7-30d, red > 30d
- Build passes without errors
</verification>

<success_criteria>
- AI summary card renders on Overview tab for any contact
- Summary displays headline + 4 expandable sections (Attendance, Satisfaction, Company Context, Next Steps)
- Caching works: second load is instant with "cached" indicator
- Regenerate button forces fresh summary
- Loading/error states handled gracefully
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/03-ai-search-intelligence/03-03-SUMMARY.md`
</output>

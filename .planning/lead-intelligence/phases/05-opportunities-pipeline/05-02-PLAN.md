---
phase: 05-opportunities-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-kanban.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/stage-column.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-card.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-table.tsx
  - dashboard/src/app/dashboard/lead-intelligence/opportunities/components/create-opportunity-modal.tsx
autonomous: true

must_haves:
  truths:
    - "User can view opportunities at /dashboard/lead-intelligence/opportunities"
    - "User can toggle between kanban board and table view"
    - "User can switch between In-House and Individual pipeline tabs"
    - "User can drag opportunities between stages on kanban board"
    - "User can create a new opportunity via modal"
  artifacts:
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx"
      provides: "Server component entry point"
      min_lines: 10
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx"
      provides: "Client component with tabs, view toggle, data fetching"
      min_lines: 80
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-kanban.tsx"
      provides: "DndContext kanban board with optimistic drag"
      min_lines: 60
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/components/stage-column.tsx"
      provides: "Droppable stage column"
      min_lines: 30
    - path: "dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-card.tsx"
      provides: "Draggable opportunity card"
      min_lines: 30
  key_links:
    - from: "opportunity-kanban.tsx"
      to: "/api/lead-intelligence/opportunities"
      via: "fetch for data + advance-stage on drag-end"
      pattern: "api/lead-intelligence/opportunities"
    - from: "opportunity-kanban.tsx"
      to: "@dnd-kit/core"
      via: "DndContext, useSensors, PointerSensor"
      pattern: "DndContext"
    - from: "create-opportunity-modal.tsx"
      to: "/api/lead-intelligence/opportunities"
      via: "POST fetch on submit"
      pattern: "method.*POST"
---

<objective>
Build the opportunities list page with kanban board (default), table view, pipeline tabs, drag-and-drop stage advancement, and create opportunity modal.

Purpose: The primary opportunities UI -- users land here to see pipeline status and manage deal flow. Covers OPP-01, OPP-02, OPP-03, OPP-04.
Output: 7 component files forming the complete opportunities list experience.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/lead-intelligence/PROJECT.md
@.planning/lead-intelligence/phases/05-opportunities-pipeline/05-CONTEXT.md
@.planning/lead-intelligence/phases/05-opportunities-pipeline/05-RESEARCH.md

# Clone kanban pattern from these:
@dashboard/src/app/dashboard/planning/components/pipeline-board.tsx
@dashboard/src/app/dashboard/planning/components/pipeline-column.tsx
@dashboard/src/app/dashboard/planning/components/project-card.tsx

# Types from Plan 01:
@dashboard/src/lib/api/lead-intelligence-opportunities-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kanban board components</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-kanban.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/stage-column.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-card.tsx
  </files>
  <action>
Clone the Planning Studio kanban pattern (pipeline-board, pipeline-column, project-card) and adapt for opportunities:

**opportunity-kanban.tsx:**
- Accept `opportunities: Opportunity[]` and `stages: string[]` and `stageLabels: Record<string, string>` props
- Group opportunities by stage into a map
- DndContext with PointerSensor (distance: 8 activation constraint) and KeyboardSensor
- rectIntersection collision detection
- onDragStart: set activeId state
- onDragEnd: extract opportunity id and new stage from event, optimistically update local state, POST to /api/lead-intelligence/opportunities/{id}/advance-stage with { stage }, revert on failure, show toast on success/failure
- DragOverlay with floating card (shadow-lg rotate-2)
- Horizontal scroll container: `flex gap-6 overflow-x-auto pb-4`
- Map stages to StageColumn components

**stage-column.tsx:**
- Accept stage key, label, color, and opportunities array
- useDroppable with id = stage key
- Drop highlight: `ring-2 ring-primary/30 bg-primary/5` when isOver
- Column header: colored dot + stage label + count badge
- Fixed width: `min-w-[280px] w-[280px] flex-shrink-0`
- Min height for empty columns: `min-h-[200px]`
- Stage colors: use distinct colors for each stage (green for won, red for lost, blues/purples for middle stages)

**opportunity-card.tsx:**
- Accept opportunity and optional isOverlay prop
- useDraggable with id = opportunity.id
- Display: title (bold), company name (if in-house) or contact name (if individual), value formatted as currency if present
- During drag: opacity-50. Overlay: shadow-lg rotate-2
- Clickable: navigate to /dashboard/lead-intelligence/opportunities/{id} on click (but not during drag)
- Card styling: bg-card border rounded-lg p-3, hover shadow
  </action>
  <verify>TypeScript compiles: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npx tsc --noEmit 2>&1 | head -20`</verify>
  <done>3 kanban component files exist, follow dnd-kit patterns from Planning Studio, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Page, content, table, and create modal</name>
  <files>
    dashboard/src/app/dashboard/lead-intelligence/opportunities/page.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/opportunities-content.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/opportunity-table.tsx
    dashboard/src/app/dashboard/lead-intelligence/opportunities/components/create-opportunity-modal.tsx
  </files>
  <action>
**page.tsx:**
- Server component, renders OpportunitiesContent client component
- Page title/metadata for opportunities

**opportunities-content.tsx (client, "use client"):**
- Tabs (Radix Tabs): "In-House" and "Individual" -- controls which pipeline is displayed
- View toggle: kanban (default) vs table icons in top-right
- Fetch opportunities from /api/lead-intelligence/opportunities?type={activeTab} using useEffect
- Loading skeleton while fetching
- "Create Opportunity" button in header, opens CreateOpportunityModal
- Pass filtered opportunities + correct stages/labels to OpportunityKanban or OpportunityTable based on view toggle
- Refetch after create/drag

**opportunity-table.tsx:**
- Table view showing: title, company/contact, stage (as badge with color), value, created date
- Horizontal stage progress bar at top showing count per stage (like a mini pipeline visualization)
- Sortable columns (title, value, created_at)
- Row click navigates to detail page
- Stage badge colors match kanban column colors

**create-opportunity-modal.tsx:**
- Radix Dialog modal
- Fields: title (required text), type (select: In-House / Individual -- pre-filled from active tab), company (type-ahead search for in-house, fetches from /api/lead-intelligence/companies?search=), contact (type-ahead for individual, fetches from /api/lead-intelligence/contacts?search=), value (optional number input)
- Stage defaults to 'inquiry' (not shown in form)
- Submit POSTs to /api/lead-intelligence/opportunities
- Toast on success, close modal, trigger parent refetch via callback prop
- If opened from company profile (via URL param or prop), auto-fill company
  </action>
  <verify>Build passes: `cd /Users/mikejackson/Documents/IAML/iaml_business_os/dashboard && npm run build 2>&1 | tail -20`</verify>
  <done>Opportunities page renders at /dashboard/lead-intelligence/opportunities with kanban board, table toggle, pipeline tabs, create modal, and drag-and-drop stage advancement</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- /dashboard/lead-intelligence/opportunities route exists
- Kanban renders columns for all stages in active pipeline
- Drag-and-drop calls advance-stage API
- Create modal submits and new opportunity appears on board
</verification>

<success_criteria>
- Opportunities list page fully functional (OPP-01)
- Pipeline visualization shows stage counts (OPP-02)
- User can create both opportunity types (OPP-03)
- Drag-and-drop advances stages (OPP-04)
</success_criteria>

<output>
After completion, create `.planning/lead-intelligence/phases/05-opportunities-pipeline/05-02-SUMMARY.md`
</output>
